# 19-02 Plan: Move ImmutableFloatTreeMap And Rewire Callers

## Objective
- Move `ImmutableFloatTreeMap` into `eyelibutil.collection` and update all root callers.

## Steps
1. Add `eyelibutil.collection.ImmutableFloatTreeMap` with imports updated to `eyelibutil.codec.CodecHelper`.
2. Update all root imports and fully-qualified references from `io.github.tt432.eyelib.util.ImmutableFloatTreeMap`.
3. Update all root codec imports to `io.github.tt432.eyelibutil.codec.*`.
4. Update `CoreUtilitySeamTest` to use `io.github.tt432.eyelibutil.codec.Eithers`.
5. Delete old root/core codec sources after rewiring succeeds.

## Verification
- Old root/core codec import search returns zero matches.
- `EitherHelper` search returns zero matches.
