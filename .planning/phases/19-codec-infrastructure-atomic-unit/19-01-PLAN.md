# 19-01 Plan: Move Codec Sources Into Util Module

## Objective
- Add canonical codec package sources to `:eyelib-util`.
- Preserve behavior while changing packages.

## Steps
1. Copy codec family sources into `eyelib-util/src/main/java/io/github/tt432/eyelibutil/codec/`.
2. Copy `Eithers` into the same canonical codec package.
3. Update package declarations and internal imports to the `eyelibutil` namespace.
4. Replace `EitherHelper::unwrap` with `Eithers::unwrap` in `CodecHelper`.
5. Keep `EitherHelper` out of the util module.

## Verification
- Compile diagnostics on moved util module codec sources.
