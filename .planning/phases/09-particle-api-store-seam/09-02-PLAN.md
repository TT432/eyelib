---
phase: 09-particle-api-store-seam
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - src/main/java/io/github/tt432/eyelib/client/manager/ParticleManager.java
  - src/main/java/io/github/tt432/eyelib/client/particle/ParticleLookup.java
  - src/main/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistry.java
  - src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java
  - src/main/java/io/github/tt432/eyelib/client/particle/README.md
  - src/main/java/io/github/tt432/eyelib/client/registry/README.md
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md
  - MODULES.md
autonomous: true
requirements: [PAPI-01, PAPI-03]
must_haves:
  truths:
    - "Existing root particle callers keep working through retained root facades."
    - "Retained root facades delegate to particle-module API seams and are documented as transitional with removal conditions."
    - "Particle publication still keys entries by `particle_effect.description.identifier`, not source/resource keys."
  artifacts:
    - path: "src/main/java/io/github/tt432/eyelib/client/manager/ParticleManager.java"
      provides: "Root backing adapter implementing particle-module store API"
    - path: "src/main/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistry.java"
      provides: "Transitional publication facade delegating to ParticlePublisher"
    - path: "src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java"
      provides: "Transitional spawn/remove adapter delegating through ParticleSpawnApi request seam"
  key_links:
    - from: "src/main/java/io/github/tt432/eyelib/client/particle/ParticleLookup.java"
      to: "io.github.tt432.eyelibparticle.api.ParticleLookupApi"
      via: "ParticleManager particle API store/read adapter"
      pattern: "ParticleLookupApi|ParticleStore"
    - from: "src/main/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistry.java"
      to: "io.github.tt432.eyelibparticle.api.ParticlePublisher"
      via: "description identifier extractor"
      pattern: "particleEffect\(\)\.description\(\)\.identifier\(\)"
---

<objective>
Rewire root compatibility surfaces into narrow particle-module APIs and document them as transitional.

Purpose: PAPI-01 and PAPI-03 require root to consume API/store/publication/spawn seams from `:eyelib-particle`, while retained root classes become named, deletable adapters rather than canonical particle owners.
Output: Delegating root adapters plus local documentation that names each transitional facade and its removal condition.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-particle-api-store-seam/09-CONTEXT.md
@.planning/phases/09-particle-api-store-seam/09-RESEARCH.md
@.planning/phases/09-particle-api-store-seam/09-PATTERNS.md
@.planning/phases/09-particle-api-store-seam/09-VALIDATION.md
@.planning/phases/09-particle-api-store-seam/09-01-SUMMARY.md
@MODULES.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
@eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md
@src/main/java/io/github/tt432/eyelib/client/particle/README.md
@src/main/java/io/github/tt432/eyelib/client/registry/README.md

<interfaces>
From Plan 09-01, use `io.github.tt432.eyelibparticle.api.ParticleStore`, `ParticleLookupApi`, `ParticlePublisher`, `ParticleSpawnApi`, and `ParticleSpawnRequest`. Root adapters may mention root runtime types (`BrParticle`, packets, render manager) because they live in root, but none of those types may be imported into `:eyelib-particle`.

Current behavior to preserve:
```java
// ParticleAssetRegistry replacement key rule
particles.forEach((ignored, particle) ->
        flattened.put(particle.particleEffect().description().identifier(), particle));

// ParticleLookup string-first read behavior
ParticleManager.readPort().get(id);

// ParticleSpawnService root runtime boundary
ParticleSpawnService.spawnFromPacket(packet);
ParticleSpawnService.removeEmitter(packet.removeId());
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make ParticleManager the root backing adapter for module store APIs</name>
  <files>src/main/java/io/github/tt432/eyelib/client/manager/ParticleManager.java, MODULES.md</files>
  <read_first>
    - `src/main/java/io/github/tt432/eyelib/client/manager/ParticleManager.java`
    - `.planning/phases/09-particle-api-store-seam/09-PATTERNS.md` `ParticleManager` section
    - `MODULES.md` module update rules and particle/runtime manager rows
  </read_first>
  <behavior>
    - `ParticleManager` remains the root storage implementation for current runtime `BrParticle` objects.
    - `ParticleManager` exposes/implements `ParticleStore<BrParticle>` from `io.github.tt432.eyelibparticle.api` so root code consumes the module-owned port.
    - Existing `readPort()` and `writePort()` callers remain source-compatible but are documented as compatibility accessors where touched.
  </behavior>
  <action>Update `ParticleManager` to implement or expose a `ParticleStore<BrParticle>` module API port without changing manager storage/event behavior. Preserve existing `Manager<BrParticle>` inheritance and singleton pattern. Add a narrow accessor such as `store()` or equivalent that returns the module API type, and keep `readPort()`/`writePort()` for existing callers. Update the relevant `MODULES.md` interactions if responsibility wording changes: root runtime manager is now the backing adapter, while `:eyelib-particle` owns the canonical API/store contract.</action>
  <verify>
    <automated>JetBrains MCP: `jetbrain_run_gradle_tasks` with taskNames `[":compileJava"]` exits 0.</automated>
    <automated>PowerShell static check: `ParticleManager.java` imports `io.github.tt432.eyelibparticle.api.ParticleStore` and still contains `extends Manager&lt;BrParticle&gt;` or equivalent existing manager inheritance.</automated>
  </verify>
  <acceptance_criteria>
    - Existing manager behavior and event publication remain intact.
    - Root now has a module API store port for `BrParticle` access.
    - `MODULES.md` remains accurate if module interactions changed.
  </acceptance_criteria>
  <done>`ParticleManager` is a root backing adapter for `ParticleStore<BrParticle>` and root compile passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delegate lookup and publication facades to particle APIs</name>
  <files>src/main/java/io/github/tt432/eyelib/client/particle/ParticleLookup.java, src/main/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistry.java, src/main/java/io/github/tt432/eyelib/client/particle/README.md, src/main/java/io/github/tt432/eyelib/client/registry/README.md, eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md</files>
  <read_first>
    - `src/main/java/io/github/tt432/eyelib/client/particle/ParticleLookup.java`
    - `src/main/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistry.java`
    - `.planning/phases/09-particle-api-store-seam/09-CONTEXT.md` transitional facade and publication decisions
    - `.planning/phases/09-particle-api-store-seam/09-PATTERNS.md` lookup, publisher, and docs sections
  </read_first>
  <behavior>
    - `ParticleLookup.get(String)`, `ParticleLookup.get(ResourceLocation)`, and `ParticleLookup.names()` delegate through the particle-module lookup/store API, with `ResourceLocation.toString()` adaptation kept only in this root facade.
    - `ParticleAssetRegistry.publishParticle` and `replaceParticles` delegate to `ParticlePublisher<BrParticle>` with extractor `particle.particleEffect().description().identifier()`.
    - README/Javadocs state `ParticleLookup` and `ParticleAssetRegistry` are transitional facades, name their particle API delegation targets, and state removal condition: remove after root callers migrate directly to `io.github.tt432.eyelibparticle.api` adapters/services.
  </behavior>
  <action>Refactor the root lookup and registry facades to call the module API seams created in Plan 09-01. Keep `ResourceLocation` overloads only in root as adapters. Preserve `replaceParticles` identifier semantics exactly: ignore loader/source map keys and use each `BrParticle` description identifier. Add Javadocs or package README entries for each retained facade; do not introduce a generic catch-all compatibility layer.</action>
  <verify>
    <automated>JetBrains MCP: `jetbrain_run_gradle_tasks` with taskNames `[":compileJava"]` exits 0.</automated>
    <automated>PowerShell static check: `ParticleAssetRegistry.java` contains `ParticlePublisher` and `particleEffect().description().identifier()`, while `ParticleLookup.java` contains `io.github.tt432.eyelibparticle.api` and keeps `ResourceLocation` only as root-boundary adaptation.</automated>
    <automated>PowerShell content check: particle and registry READMEs contain `transitional`, `io.github.tt432.eyelibparticle.api`, and `removal`.</automated>
  </verify>
  <acceptance_criteria>
    - Existing lookup and publication method signatures remain available to root callers.
    - Business logic for publication flattening routes through the particle module publisher seam.
    - Transitional documentation exists locally and in module README notes.
  </acceptance_criteria>
  <done>Lookup and publication root facades are delegation adapters and documentation names their transitional status/removal condition.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delegate spawn/remove through module request API without moving runtime internals</name>
  <files>src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java, src/main/java/io/github/tt432/eyelib/client/particle/README.md</files>
  <read_first>
    - `src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java`
    - `src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnRequest.java`
    - `.planning/phases/09-particle-api-store-seam/09-CONTEXT.md` deferred runtime/render extraction decisions
    - `docs/architecture/02-side-boundaries.md` client packet handler rules
  </read_first>
  <behavior>
    - `spawnFromPacket(SpawnParticlePacket)` converts packet fields into module `ParticleSpawnRequest` and delegates to a root adapter implementing `ParticleSpawnApi`.
    - Runtime work (`Minecraft`, `DataAttachmentHelper`, `BrParticleEmitter`, `BrParticleRenderManager`) remains inside root `ParticleSpawnService` or a root-private nested adapter.
    - `removeEmitter(String)` delegates through `ParticleSpawnApi.remove(String)` while preserving existing `BrParticleRenderManager.removeEmitter` behavior in root adapter code.
  </behavior>
  <action>Introduce a narrow root binding to `ParticleSpawnApi` inside `ParticleSpawnService` and route public static spawn/remove methods through it. Keep packet classes and render manager calls in root. Do not move `BrParticleEmitter`, `BrParticleRenderManager`, packet handlers, capability helpers, loader code, or Bedrock runtime definitions to `:eyelib-particle` in this phase. Document `ParticleSpawnService` as a transitional root runtime adapter in the particle README with delegation target and removal condition.</action>
  <verify>
    <automated>JetBrains MCP: `jetbrain_run_gradle_tasks` with taskNames `[":compileJava"]` exits 0.</automated>
    <automated>PowerShell static check: `ParticleSpawnService.java` imports `ParticleSpawnApi` and module `ParticleSpawnRequest`, still contains root runtime imports for `Minecraft`/`BrParticleRenderManager`, and `eyelib-particle/src/main/java` contains none of those root/runtime imports.</automated>
  </verify>
  <acceptance_criteria>
    - Packet-driven spawn/remove behavior remains string-keyed and root-runtime backed.
    - Public root facade delegates through module request API.
    - No runtime/render implementation moves occur.
  </acceptance_criteria>
  <done>`ParticleSpawnService` delegates through `ParticleSpawnApi` while preserving root-only runtime/render internals and documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root compatibility facade -> particle API | Existing root call sites enter retained static facades that must delegate to module contracts. |
| packet/runtime adapter -> spawn API | Network packet data becomes a string-keyed particle API request before root runtime work. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Tampering | `ParticleAssetRegistry` | mitigate | Preserve `particle_effect.description.identifier` extraction and test/static-check that source map keys are ignored. |
| T-09-04 | Denial of Service | `ParticleSpawnService` / module boundary | mitigate | Keep MC/client-only runtime imports in root adapter and static-check that `:eyelib-particle` remains root/MC/Forge-clean. |
| T-09-05 | Repudiation | transitional root facades | mitigate | Javadocs/READMEs name delegation target and removal condition so future maintainers can audit why each facade exists. |
</threat_model>

<verification>
Use JetBrains MCP Gradle tools only for compile checks. Required commands: `jetbrain_run_gradle_tasks` for `:eyelib-particle:compileJava` after module API dependency changes if needed, then root `:compileJava` after adapter rewiring. Use PowerShell content/static checks for delegation and documentation. Do not run shell Gradle.
</verification>

<success_criteria>
- PAPI-01: Root lookup, store/publication, lifecycle, and spawn/remove paths consume module-owned API seams.
- PAPI-03: Retained root facades are documented as transitional and delegate to particle API targets.
- String-keyed particle semantics and identifier-keyed publication are preserved.
- `:eyelib-particle` has no reverse dependency on root runtime packages.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Root runtime uses narrow APIs instead of owning internals | 02 | COVERED | Root classes become backing/delegating adapters.
REQ | PAPI-01 | Root access through particle APIs | 02 | COVERED | Manager, lookup, registry, spawn service delegate/implement API seams.
REQ | PAPI-03 | Temporary facades delegate and are documented | 02 | COVERED | READMEs/Javadocs required.
RESEARCH | — | Preserve `description.identifier` publication keys | 02 | COVERED | Publisher extractor and static checks required.
CONTEXT | D-STORE-01 | Particle module owns store port/API | 02 | COVERED | `ParticleManager` becomes backing adapter.
CONTEXT | D-FACADE-01 | Specific named deletable facades only | 02 | COVERED | No broad compatibility layer; documentation required.
</source_audit>

<output>
After completion, create `.planning/phases/09-particle-api-store-seam/09-02-SUMMARY.md`.
</output>
