---
phase: 09-particle-api-store-seam
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/api/ParticlePublisherTest.java
  - eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/api/ParticleSpawnRequestTest.java
  - src/test/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistryTest.java
  - src/test/java/io/github/tt432/eyelib/client/particle/ParticleApiDelegationBoundaryTest.java
  - .planning/phases/09-particle-api-store-seam/09-VALIDATION.md
autonomous: true
requirements: [PAPI-01, PAPI-03]
must_haves:
  truths:
    - "Automated tests prove particle publication uses particle description identifiers and not loader/source keys."
    - "Automated/static checks prove root facades delegate and transitional docs exist."
    - "Automated/static checks prove `:eyelib-particle` API remains root-clean."
  artifacts:
    - path: "eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/api/ParticlePublisherTest.java"
      provides: "Module API publication behavior coverage"
    - path: "src/test/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistryTest.java"
      provides: "Root registry identifier-key regression coverage"
    - path: "src/test/java/io/github/tt432/eyelib/client/particle/ParticleApiDelegationBoundaryTest.java"
      provides: "Delegation/docs/static boundary coverage"
  key_links:
    - from: "src/test/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistryTest.java"
      to: "src/main/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistry.java"
      via: "mismatched source key vs particle description identifier fixture"
      pattern: "description identifier"
    - from: "src/test/java/io/github/tt432/eyelib/client/particle/ParticleApiDelegationBoundaryTest.java"
      to: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/api"
      via: "source-file static import/content checks"
      pattern: "forbidden imports"
---

<objective>
Add targeted tests/static checks that lock Phase 9 API/store/facade seam behavior.

Purpose: PAPI-01/PAPI-03 are not complete unless compile and automated checks prove root facades delegate, transitional docs exist, reverse dependencies are absent, and string-keyed publication behavior is preserved.
Output: JUnit coverage plus updated validation status for Phase 9 gates.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-particle-api-store-seam/09-CONTEXT.md
@.planning/phases/09-particle-api-store-seam/09-RESEARCH.md
@.planning/phases/09-particle-api-store-seam/09-PATTERNS.md
@.planning/phases/09-particle-api-store-seam/09-VALIDATION.md
@.planning/phases/09-particle-api-store-seam/09-01-SUMMARY.md
@.planning/phases/09-particle-api-store-seam/09-02-SUMMARY.md
@MODULES.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test particle module publisher and spawn request contracts</name>
  <files>eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/api/ParticlePublisherTest.java, eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/api/ParticleSpawnRequestTest.java</files>
  <read_first>
    - `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/api/ParticlePublisher.java`
    - `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/api/ParticleSpawnRequest.java`
    - `.planning/phases/09-particle-api-store-seam/09-PATTERNS.md` test patterns section
  </read_first>
  <behavior>
    - `ParticlePublisherTest` uses an in-memory `ParticleStore<String>` and an identifier extractor that maps values to distinct IDs; `replaceParticles` must overwrite stale entries and key by extractor result.
    - `publishParticle` writes exactly one entry under the extracted string identifier.
    - `ParticleSpawnRequestTest` proves `spawnId` and `particleId` are strings, nulls are rejected, and `Vector3f` is defensively copied.
  </behavior>
  <action>Add focused JUnit Jupiter tests under the particle module. Keep fixtures simple and generic so no root `BrParticle` or root package import enters `:eyelib-particle` tests. Use project-local style from `ManagerStorageTest` and `SpawnParticlePacketTest`. If test task discovery is unavailable, still express verification through JetBrains MCP Gradle task invocation rather than shell Gradle.</action>
  <verify>
    <automated>JetBrains MCP: `jetbrain_run_gradle_tasks` with taskNames `[":eyelib-particle:test"]` exits 0.</automated>
    <automated>JetBrains MCP: `jetbrain_run_gradle_tasks` with taskNames `[":eyelib-particle:compileJava"]` exits 0.</automated>
  </verify>
  <acceptance_criteria>
    - Module tests fail if publisher uses source iteration keys instead of extracted identifiers.
    - Module tests fail if spawn request stops preserving string IDs or defensive-copy semantics.
    - Particle module tests contain no root runtime imports.
  </acceptance_criteria>
  <done>Particle module API behavior tests exist and pass through JetBrains MCP Gradle tasks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test root facade delegation, identifier-key publication, docs, and boundary invariants</name>
  <files>src/test/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistryTest.java, src/test/java/io/github/tt432/eyelib/client/particle/ParticleApiDelegationBoundaryTest.java, .planning/phases/09-particle-api-store-seam/09-VALIDATION.md</files>
  <read_first>
    - `src/main/java/io/github/tt432/eyelib/client/registry/ParticleAssetRegistry.java`
    - `src/main/java/io/github/tt432/eyelib/client/particle/ParticleLookup.java`
    - `src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java`
    - `src/main/java/io/github/tt432/eyelib/client/particle/README.md`
    - `src/main/java/io/github/tt432/eyelib/client/registry/README.md`
    - `.planning/phases/09-particle-api-store-seam/09-VALIDATION.md`
  </read_first>
  <behavior>
    - `ParticleAssetRegistryTest` parses or constructs a `BrParticle` fixture whose loader/source key intentionally differs from `particle_effect.description.identifier`; after replacement, `ParticleManager` contains the description identifier and not the source key.
    - `ParticleApiDelegationBoundaryTest` statically reads relevant source files and asserts root facades mention `io.github.tt432.eyelibparticle.api`, retained facade docs contain `transitional` plus removal condition wording, and particle module API source contains none of the forbidden root/MC/Forge imports.
    - `09-VALIDATION.md` status rows for 09-01-01 through 09-02-02 are updated to reference the implemented test/static checks without marking them green unless the executor actually ran the checks successfully.
  </behavior>
  <action>Add root-side regression/static tests without weakening existing tests. Use existing codec fixture style from `ClientLookupFacadeTest` for `BrParticle` construction if needed. Static checks may use `java.nio.file.Files` inside JUnit so the verification remains automated and portable. Update `09-VALIDATION.md` with concrete file/test names and leave runtime-sensitive hardware/client behavior to later Phase 14; do not move command/network/render behavior in this validation plan.</action>
  <verify>
    <automated>JetBrains MCP: `jetbrain_run_gradle_tasks` with taskNames `[":test"]` exits 0, or if root full tests are too broad for the executor environment, run the narrow root test task/filter available through JetBrains MCP and record exact task/filter in the summary.</automated>
    <automated>JetBrains MCP: `jetbrain_run_gradle_tasks` with taskNames `[":compileJava", ":eyelib-particle:compileJava"]` exits 0.</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail if root registry publication uses source keys instead of particle description identifiers.
    - Tests fail if retained root facades lose delegation imports/targets or transitional documentation.
    - Tests fail if `:eyelib-particle` API imports root runtime, network, capability, mc/impl, Minecraft, or Forge classes.
    - Validation strategy names the concrete tests/checks created in this phase.
  </acceptance_criteria>
  <done>Root regression/static tests and validation updates prove PAPI-01/PAPI-03 seam behavior and boundary documentation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests -> source boundary assertions | Static tests inspect source files to prevent dependency direction drift. |
| loader/source map -> particle publication | Parsed particles arrive with incidental source keys but must publish by description identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | `ParticleAssetRegistryTest` | mitigate | Regression fixture uses mismatched source key and description identifier to detect key-selection drift. |
| T-09-07 | Denial of Service | boundary static test | mitigate | Automated source checks reject root/MC/Forge imports in particle module API. |
| T-09-08 | Repudiation | transitional docs test | mitigate | Static test requires transitional/removal wording so retained facades are auditable. |
</threat_model>

<verification>
Use JetBrains MCP Gradle tools only. Required phase gate after tests: `jetbrain_run_gradle_tasks` for `:eyelib-particle:test`, root test task containing the new tests, `:eyelib-particle:compileJava`, and `:compileJava`. Do not run shell Gradle.
</verification>

<success_criteria>
- PAPI-01 has automated coverage for module API behavior and root publication/lookup/spawn seam delegation.
- PAPI-03 has automated coverage for delegation-only transitional documentation.
- Static tests prove no reverse dependency from `:eyelib-particle` to root runtime packages.
- Phase 9 validation file points to concrete tests/checks created by the phase.
</success_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Root runtime can use narrow APIs with proof | 03 | COVERED | Tests/static checks prove behavior.
REQ | PAPI-01 | API seam behavior and string-keyed semantics | 03 | COVERED | Module/root tests cover publisher, request, and registry key rules.
REQ | PAPI-03 | Transitional delegating facades documented | 03 | COVERED | Static test requires delegation/docs.
RESEARCH | — | Boundary/static checks for forbidden imports | 03 | COVERED | Static JUnit source checks planned.
CONTEXT | D-VERIFY-01 | Compile plus static checks proving delegation and no reverse dependency | 03 | COVERED | MCP Gradle plus JUnit/static checks required.
</source_audit>

<output>
After completion, create `.planning/phases/09-particle-api-store-seam/09-03-SUMMARY.md`.
</output>
