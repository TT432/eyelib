---
phase: 17-tier-1-category-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build.gradle
  - eyelib-util/README.md
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/time/SimpleTimer.java
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/time/FixedStepTimerState.java
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/color/ColorEncodings.java
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/loader/SharedLibraryLoader.java
  - eyelib-util/src/test/java/io/github/tt432/eyelibutil/time/FixedStepTimerStateTest.java
  - eyelib-util/src/test/java/io/github/tt432/eyelibutil/color/ColorEncodingsTest.java
  - src/main/java/io/github/tt432/eyelib/client/render/texture/NativeImageIO.java
  - src/test/java/io/github/tt432/eyelib/core/util/CoreUtilitySeamTest.java
autonomous: true
requirements: [MIGR-01]
must_haves:
  truths:
    - "Root can resolve the new :eyelib-util project before any migrated imports are introduced."
    - "Time, color, and SharedLibraryLoader classes compile from io.github.tt432.eyelibutil packages."
    - "NativeImageIO uses the migrated ColorEncodings class without retaining the old core util import."
  artifacts:
    - path: "build.gradle"
      provides: "root dependency edge to :eyelib-util"
      contains: "project(':eyelib-util')"
    - path: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/time/FixedStepTimerState.java"
      provides: "migrated fixed-step timer state"
      contains: "package io.github.tt432.eyelibutil.time;"
    - path: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/color/ColorEncodings.java"
      provides: "migrated color channel encoding helper"
      contains: "package io.github.tt432.eyelibutil.color;"
    - path: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/loader/SharedLibraryLoader.java"
      provides: "migrated native library loader at locked package target"
      contains: "package io.github.tt432.eyelibutil.loader;"
  key_links:
    - from: "src/main/java/io/github/tt432/eyelib/client/render/texture/NativeImageIO.java"
      to: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/color/ColorEncodings.java"
      via: "import io.github.tt432.eyelibutil.color.ColorEncodings"
      pattern: "import io\\.github\\.tt432\\.eyelibutil\\.color\\.ColorEncodings;"
---

<objective>
Move the Phase 17 time, color, and loader utilities into `:eyelib-util` and add the root dependency edge needed for all later Phase 17 consumer rewires.

Purpose: Establish the build-classpath and the first migrated utility packages without touching Phase 18 resource/texture, Phase 19 codec, or Phase 20 submodule-centralization files.
Output: Root consumes `:eyelib-util`; `SimpleTimer`, `FixedStepTimerState`, `ColorEncodings`, and `SharedLibraryLoader` live under `io.github.tt432.eyelibutil.*` with behavior tests in the owning module.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
@docs/architecture/migration/utility-routing-manifest.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-tier-1-category-migration/17-CONTEXT.md
@.planning/phases/17-tier-1-category-migration/17-RESEARCH.md
@.planning/phases/17-tier-1-category-migration/17-VALIDATION.md
@.planning/phases/17-tier-1-category-migration/17-PATTERNS.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-02-SUMMARY.md
@eyelib-util/README.md
@eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md

<interfaces>
Root dependency pattern from `17-PATTERNS.md`:
```gradle
dependencies {
    api project(':eyelib-util')
    modImplementation project(':eyelib-util')
    jarJar project(':eyelib-util')
}
```

Locked decision trace:
- D-01: `SharedLibraryLoader` target package is `io.github.tt432.eyelibutil.loader`.
- D-02: `Lists.java` FastUtil migration is not in this plan; Plan 05 owns it and must prove `:eyelib-util:build`.
- D-03: `ListHelper` deletion is not in this plan; Plan 05 owns deletion after caller rewiring.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add root consumption edge for :eyelib-util</name>
  <files>build.gradle</files>
  <behavior>
    - Root declares `api project(':eyelib-util')`, `modImplementation project(':eyelib-util')`, and `jarJar project(':eyelib-util')` in the existing sibling-module dependency block.
    - `eyelib-util/build.gradle` remains a leaf module with zero `project(...)` dependencies.
  </behavior>
  <action>Add the root dependency entries for `:eyelib-util` using the same Forge-module pattern already used for attachment/material/particle/importer. Do not add any project dependency to `eyelib-util/build.gradle`; root depends on util, never util on root. This implements MIGR-01 classpath setup and preserves the Phase 16 leaf boundary.</action>
  <verify>
    <automated>JetBrains MCP: jetbrain_search_in_files_by_text(projectPath="E:\\_ideaProjects\\qylEyelib", directoryToSearch="", fileMask="build.gradle", searchText="project(':eyelib-util')", caseSensitive=true, maxUsageCount=10, timeout=60000) returns root build.gradle matches, and jetbrain_search_in_files_by_text(... directoryToSearch="eyelib-util", fileMask="build.gradle", searchText="project(", caseSensitive=true, maxUsageCount=10, timeout=60000) returns zero matches.</automated>
  </verify>
  <done>Root can consume `:eyelib-util`; util module still has no project-internal dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move time, color, and loader utilities into eyelibutil packages</name>
  <files>eyelib-util/src/main/java/io/github/tt432/eyelibutil/time/SimpleTimer.java, eyelib-util/src/main/java/io/github/tt432/eyelibutil/time/FixedStepTimerState.java, eyelib-util/src/main/java/io/github/tt432/eyelibutil/color/ColorEncodings.java, eyelib-util/src/main/java/io/github/tt432/eyelibutil/loader/SharedLibraryLoader.java, eyelib-util/src/test/java/io/github/tt432/eyelibutil/time/FixedStepTimerStateTest.java, eyelib-util/src/test/java/io/github/tt432/eyelibutil/color/ColorEncodingsTest.java, eyelib-util/README.md, eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md</files>
  <behavior>
    - `FixedStepTimerState` keeps the `rate > 0` constructor guard and one-fixed-step-per-call behavior.
    - `ColorEncodings.argbToAbgr(0x11223344)` returns `0x11443322`.
    - `SharedLibraryLoader` is package-moved only per D-01; native extraction/loading behavior is unchanged.
  </behavior>
  <action>Move `src/main/java/io/github/tt432/eyelib/util/SimpleTimer.java` to `eyelib-util/src/main/java/io/github/tt432/eyelibutil/time/SimpleTimer.java`; move `src/main/java/io/github/tt432/eyelib/core/util/time/FixedStepTimerState.java` to `.../time/FixedStepTimerState.java`; move `src/main/java/io/github/tt432/eyelib/core/util/color/ColorEncodings.java` to `.../color/ColorEncodings.java`; move `src/main/java/io/github/tt432/eyelib/util/SharedLibraryLoader.java` to `.../loader/SharedLibraryLoader.java` per D-01. Update only package declarations and imports required by the move; do not rewrite algorithms. Move `FixedStepTimerStateTest` to the util module and add `ColorEncodingsTest` there using the behavior above. Update util README package lists to include `time`, `color`, and `loader` as active migrated packages instead of scaffold-only wording.</action>
  <verify>
    <automated>JetBrains MCP: jetbrain_run_gradle_tasks(projectPath="E:\\_ideaProjects\\qylEyelib", externalProjectPath="E:\\_ideaProjects\\qylEyelib", taskNames=[":eyelib-util:build"], scriptParameters="", timeoutMillis=240000) exits 0.</automated>
  </verify>
  <done>Moved files exist in `eyelib-util`; old time/color/loader source files are absent from root/core source paths; util module tests cover timer and color behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rewire color consumers without migrating later-phase utilities</name>
  <files>src/main/java/io/github/tt432/eyelib/client/render/texture/NativeImageIO.java, src/test/java/io/github/tt432/eyelib/core/util/CoreUtilitySeamTest.java</files>
  <behavior>
    - `NativeImageIO` imports `io.github.tt432.eyelibutil.color.ColorEncodings` and keeps its pixel conversion calls unchanged.
    - `CoreUtilitySeamTest` no longer imports the old core color path; Phase 18/19 assertions remain on their old owners until those phases.
  </behavior>
  <action>Update `NativeImageIO` and the root seam test so Phase 17 color references point to `io.github.tt432.eyelibutil.color.ColorEncodings`. Do not touch `TexturePaths`, `ResourceLocations`, `Eithers`, `EitherHelper`, or any `util/codec/**` import; those are outside Phase 17.</action>
  <verify>
    <automated>JetBrains MCP: jetbrain_search_regex(projectPath="E:\\_ideaProjects\\qylEyelib", q="import\\s+io\\.github\\.tt432\\.eyelib\\.core\\.util\\.color\\.ColorEncodings;", paths=["src/main/java/**", "src/test/java/**"], limit=20) returns zero results; then jetbrain_build_project(projectPath="E:\\_ideaProjects\\qylEyelib", filesToRebuild=["src/main/java/io/github/tt432/eyelib/client/render/texture/NativeImageIO.java", "src/test/java/io/github/tt432/eyelib/core/util/CoreUtilitySeamTest.java"], rebuild=false, timeout=120000) succeeds.</automated>
  </verify>
  <done>No old core `ColorEncodings` imports remain in source/test code; no Phase 18/19 files were migrated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root build -> :eyelib-util | Root consumes classes from the new utility module through Gradle dependency edges. |
| OS/native library path -> SharedLibraryLoader | Existing native extraction/loading behavior touches filesystem and process library loading. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | `SharedLibraryLoader` | mitigate | Move package only per D-01; preserve existing extraction/loading code and leave `AUDT-F01` audit scope untouched. |
| T-17-02 | Elevation of Privilege | `eyelib-util/build.gradle` | mitigate | Keep `:eyelib-util` leaf by verifying zero `project(` in its build file. |
| T-17-03 | Tampering | color import rewiring | mitigate | Use exact `ColorEncodings` import scans so the old core path cannot silently shadow the new util owner. |
</threat_model>

<verification>
Use JetBrains MCP only. Required gates: `:eyelib-util:build` exit 0, targeted IDE build for color consumers succeeds, no shell Gradle command is run, and residual old color/time/loader source paths are absent.
</verification>

<success_criteria>
- `SimpleTimer`, `FixedStepTimerState`, `ColorEncodings`, and `SharedLibraryLoader` exist only under `eyelib-util/src/main/java/io/github/tt432/eyelibutil/{time,color,loader}`.
- Root `build.gradle` consumes `:eyelib-util`; util `build.gradle` remains free of project dependencies.
- `NativeImageIO` and root seam tests compile against `io.github.tt432.eyelibutil.color.ColorEncodings`.
</success_criteria>

<output>
After completion, create `.planning/phases/17-tier-1-category-migration/17-01-SUMMARY.md`.
</output>
