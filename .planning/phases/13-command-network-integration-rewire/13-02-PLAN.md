---
phase: 13-command-network-integration-rewire
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/SpawnParticlePacket.java
  - src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/RemoveParticlePacket.java
  - src/main/java/io/github/tt432/eyelib/mc/impl/network/EyelibNetworkTransport.java
  - src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java
  - src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java
  - src/test/java/io/github/tt432/eyelib/network/SpawnParticlePacketTest.java
  - src/test/java/io/github/tt432/eyelib/network/RemoveParticlePacketTest.java
  - src/test/java/io/github/tt432/eyelib/network/ParticleNetworkDelegationBoundaryTest.java
autonomous: true
requirements:
  - PNET-02
  - PNET-03
must_haves:
  truths:
    - "D-09: Spawn/remove packet payloads remain string-keyed."
    - "D-10: Packet DTO/codec ownership stays under `src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/` with `FriendlyByteBuf` usage outside pure `:eyelib-particle`."
    - "D-11: Packet registration and transport stay in `EyelibNetworkTransport`/network integration ownership while preserving current registration-order expectations."
    - "D-12: PFUT-02 packet-contract relocation remains deferred; Phase 13 introduces no broad new transport module."
    - "D-13/D-14: `NetClientHandlers` routes particle packets into `ParticleSpawnService` instead of render/loading internals."
    - "D-15: `ParticleSpawnRequest` remains the module-owned request seam; no duplicate root request type is introduced."
    - "D-16: Missing definition, player, or client level remains a no-op at the root runtime adapter boundary."
    - "D-17: Particle ids crossing command, lookup, request, and packet seams stay as strings."
    - "D-18: Packet spawn lookup uses active `ParticleDefinition.identifier()` values published by Phase 12 loading/publication."
    - "D-19: Packet spawn lookup resolves through active module definitions, not the legacy root `ParticleManager` map or root `BrParticle` canonical state."
  artifacts:
    - path: "src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/SpawnParticlePacket.java"
      provides: "String-keyed spawn packet DTO and codec"
      contains: "String particleId"
    - path: "src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java"
      provides: "Context-free client packet routing"
      contains: "ParticleSpawnService.spawnFromPacket(packet)"
    - path: "src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java"
      provides: "Root runtime adapter into module particle API/client services"
      contains: "new ParticleSpawnRequest(packet.spawnId(), packet.particleId(), packet.position())"
  key_links:
    - from: "EyelibNetworkTransport.register"
      to: "NetClientHandlers"
      via: "onClientHandle for RemoveParticlePacket and SpawnParticlePacket"
      pattern: "onClientHandle\\(NetClientHandlers::onSpawnParticlePacket\\)"
    - from: "NetClientHandlers.onSpawnParticlePacket"
      to: "ParticleSpawnService.spawnFromPacket"
      via: "packet delegation"
      pattern: "ParticleSpawnService\\.spawnFromPacket\\(packet\\)"
    - from: "ParticleSpawnService.spawnFromPacket"
      to: "io.github.tt432.eyelibparticle.api.ParticleSpawnRequest"
      via: "string-keyed request seam"
      pattern: "new ParticleSpawnRequest\\(packet\\.spawnId\\(\\), packet\\.particleId\\(\\), packet\\.position\\(\\)\\)"
---

<objective>
Preserve string-keyed spawn/remove packet behavior and thin network-handler delegation into particle services.

Purpose: Phase 13 must satisfy PNET-02 and the network side of PNET-03 without moving packet codecs into pure particle packages or exposing render internals to handlers.
Output: Targeted packet/handler/source-boundary tests and minimal network adapter rewires where required.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-command-network-integration-rewire/13-CONTEXT.md
@.planning/phases/13-command-network-integration-rewire/13-RESEARCH.md
@.planning/phases/13-command-network-integration-rewire/13-PATTERNS.md
@docs/index/network.md
@docs/architecture/02-side-boundaries.md
@src/main/java/io/github/tt432/eyelib/mc/impl/network/EyelibNetworkTransport.java
@src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java
@src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java
@src/test/java/io/github/tt432/eyelib/network/SpawnParticlePacketTest.java

<interfaces>
Packet contracts:
```java
public record SpawnParticlePacket(String spawnId, String particleId, Vector3f position)
public record RemoveParticlePacket(String removeId)
```

Network handler contract:
```java
public static void onRemoveParticlePacket(RemoveParticlePacket packet) {
    ParticleSpawnService.removeEmitter(packet.removeId());
}
public static void onSpawnParticlePacket(SpawnParticlePacket packet) {
    ParticleSpawnService.spawnFromPacket(packet);
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add packet shape and codec ownership tests</name>
  <files>src/test/java/io/github/tt432/eyelib/network/SpawnParticlePacketTest.java, src/test/java/io/github/tt432/eyelib/network/RemoveParticlePacketTest.java, src/test/java/io/github/tt432/eyelib/network/ParticleNetworkDelegationBoundaryTest.java</files>
  <read_first>
    - src/test/java/io/github/tt432/eyelib/network/SpawnParticlePacketTest.java
    - src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/SpawnParticlePacket.java
    - src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/RemoveParticlePacket.java
    - src/main/java/io/github/tt432/eyelib/mc/impl/network/EyelibNetworkTransport.java
    - src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java
  </read_first>
  <behavior>
    - Test 1: `SpawnParticlePacket` accepts arbitrary string `particleId` such as `not-a-resource-location`, preserving D-09/D-17.
    - Test 2: `RemoveParticlePacket` carries arbitrary string `removeId`, preserving D-09.
    - Test 3: Source scan confirms `SpawnParticlePacket` record signature contains `String spawnId`, `String particleId`, `Vector3f position`, and string codec encode/decode calls, preserving D-09/D-10.
    - Test 4: Source scan confirms `RemoveParticlePacket` record signature contains `String removeId` and string codec encode/decode calls, preserving D-09/D-10.
    - Test 5: Source scan confirms `EyelibNetworkTransport.register()` still registers remove then spawn packet handlers through `NetClientHandlers`, preserving D-11.
  </behavior>
  <action>Create `RemoveParticlePacketTest` and `ParticleNetworkDelegationBoundaryTest` under package `io.github.tt432.eyelib.network`; extend `SpawnParticlePacketTest` without weakening its current arbitrary-string assertion. Do not instantiate Minecraft client runtime. Use `Files.readString(Path.of(...))` source assertions for codec ownership and registration order. This task implements D-09, D-10, D-11, D-12, and D-17.</action>
  <verify>
    <automated>JetBrains MCP `jetbrain_run_gradle_tasks(projectPath="E:/_ideaProjects/qylEyelib", taskNames=[":test"], scriptParameters="--tests io.github.tt432.eyelib.network.SpawnParticlePacketTest --tests io.github.tt432.eyelib.network.RemoveParticlePacketTest --tests io.github.tt432.eyelib.network.ParticleNetworkDelegationBoundaryTest", timeoutMillis=120000)` exits 0.</automated>
  </verify>
  <acceptance_criteria>
    - `RemoveParticlePacketTest.java` exists and contains `not-a-resource-location` or another non-`ResourceLocation` arbitrary string assertion for `removeId`.
    - `ParticleNetworkDelegationBoundaryTest.java` contains `String particleId`, `String removeId`, `EyelibStreamCodecs.STRING.encode(obj.particleId, buf);`, `EyelibStreamCodecs.STRING.encode(obj.removeId, buf);`, `NetClientHandlers::onRemoveParticlePacket`, and `NetClientHandlers::onSpawnParticlePacket`.
  </acceptance_criteria>
  <done>Packet contracts are locked as string-keyed MC/network DTOs with codec ownership outside pure particle packages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock handler delegation and runtime no-op boundary</name>
  <files>src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java, src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java, src/test/java/io/github/tt432/eyelib/network/ParticleNetworkDelegationBoundaryTest.java</files>
  <read_first>
    - src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java
    - src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java
    - src/test/java/io/github/tt432/eyelib/client/particle/ParticleRuntimeDelegationBoundaryTest.java
    - src/main/java/io/github/tt432/eyelibparticle/api/ParticleSpawnRequest.java
  </read_first>
  <behavior>
    - `NetClientHandlers.onSpawnParticlePacket` contains exactly service delegation to `ParticleSpawnService.spawnFromPacket(packet)` and no `ParticleDefinitionRegistry`, `ParticleRenderManager`, loader, or legacy `BrParticle` direct access per D-13/D-14/D-19.
    - `NetClientHandlers.onRemoveParticlePacket` contains `ParticleSpawnService.removeEmitter(packet.removeId())` and no direct render-manager access per D-13.
    - `ParticleSpawnService.spawnFromPacket` constructs `new ParticleSpawnRequest(packet.spawnId(), packet.particleId(), packet.position())` per D-15/D-17.
    - `RootParticleSpawnApi.spawn` keeps the no-op guard `definition == null || Minecraft.getInstance().player == null || Minecraft.getInstance().level == null` per D-16.
  </behavior>
  <action>Update `ParticleNetworkDelegationBoundaryTest` to assert handler and service source boundaries above. Make only minimal implementation edits if these assertions fail. Do not move render manager operations into `NetClientHandlers`; keep them in `ParticleSpawnService`/module client services. Do not create a duplicate root `ParticleSpawnRequest`. This task implements D-13, D-14, D-15, D-16, D-17, D-18, and D-19.</action>
  <verify>
    <automated>JetBrains MCP `jetbrain_run_gradle_tasks(projectPath="E:/_ideaProjects/qylEyelib", taskNames=[":test"], scriptParameters="--tests io.github.tt432.eyelib.network.ParticleNetworkDelegationBoundaryTest --tests io.github.tt432.eyelib.client.particle.ParticleRuntimeDelegationBoundaryTest", timeoutMillis=120000)` exits 0.</automated>
  </verify>
  <acceptance_criteria>
    - `NetClientHandlers.java` contains `ParticleSpawnService.spawnFromPacket(packet);` and `ParticleSpawnService.removeEmitter(packet.removeId());`.
    - `NetClientHandlers.java` does not contain `ParticleRenderManager`, `ParticleDefinitionRegistry`, `BrParticleLoader`, or `BrParticleRenderManager`.
    - `ParticleSpawnService.java` contains `new ParticleSpawnRequest(packet.spawnId(), packet.particleId(), packet.position())` and the missing-definition/player/level guard.
  </acceptance_criteria>
  <done>Spawn/remove packets route through a thin handler into the root particle service adapter, and missing runtime state remains a safe no-op.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network byte buffer → packet DTO | Remote packet bytes decode into spawn/remove packet records. |
| Packet DTO → client handler | Decoded packet crosses from MC transport into context-free root network routing. |
| Client handler → particle service | Packet state crosses into root particle service and module runtime/client APIs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | `SpawnParticlePacket` / `RemoveParticlePacket` | mitigate | Keep packet fields string-keyed and codec-owned in MC/network layer; do not reinterpret ids as `ResourceLocation` in DTOs. |
| T-13-05 | Denial of Service | `ParticleSpawnService.RootParticleSpawnApi.spawn` | mitigate | Preserve missing definition/player/level no-op guard before emitter creation. |
| T-13-06 | Elevation of Privilege | `NetClientHandlers` | mitigate | Keep handler as delegation-only; no loader/registry/render-manager direct access in packet routing. |
</threat_model>

<verification>
Run targeted network tests through JetBrains MCP after each task. Do not run Gradle in shell.
</verification>

<success_criteria>
- PNET-02 packet string-key behavior is covered for spawn and remove.
- PNET-03 network/platform boundaries are covered by source-scan tests.
- Packet handlers remain context-free and do not expose render internals.
</success_criteria>

<output>
After completion, create `.planning/phases/13-command-network-integration-rewire/13-02-SUMMARY.md`.
</output>
