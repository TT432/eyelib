# 20-03 Plan: Docs And Build Verification

## Objective
- Update docs and verify attachment/material/full project builds.

## Steps
1. Update `eyelib-util` docs for `streamcodec` ownership.
2. Update module inventory and architecture docs for dependency direction.
3. Run `:eyelib-attachment:build`, `:eyelib-material:build`, and full project rebuild through JetBrains MCP.
4. Confirm `eyelib-util` remains leaf-only.

## Verification
- All three JetBrains MCP build gates pass.
