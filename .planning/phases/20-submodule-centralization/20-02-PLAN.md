# 20-02 Plan: Deduplicate Material DispatchedMapCodec

## Objective
- Remove material duplicate `DispatchedMapCodec` implementations and use the util module canonical codec.

## Steps
1. Add `implementation project(':eyelib-util')` to `eyelib-material`.
2. Rewire material main/test imports to `io.github.tt432.eyelibutil.codec.DispatchedMapCodec`.
3. Delete both material duplicate `DispatchedMapCodec` files.
4. Verify old material duplicate package imports return zero matches.

## Verification
- Material build passes through JetBrains MCP.
