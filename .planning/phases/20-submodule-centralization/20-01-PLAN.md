# 20-01 Plan: Move Attachment StreamCodec Suite

## Objective
- Move the attachment stream codec suite into `eyelibutil.streamcodec` and rewire consumers.

## Steps
1. Add `implementation project(':eyelib-util')` to `eyelib-attachment`.
2. Move the four stream codec files from `eyelib-attachment/.../codec/stream/` to `eyelib-util/.../streamcodec/` using IDE semantic move.
3. Verify root and attachment imports now point to `io.github.tt432.eyelibutil.streamcodec`.
4. Update attachment README ownership wording.

## Verification
- Old `io.github.tt432.eyelibattachment.codec.stream` imports return zero Java matches.
