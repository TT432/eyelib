---
phase: 10-schema-runtime-ownership-adapter
plan: 02
type: execute
wave: 2
depends_on:
  - 01
files_modified:
  - MODULES.md
  - docs/architecture/01-module-boundaries.md
  - docs/architecture/02-side-boundaries.md
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md
  - src/main/java/io/github/tt432/eyelib/client/particle/README.md
  - eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionBoundaryTest.java
  - eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionDocumentationTest.java
autonomous: true
requirements:
  - PSCHEMA-01
  - PSCHEMA-02
  - PSCHEMA-03
read_first:
  - AGENTS.md
  - MODULES.md
  - docs/index/repo-map.md
  - docs/architecture/01-module-boundaries.md
  - docs/architecture/02-side-boundaries.md
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md
  - src/main/java/io/github/tt432/eyelib/client/particle/README.md
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/package-info.java
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinition.java
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapter.java
must_haves:
  truths:
    - "Canonical ownership documentation names importer/raw schema owner, particle runtime definition owner, and root legacy/non-canonical status."
    - "Duplicate BrParticle ownership cannot drift silently because source scans forbid a particle-module BrParticle duplicate."
    - "Particle runtime package remains free of root runtime, Minecraft, and Forge imports while intentionally allowing importer/molang schema dependencies."
    - "Mapped-field documentation and tests name every parity-critical field preserved by the adapter."
  artifacts:
    - path: "MODULES.md"
      provides: "Repository module inventory updated for Phase 10 particle runtime definition ownership"
      contains: "ParticleDefinition"
    - path: "docs/architecture/01-module-boundaries.md"
      provides: "Architecture ownership map updated from future runtime definitions to current ParticleDefinition/adapter ownership"
      contains: "root `client/particle/bedrock/BrParticle` is legacy"
    - path: "docs/architecture/02-side-boundaries.md"
      provides: "Side-boundary rule for particle runtime definition and adapter seam"
      contains: ":eyelib-particle"
    - path: "eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionBoundaryTest.java"
      provides: "Forbidden import and no duplicate BrParticle drift checks"
      contains: "record BrParticle"
    - path: "eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionDocumentationTest.java"
      provides: "Documentation invariant checks for owners and mapped fields"
      contains: "canonical raw schema"
  key_links:
    - from: "MODULES.md"
      to: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinition.java"
      via: "module inventory names current runtime definition ownership"
      pattern: "ParticleDefinition"
    - from: "ParticleDefinitionBoundaryTest.java"
      to: "eyelib-particle/src/main/java"
      via: "source walk rejects root/MC/Forge imports and BrParticle declarations"
      pattern: "Files.walk"
---

<objective>
Lock Phase 10 ownership and drift-prevention invariants in documentation and automated static tests.

Purpose: PSCHEMA-01/03 require maintainers to identify canonical owners and prevent duplicate `BrParticle` schema/runtime ownership from drifting silently after the adapter is introduced.
Output: Updated module/architecture/package docs plus source-scan tests for forbidden imports, duplicate `BrParticle`, and documentation invariants.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<read_first>
1. `AGENTS.md` — obey JetBrains MCP-only Gradle verification and documentation update rules.
2. `MODULES.md`, `docs/architecture/01-module-boundaries.md`, `docs/architecture/02-side-boundaries.md` — update only rows/rules whose responsibility changed in Phase 10.
3. `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md` and `src/main/java/io/github/tt432/eyelib/client/particle/README.md` — document particle module runtime ownership and legacy root status.
4. Plan 01 outputs: `ParticleDefinition.java`, `ParticleDefinitionAdapter.java`, and `runtime/package-info.java` — tests must assert these contracts rather than invent alternate names.
5. `src/test/java/io/github/tt432/eyelib/client/particle/ParticleApiDelegationBoundaryTest.java` if needed for the existing static source-scan `SourceCheck` pattern.
</read_first>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-CONTEXT.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-RESEARCH.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-PATTERNS.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-VALIDATION.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-01-SUMMARY.md
@MODULES.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md

<interfaces>
Plan 01 creates these contracts and this plan must preserve their names:
```java
package io.github.tt432.eyelibparticle.runtime;

public record ParticleDefinition(/* canonical runtime definition */) { ... }

public final class ParticleDefinitionAdapter {
    public static DataResult<ParticleDefinition> fromSchema(BrParticle schema) { ... }
}
```

Forbidden in `:eyelib-particle` main sources:
```text
record BrParticle
class BrParticle
import io.github.tt432.eyelib.client.
import io.github.tt432.eyelib.network.
import io.github.tt432.eyelib.capability.
import io.github.tt432.eyelib.mc.impl.
import net.minecraft.
import net.minecraftforge.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document canonical owners and legacy root status</name>
  <files>MODULES.md, docs/architecture/01-module-boundaries.md, docs/architecture/02-side-boundaries.md, eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md, src/main/java/io/github/tt432/eyelib/client/particle/README.md</files>
  <action>Update the existing particle/importer/root documentation to state all Phase 10 locked decisions: importer `io.github.tt432.eyelibimporter.particle.BrParticle` remains canonical raw Bedrock schema/codec owner; `:eyelib-particle` owns `io.github.tt432.eyelibparticle.runtime.ParticleDefinition` and `ParticleDefinitionAdapter`; root `src/main/java/io/github/tt432/eyelib/client/particle/bedrock/BrParticle.java` is legacy/non-canonical runtime adapter target until Phase 11/12. Name the allowed particle -> importer dependency for the adapter seam. Explicitly state that full runtime extraction is Phase 11, loader/publication rewire is Phase 12, command/network rewire is Phase 13; do not imply those behaviors moved in Phase 10. Document the mapped fields: identifier, format version, basic render material/texture, curves, events, raw components, billboard flipbook summary, and Molang value preservation.</action>
  <verify>
    <automated>JetBrains MCP only after Task 2 test exists: `jetbrain_run_gradle_tasks` with `taskNames=[":eyelib-particle:test"]`, `scriptParameters="--tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionDocumentationTest"`, timeout >= 120000; require exitCode 0.</automated>
  </verify>
  <done>Docs clearly identify canonical owners, allowed dependency direction, mapped fields, and root `BrParticle` legacy/non-canonical status without moving deferred Phase 11/12/13 behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add boundary and documentation drift tests</name>
  <files>eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionBoundaryTest.java, eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionDocumentationTest.java</files>
  <behavior>
    - Boundary test walks `eyelib-particle/src/main/java` and fails if any main source declares `record BrParticle` or `class BrParticle`.
    - Boundary test rejects root runtime, root network, root capability, root `mc/impl`, Minecraft, and Forge imports in particle module main sources.
    - Documentation test reads `MODULES.md`, both architecture docs, particle module README, runtime package-info, and root particle README; it asserts exact owner/legacy/mapped-field phrases.
    - Tests allow importer/molang imports because Phase 10 explicitly authorizes particle -> importer for the adapter seam.
  </behavior>
  <action>Create JUnit 5 static source-scan tests using the existing `Files.walk` and `SourceCheck` pattern from Phase 9 boundary tests. In `ParticleDefinitionBoundaryTest`, scan all Java files under `eyelib-particle/src/main/java`, not only the new runtime package. In `ParticleDefinitionDocumentationTest`, assert that docs contain unambiguous phrases for `canonical raw Bedrock particle schema`, `ParticleDefinition`, `ParticleDefinitionAdapter`, `legacy/non-canonical`, and the mapped-field list. Avoid brittle line-number assertions; assert content semantics. Do not use shell scripts or grep gates; keep checks in JUnit so JetBrains MCP Gradle verification can run them.</action>
  <verify>
    <automated>JetBrains MCP only: `jetbrain_run_gradle_tasks` with `taskNames=[":eyelib-particle:test"]`, `scriptParameters="--tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionBoundaryTest --tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionDocumentationTest"`, timeout >= 120000; require exitCode 0.</automated>
  </verify>
  <done>Static tests fail on duplicate particle-module `BrParticle`, forbidden root/MC/Forge imports, missing owner docs, missing root legacy status, or missing mapped-field invariants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Particle module main source → root/platform packages | Pure module code must not cross into root runtime, Minecraft, or Forge classes. |
| Documentation → future maintainers | Ambiguous ownership docs can cause future schema/runtime drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-04 | Elevation of Privilege / DoS | `:eyelib-particle` source boundary | mitigate | JUnit source scan rejects root/MC/Forge imports and duplicate `BrParticle` declarations. |
| T-10-05 | Tampering | Ownership documentation | mitigate | Documentation test asserts canonical owner, legacy status, and mapped-field invariants. |
| T-10-06 | Repudiation | Phase 10 scope boundaries | mitigate | Docs explicitly state Phase 11/12/13 deferrals so Phase 10 cannot be mistaken for runtime/loading/command/network migration. |
</threat_model>

<verification>
Use JetBrains MCP Gradle tools only. Required plan verification:
1. `:eyelib-particle:test --tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionBoundaryTest --tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionDocumentationTest` exits 0.
2. Wave gate after Plans 01-02: `:eyelib-particle:test :eyelib-particle:compileJava :compileJava` exits 0 via JetBrains MCP.
3. Do not run `./gradlew`, `gradle`, or any shell Gradle command.
</verification>

<acceptance_criteria>
- PSCHEMA-01: `MODULES.md`, architecture docs, particle module README, runtime package docs, and root particle README name canonical raw schema owner, canonical runtime definition owner, and root legacy status.
- PSCHEMA-02: docs and tests reference the named `ParticleDefinitionAdapter` seam and its mapped parity fields.
- PSCHEMA-03: automated tests prevent duplicate particle-module `BrParticle`, forbidden imports, and documentation drift from passing silently.
</acceptance_criteria>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
--- | --- | --- | --- | --- | ---
GOAL | — | Importer/raw particle schema and executable runtime particle definitions have explicit canonical owners and a tested conversion seam | 01-02 | COVERED | Plan 01 builds seam; Plan 02 locks docs/drift tests.
REQ | PSCHEMA-01 | Maintainer can identify canonical owner for importer/raw schema and runtime definitions | 01-02 | COVERED | Runtime package docs plus module/architecture/root docs.
REQ | PSCHEMA-02 | Runtime definitions created from importer/raw schema through named adapter with parity coverage | 01 | COVERED | `ParticleDefinitionAdapter.fromSchema` + real fixture parity test.
REQ | PSCHEMA-03 | Duplicate `BrParticle` ownership cannot drift silently | 01-02 | COVERED | Distinct `ParticleDefinition`, no duplicate `BrParticle`, boundary/documentation tests.
RESEARCH | — | Keep `BedrockResourceValue` in `ParticleDefinition` | 01 | COVERED | Task 1 raw components field.
RESEARCH | — | Root comparison only for safe fields, not root codec/component registry prerequisite | 01 | COVERED | Task 2 explicitly avoids full root comparison prerequisite.
RESEARCH | — | Use selected large real `witchspell.json` fixture | 01 | COVERED | Task 2 fixture parity.
RESEARCH | — | Add particle -> importer dependency, never particle -> root | 01-02 | COVERED | Build wiring and boundary test.
CONTEXT | D-01 | `:eyelib-importer` owns raw schema/codecs including importer `BrParticle` | 01-02 | COVERED | Package/docs/test invariants.
CONTEXT | D-02 | `:eyelib-particle` owns canonical runtime definition; root remains adapter/legacy target | 01-02 | COVERED | `ParticleDefinition` and docs.
CONTEXT | D-03 | Root `client/particle/bedrock/BrParticle` non-canonical legacy status | 02 | COVERED | Root and architecture docs.
CONTEXT | D-04 | Do not introduce another particle-module `BrParticle`; use `ParticleDefinition` | 01-02 | COVERED | Type name and boundary test.
CONTEXT | D-05 | Particle module provides named adapter from importer schema | 01 | COVERED | `ParticleDefinitionAdapter.fromSchema`.
CONTEXT | D-06 | Preserve identifier, render params, curves, events, raw components, flipbook, Molang/lifetime/remove parity fields | 01-02 | COVERED | Definition fields, fixture tests, mapped-field docs.
CONTEXT | D-07 | Fail loudly for invalid/missing required data | 01 | COVERED | `DataResult.error` tests.
CONTEXT | D-08 | Use large real fixture where practical | 01 | COVERED | Copied `witchspell.json` fixture.
CONTEXT | D-09 | Keep legacy root CODEC temporarily for compatibility/safe comparison | 01-02 | COVERED | No loader/runtime rewire; docs mark root legacy.
</source_audit>

<success_criteria>
Maintainers can read docs/tests and see exactly where raw schema, runtime definition, adapter conversion, and legacy root compatibility live; future changes cannot silently add a duplicate `BrParticle` or contaminate `:eyelib-particle` with root/Minecraft/Forge imports.
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema-runtime-ownership-adapter/10-02-SUMMARY.md`.
</output>
