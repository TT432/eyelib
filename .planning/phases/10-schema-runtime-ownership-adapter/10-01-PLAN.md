---
phase: 10-schema-runtime-ownership-adapter
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - eyelib-particle/build.gradle
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinition.java
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapter.java
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/package-info.java
  - eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapterTest.java
  - eyelib-particle/src/test/resources/io/github/tt432/eyelibparticle/runtime/fixtures/witchspell.json
autonomous: true
requirements:
  - PSCHEMA-01
  - PSCHEMA-02
  - PSCHEMA-03
read_first:
  - AGENTS.md
  - MODULES.md
  - docs/index/repo-map.md
  - docs/architecture/01-module-boundaries.md
  - docs/architecture/02-side-boundaries.md
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/api/package-info.java
  - eyelib-particle/build.gradle
  - eyelib-importer/src/main/java/io/github/tt432/eyelibimporter/particle/BrParticle.java
  - eyelib-importer/src/test/resources/io/github/tt432/eyelibimporter/addon/fixtures/microsoft-shapeshifter/resource_pack/shapeshifter/particles/witchspell.json
must_haves:
  truths:
    - "Maintainer can identify importer BrParticle as the canonical raw Bedrock particle schema owner and particle ParticleDefinition as the canonical module runtime definition owner."
    - "Runtime ParticleDefinition instances can be created from importer BrParticle through a named ParticleDefinitionAdapter seam."
    - "Adapter conversion preserves parity-critical identifier, render parameters, Molang-backed curves, events, raw components, and billboard flipbook summary for a real fixture."
    - "Invalid or incomplete schema input fails loudly through DataResult instead of silently producing partial runtime definitions."
  artifacts:
    - path: "eyelib-particle/build.gradle"
      provides: "Intentional particle -> importer/molang dependency for schema adapter only"
      contains: "project(':eyelib-importer')"
    - path: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinition.java"
      provides: "Canonical module runtime particle definition with a distinct non-BrParticle name"
      contains: "record ParticleDefinition"
    - path: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapter.java"
      provides: "Named importer schema to runtime definition conversion seam"
      contains: "DataResult<ParticleDefinition>"
    - path: "eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapterTest.java"
      provides: "Real-fixture parity and loud-failure coverage"
      contains: "witchspell"
  key_links:
    - from: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapter.java"
      to: "eyelib-importer/src/main/java/io/github/tt432/eyelibimporter/particle/BrParticle.java"
      via: "fromSchema(BrParticle schema) accepts importer schema only"
      pattern: "fromSchema\\(BrParticle schema\\)"
    - from: "eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapterTest.java"
      to: "eyelib-particle/src/test/resources/io/github/tt432/eyelibparticle/runtime/fixtures/witchspell.json"
      via: "decode real fixture with importer BrParticle.CODEC, then adapt"
      pattern: "BrParticle\\.CODEC"
---

<objective>
Create the Phase 10 runtime ownership seam inside `:eyelib-particle`: a distinct `ParticleDefinition` model plus named `ParticleDefinitionAdapter` from importer `BrParticle` schema.

Purpose: PSCHEMA-01/02 require explicit canonical owners and a conversion seam before later runtime/loading rewires. This plan implements that seam without moving full runtime/loading/command/network behavior.
Output: Particle runtime definition contracts, adapter implementation, fixture-backed parity tests, and Gradle dependency wiring.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<read_first>
1. `AGENTS.md` — obey JetBrains MCP-only Gradle verification; do not run shell Gradle.
2. `MODULES.md`, `docs/index/repo-map.md`, `docs/architecture/01-module-boundaries.md`, `docs/architecture/02-side-boundaries.md` — confirm affected modules and root-clean particle boundary.
3. `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md` and `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/api/package-info.java` — copy package documentation/nullness pattern.
4. `eyelib-importer/src/main/java/io/github/tt432/eyelibimporter/particle/BrParticle.java` — source of importer schema fields, `DataResult` style, `billboardFlipbook()` behavior, and raw component ownership.
5. `eyelib-importer/src/test/resources/io/github/tt432/eyelibimporter/addon/fixtures/microsoft-shapeshifter/resource_pack/shapeshifter/particles/witchspell.json` — real fixture source for parity test.
</read_first>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-CONTEXT.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-RESEARCH.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-PATTERNS.md
@.planning/phases/10-schema-runtime-ownership-adapter/10-VALIDATION.md
@.planning/phases/09-particle-api-store-seam/09-VERIFICATION.md
@MODULES.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md

<interfaces>
Existing patterns the executor should use directly:

From `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/api/package-info.java`:
```java
@NullMarked
package io.github.tt432.eyelibparticle.api;

import org.jspecify.annotations.NullMarked;
```

From `eyelib-importer/src/main/java/io/github/tt432/eyelibimporter/particle/BrParticle.java`:
```java
public record BrParticle(String formatVersion, ParticleEffect particleEffect) { ... }
public record ParticleEffect(Description description, Map<String, Curve> curves, Events events, Map<String, BedrockResourceValue> components) { ... }
public record Description(String identifier, BasicRenderParameters basicRenderParameters) { ... }
public record BasicRenderParameters(String material, String texture) { ... }
public Optional<BillboardFlipbook> billboardFlipbook() { ... }
```

Required new contract names:
```java
public record ParticleDefinition(/* distinct runtime definition, not BrParticle */) { ... }
public final class ParticleDefinitionAdapter {
    public static DataResult<ParticleDefinition> fromSchema(BrParticle schema) { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add runtime definition contract and adapter dependency wiring</name>
  <files>eyelib-particle/build.gradle, eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinition.java, eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapter.java, eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/runtime/package-info.java</files>
  <behavior>
    - `:eyelib-particle` intentionally depends on `:eyelib-importer` for importer schema/raw values and on `:eyelib-molang` only if public Molang value types require a direct dependency.
    - `ParticleDefinition` is the canonical module-level runtime definition name; do not create any `BrParticle` class/record under `io.github.tt432.eyelibparticle`.
    - `ParticleDefinitionAdapter.fromSchema(BrParticle schema)` returns `DataResult<ParticleDefinition>` and fails with a clear error for null schema, blank identifier, or missing render parameters.
    - Raw components remain `Map<String, BedrockResourceValue>` and are defensively copied/preserved; Molang curve/event data is preserved without parsing/recompiling expressions.
  </behavior>
  <action>Add `implementation project(':eyelib-importer')` to `eyelib-particle/build.gradle`; add `implementation project(':eyelib-molang')` only if Java compilation requires it for public Molang types used by imported schema fields. Create `io.github.tt432.eyelibparticle.runtime` with `package-info.java` documenting: importer `BrParticle` is canonical raw schema, particle `ParticleDefinition` is canonical runtime definition, root `client/particle/bedrock/BrParticle` is legacy/non-canonical until Phase 11/12. Implement `ParticleDefinition` as a record preserving `formatVersion`, `identifier`, basic render material/texture, curves, events, raw components as `BedrockResourceValue`, and optional billboard flipbook summary. Implement `ParticleDefinitionAdapter` as a named static adapter from importer schema to definition using `DataResult`; validate required identifier/render parameter fields and never silently drop parity-critical maps. Do not import root runtime packages, root managers/registries/packets/capabilities, Minecraft, Forge, or `mc/impl`.</action>
  <verify>
    <automated>JetBrains MCP only: `jetbrain_run_gradle_tasks` with `taskNames=[":eyelib-particle:compileJava"]`, `scriptParameters=""`, timeout >= 120000; require exitCode 0.</automated>
  </verify>
  <done>`ParticleDefinition` and `ParticleDefinitionAdapter` compile in `:eyelib-particle`; no `io.github.tt432.eyelibparticle.*BrParticle` type exists; adapter uses importer schema input and `DataResult` loud-failure semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove adapter parity with a large real particle fixture</name>
  <files>eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapterTest.java, eyelib-particle/src/test/resources/io/github/tt432/eyelibparticle/runtime/fixtures/witchspell.json</files>
  <behavior>
    - Real fixture decode uses importer `BrParticle.CODEC` and adapts with `ParticleDefinitionAdapter.fromSchema`.
    - Assertions cover identifier, format version, render material/texture, curve keys, events presence/content shape, raw component keys and selected nested values, prefixed billboard component handling, and flipbook summary values.
    - Invalid constructed schema cases prove blank identifier/missing render parameters fail loudly with `DataResult.error`.
  </behavior>
  <action>Copy the real fixture from `eyelib-importer/src/test/resources/io/github/tt432/eyelibimporter/addon/fixtures/microsoft-shapeshifter/resource_pack/shapeshifter/particles/witchspell.json` into the particle test fixture path with a source comment in the test naming the origin. Create `ParticleDefinitionAdapterTest` that decodes the copied JSON through importer `BrParticle.CODEC`, adapts it, and asserts the parity-critical fields listed above. Add small direct-record invalid cases rather than hand-rolling JSON parsing for failures. Keep root legacy comparison out of this task except for safe field names if already trivial; do not make root codec/component registry setup a prerequisite.</action>
  <verify>
    <automated>JetBrains MCP only: `jetbrain_run_gradle_tasks` with `taskNames=[":eyelib-particle:test"]`, `scriptParameters="--tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionAdapterTest"`, timeout >= 120000; require exitCode 0.</automated>
  </verify>
  <done>Real fixture parity passes; adapter preserves fields needed by loading/rendering/Molang/lifetime/remove behavior; invalid schema tests fail through a clear failure channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bedrock particle JSON → importer schema | Untrusted addon JSON is decoded by importer `BrParticle.CODEC`. |
| Importer schema → particle runtime definition | Raw schema data crosses into module-owned runtime definition through `ParticleDefinitionAdapter`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `ParticleDefinitionAdapter.fromSchema` | mitigate | Validate identifier/render parameters and return `DataResult.error` for invalid required data; tests assert loud failure. |
| T-10-02 | Denial of Service | Fixture parity tests | mitigate | Use selected real `witchspell.json` fixture, not whole pack traversal, so tests stay bounded. |
| T-10-03 | Elevation of Privilege / DoS | `:eyelib-particle` runtime package | mitigate | Keep imports root/MC/Forge-clean; Plan 02 adds boundary source scans. |
</threat_model>

<verification>
Use JetBrains MCP Gradle tools only. Required plan verification:
1. `:eyelib-particle:compileJava` exits 0 after runtime contracts and adapter compile.
2. `:eyelib-particle:test --tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionAdapterTest` exits 0 after fixture parity tests.
3. Do not run `./gradlew`, `gradle`, or any shell Gradle command.
</verification>

<acceptance_criteria>
- PSCHEMA-01: package docs and runtime type names establish `:eyelib-importer` raw schema ownership and `:eyelib-particle` runtime definition ownership.
- PSCHEMA-02: `ParticleDefinitionAdapter.fromSchema` converts importer `BrParticle` into `ParticleDefinition` with real-fixture parity coverage.
- PSCHEMA-03: no duplicate particle-module `BrParticle` is introduced by this plan; Plan 02 will lock this with static drift checks.
</acceptance_criteria>

<success_criteria>
The particle module has a root-clean, named schema-to-runtime conversion seam with a distinct `ParticleDefinition`, loud failure behavior, and real fixture parity proof, while root loader/runtime/command/network behavior remains untouched.
</success_criteria>

<output>
After completion, create `.planning/phases/10-schema-runtime-ownership-adapter/10-01-SUMMARY.md`.
</output>
