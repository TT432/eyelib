---
phase: 14-verification-documentation-gate
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/test/java/io/github/tt432/eyelib/docs/ParticleFinalDocumentationGateTest.java
  - src/test/java/io/github/tt432/eyelib/client/particle/ParticleFinalSplitBoundaryTest.java
  - eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/ParticleModuleFinalBoundaryTest.java
autonomous: true
requirements:
  - PVERIFY-01
must_haves:
  truths:
    - "D-01/D-02: Final tests cover the complete module split without weakening prior particle assertions."
    - "D-04/D-07: Tests use existing JUnit/source-scan style and do not depend on `.planning` artifacts."
    - "D-02/D-10: Boundary/parity/regression tests cover dependency direction, schema/runtime conversion, loading keys, command/network delegation, and side boundaries."
  artifacts:
    - path: "src/test/java/io/github/tt432/eyelib/docs/ParticleFinalDocumentationGateTest.java"
      provides: "Stable-doc drift test for final ownership and no `.planning` dependency."
    - path: "src/test/java/io/github/tt432/eyelib/client/particle/ParticleFinalSplitBoundaryTest.java"
      provides: "Root adapter/delegation and packet/command split gate."
    - path: "eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/ParticleModuleFinalBoundaryTest.java"
      provides: "Particle module final pure/client boundary gate."
  key_links:
    - from: "ParticleFinalDocumentationGateTest.java"
      to: "stable docs"
      via: "Files.readString over repository docs only"
      pattern: "readDocs\(.*MODULES.md"
    - from: "ParticleFinalSplitBoundaryTest.java"
      to: "ParticleSpawnService and NetClientHandlers"
      via: "source-scan delegation assertions"
      pattern: "ParticleSpawnService|NetClientHandlers"
    - from: "ParticleModuleFinalBoundaryTest.java"
      to: "eyelib-particle/src/main/java"
      via: "Files.walk source scans"
      pattern: "Files.walk"
---

<objective>
Add final-gate automated tests that prove the completed particle module split remains covered without weakening existing assertions.

Purpose: PVERIFY-01 requires moved/adapted particle tests and new boundary/parity/regression coverage for the full split. This plan implements D-01 through D-04 and D-07 by adding aggregate final tests that reuse existing JUnit 5 patterns and stable repository docs only.

Output: Three final gate test classes covering stable documentation, root adapter/delegation behavior, particle module boundaries, and no `.planning` dependency in normal tests.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/14-verification-documentation-gate/14-CONTEXT.md
@.planning/phases/14-verification-documentation-gate/14-RESEARCH.md
@.planning/phases/14-verification-documentation-gate/14-PATTERNS.md
@src/test/java/io/github/tt432/eyelib/client/particle/ParticleApiDelegationBoundaryTest.java
@src/test/java/io/github/tt432/eyelib/docs/ParticleCommandNetworkDocumentationTest.java
@eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionAdapterTest.java
@eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/loading/ParticleResourcePublicationTest.java
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md

<interfaces>
Test style from existing patterns:
```java
class ParticleFinalDocumentationGateTest {
    @Test
    void finalDocsDescribeParticleSplitClosureWithoutPlanningDependencies() throws IOException { }
}
```
Use `Files.readString(Path.of(path))`, `Files.walk(Path.of(...))`, `assertAll`, `assertTrue`, `assertFalse`, and private `SourceCheck`/`readDocs` helpers. Do not add Mockito or new test dependencies.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add stable documentation final gate test</name>
  <files>src/test/java/io/github/tt432/eyelib/docs/ParticleFinalDocumentationGateTest.java</files>
  <read_first>src/test/java/io/github/tt432/eyelib/docs/ParticleCommandNetworkDocumentationTest.java; MODULES.md; docs/index/repo-map.md; docs/architecture/01-module-boundaries.md; docs/architecture/02-side-boundaries.md; eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md; src/main/java/io/github/tt432/eyelib/client/particle/README.md; src/main/java/io/github/tt432/eyelib/network/README.md</read_first>
  <behavior>
    - Test 1: stable docs contain final owner anchors: `ParticleDefinitionRegistry`, `ParticleResourcePublication`, `ParticleDefinition.identifier()`, `ParticleDefinitionAdapter`, `io.github.tt432.eyelibimporter.particle.BrParticle`, `mc/impl/common/command`, `mc/impl/network/packet`, `SpawnParticlePacket(String spawnId, String particleId, Vector3f position)`, `RemoveParticlePacket(String removeId)`, `ClientSmoke`, `hardware`, `PFUT-02`, and `PFUT-03`.
    - Test 2: the final documentation gate test source itself does not contain `.planning/`, `VALIDATION.md`, `14-RESEARCH.md`, or `14-FINAL-GATE-EVIDENCE.md` as input paths.
  </behavior>
  <action>Create `ParticleFinalDocumentationGateTest` in package `io.github.tt432.eyelib.docs`. Follow `ParticleCommandNetworkDocumentationTest` style: package-private class, two package-private `@Test` methods, `readDocs(String... paths)`, and `assertAll`. Read only stable repository docs from D-06 plus `src/main/java/io/github/tt432/eyelib/mc/impl/common/command/README.md` and `src/main/java/io/github/tt432/eyelib/mc/impl/network/README.md` if needed for command/network anchors. Implement D-07 by adding a test that reads the new test source file and asserts it does not contain `.planning/`, `VALIDATION.md`, `14-RESEARCH.md`, or `14-FINAL-GATE-EVIDENCE.md` in read paths.</action>
  <verify>
    <automated>Use JetBrains MCP `jetbrain_run_gradle_tasks` with taskNames=[":test"] and scriptParameters="--tests io.github.tt432.eyelib.docs.ParticleFinalDocumentationGateTest"; require exitCode 0.</automated>
  </verify>
  <acceptance_criteria>The new test has at least two `@Test` methods; it reads stable docs only; it asserts all anchor strings listed in behavior; the targeted JetBrains MCP root test exits 0.</acceptance_criteria>
  <done>Stable documentation drift coverage exists for Phase 14 final ownership and no `.planning` dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add root adapter and module boundary final gate tests</name>
  <files>src/test/java/io/github/tt432/eyelib/client/particle/ParticleFinalSplitBoundaryTest.java, eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/ParticleModuleFinalBoundaryTest.java</files>
  <read_first>src/test/java/io/github/tt432/eyelib/client/particle/ParticleApiDelegationBoundaryTest.java; src/test/java/io/github/tt432/eyelib/client/particle/ParticleRuntimeDelegationBoundaryTest.java; src/test/java/io/github/tt432/eyelib/network/ParticleNetworkDelegationBoundaryTest.java; eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleDefinitionBoundaryTest.java; eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/runtime/ParticleRuntimeBoundaryTest.java; eyelib-particle/src/test/java/io/github/tt432/eyelibparticle/loading/ParticleLoadingBoundaryTest.java</read_first>
  <behavior>
    - Test 1: root final split gate confirms `NetClientHandlers` delegates spawn/remove only to `ParticleSpawnService`, packet DTO/codecs stay under `mc/impl/network/packet`, command adapter stays under `mc/impl/common/command`, and root compatibility adapters import/delegate to module APIs/loading/runtime instead of loader/render internals.
    - Test 2: particle module final gate confirms pure module packages outside `client/**` have no root runtime, root network, root capability, root `mc/impl`, Minecraft, or Forge imports; `client/**` is checked for documented client integration ownership and `Dist.CLIENT` side gating where Forge event hooks are present.
    - Test 3: final gate explicitly covers schema/runtime conversion and loading keys by asserting existing test sources `ParticleDefinitionAdapterTest` and `ParticleResourcePublicationTest` still contain real codec/fixture and `ParticleDefinition.identifier()`/source-key assertions.
  </behavior>
  <action>Create `ParticleFinalSplitBoundaryTest` in root package `io.github.tt432.eyelib.client.particle` and `ParticleModuleFinalBoundaryTest` in package `io.github.tt432.eyelibparticle`. Use existing source-scan helpers. For root assertions, read `src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java`, `src/main/java/io/github/tt432/eyelib/client/particle/ParticleSpawnService.java`, `src/main/java/io/github/tt432/eyelib/mc/impl/common/command/EyelibParticleCommand.java`, `src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/SpawnParticlePacket.java`, `src/main/java/io/github/tt432/eyelib/mc/impl/network/packet/RemoveParticlePacket.java`, and the root facade files. For module assertions, walk `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle`, skip `client` for pure-package checks, and separately assert `client/ParticleRenderHooks.java` contains `Dist.CLIENT` and delegates to `ParticleRenderManager`. For parity/key coverage, read existing test source files and assert they contain `BrParticle.CODEC.parse`, `ParticleDefinitionAdapter.fromSchema`, `ParticleDefinition.identifier`, `sourceKeyIsNotPublishedAsActiveIdentifier`, and `fullReplacementRemovesStaleEntries`. This implements D-01/D-02 without weakening existing tests.</action>
  <verify>
    <automated>Use JetBrains MCP `jetbrain_run_gradle_tasks` with taskNames=[":eyelib-particle:test", ":test"] and scriptParameters="--tests io.github.tt432.eyelibparticle.ParticleModuleFinalBoundaryTest --tests io.github.tt432.eyelib.client.particle.ParticleFinalSplitBoundaryTest --tests io.github.tt432.eyelibparticle.runtime.ParticleDefinitionAdapterTest --tests io.github.tt432.eyelibparticle.loading.ParticleResourcePublicationTest"; require exitCode 0.</automated>
  </verify>
  <acceptance_criteria>Final boundary tests include dependency direction, side-boundary, schema/runtime conversion, publication key, command/network delegation, and no `.planning` dependency assertions; targeted JetBrains MCP matrix exits 0.</acceptance_criteria>
  <done>Automated final split gate exists for PVERIFY-01 and covers all D-02 categories without broad runtime source work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source tests -> stable docs/source files | Tests trust repository files as the source of truth for final split invariants. |
| Particle module pure packages -> client/platform integration | Pure module packages must not import root/MC/Forge; client integration has documented side-gated exceptions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | Final JUnit tests | mitigate | Add tests that assert exact invariant strings and run through JetBrains MCP; preserve existing tests instead of deleting them. |
| T-14-02-02 | Elevation of Privilege | Particle module dependencies | mitigate | Source-scan pure module packages for forbidden root/MC/Forge imports and separately validate client side gating. |
| T-14-02-03 | Repudiation | Documentation drift coverage | mitigate | Test stable docs only and reject `.planning` dependencies in normal source tests. |
</threat_model>

<verification>
Run the two task-level JetBrains MCP `jetbrain_run_gradle_tasks` commands. Do not run Gradle through shell. If unrelated root fixture failures appear outside the targeted filters, record them for Plan 03 triage instead of weakening these tests.
</verification>

<success_criteria>
- PVERIFY-01 is covered by final documentation, root split, and particle module boundary tests.
- New tests use repository JUnit 5 style and real source/fixture assertions.
- Normal source tests do not read `.planning` artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/14-verification-documentation-gate/14-02-SUMMARY.md`.
</output>
