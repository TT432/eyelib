---
phase: 15-pre-migration-audit-routing
plan: 02
type: execute
wave: 1
depends_on:
  - 01
files_modified:
  - src/main/java/io/github/tt432/eyelib/client/animation/bedrock/BrAnimationEntry.java
  - src/main/java/io/github/tt432/eyelib/util/codec/TupleCodec.java
autonomous: true
requirements:
  - AUDIT-02
must_haves:
  truths:
    - "Production root Java source has zero wildcard imports from `io.github.tt432.eyelib.util` packages."
    - "BrAnimationEntry and TupleCodec keep their codec behavior while using explicit imports or qualified nested references."
  artifacts:
    - path: "src/main/java/io/github/tt432/eyelib/client/animation/bedrock/BrAnimationEntry.java"
      provides: "Bedrock animation entry codec without util wildcard imports"
      contains: "ChinExtraCodecs"
    - path: "src/main/java/io/github/tt432/eyelib/util/codec/TupleCodec.java"
      provides: "Tuple codec without `Tuple.*` wildcard import"
      contains: "Tuple"
  key_links:
    - from: "BrAnimationEntry.java"
      to: "io.github.tt432.eyelib.util.codec.ChinExtraCodecs"
      via: "explicit import"
      pattern: "import io.github.tt432.eyelib.util.codec.ChinExtraCodecs;"
    - from: "TupleCodec.java"
      to: "io.github.tt432.eyelib.util.codec.Tuple nested types"
      via: "explicit nested imports or qualified `Tuple.Tn` references"
      pattern: "import io.github.tt432.eyelib.util.codec.Tuple\\.(T2|Function3)"
---

<objective>
Remove util wildcard imports from the two known production files before package moves begin.

Purpose: Prevent wildcard imports from hiding real dependencies during Phase 15 routing and later `:eyelib-util` migrations.
Output: `BrAnimationEntry.java` and `TupleCodec.java` use explicit imports/qualified references and pass residual wildcard scans.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-pre-migration-audit-routing/15-CONTEXT.md
@.planning/phases/15-pre-migration-audit-routing/15-RESEARCH.md
@.planning/phases/15-pre-migration-audit-routing/15-VALIDATION.md
@.planning/phases/15-pre-migration-audit-routing/15-PATTERNS.md
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
@src/main/java/io/github/tt432/eyelib/client/animation/bedrock/BrAnimationEntry.java
@src/main/java/io/github/tt432/eyelib/util/codec/TupleCodec.java
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace BrAnimationEntry util wildcard imports</name>
  <read_first>
    - `src/main/java/io/github/tt432/eyelib/client/animation/bedrock/BrAnimationEntry.java` current imports and all referenced simple names.
    - `.planning/phases/15-pre-migration-audit-routing/15-PATTERNS.md` section `BrAnimationEntry.java`.
    - `src/main/java/io/github/tt432/eyelib/client/animation/README.md` before editing under the animation subtree.
    - `src/main/java/io/github/tt432/eyelib/client/model/README.md` if adding explicit model-domain imports.
  </read_first>
  <files>src/main/java/io/github/tt432/eyelib/client/animation/bedrock/BrAnimationEntry.java</files>
  <action>
    Replace `import io.github.tt432.eyelib.util.*;` and `import io.github.tt432.eyelib.util.codec.*;` with explicit class-level imports for every used util/codec symbol. The research-confirmed minimum explicit codec imports are `io.github.tt432.eyelib.util.codec.ChinExtraCodecs` and `io.github.tt432.eyelib.util.codec.CodecHelper`; add any additional explicit imports required by the current file after reading it. Also replace broad third-party/project wildcards in the same import block when touched by IDE optimize imports, but do not alter runtime logic, codecs, field names, or TODO comments. Prefer IDE import optimization; if editing manually, preserve Java syntax and current import order conventions.
  </action>
  <verify>
    <automated>`jetbrain_search_regex` over `src/main/java/io/github/tt432/eyelib/client/animation/bedrock/BrAnimationEntry.java` with pattern `import\s+io\.github\.tt432\.eyelib\.util(?:\.[A-Za-z0-9_]+)*\.\*;` returns zero results.</automated>
  </verify>
  <acceptance_criteria>
    - `BrAnimationEntry.java` contains no `io.github.tt432.eyelib.util...*` wildcard import.
    - `BrAnimationEntry.java` still imports or otherwise resolves `ChinExtraCodecs` and `CodecHelper` explicitly.
    - IDE diagnostics for `BrAnimationEntry.java` report no unresolved symbol errors after the import cleanup.
  </acceptance_criteria>
  <done>BrAnimationEntry no longer hides util dependencies behind wildcard imports.</done>
</task>

<task type="auto">
  <name>Task 2: Replace TupleCodec nested wildcard import</name>
  <read_first>
    - `src/main/java/io/github/tt432/eyelib/util/codec/TupleCodec.java` current imports and nested `Tuple` type usage.
    - `.planning/phases/15-pre-migration-audit-routing/15-PATTERNS.md` section `TupleCodec.java`.
    - `src/main/java/io/github/tt432/eyelib/util/README.md` and any package README in `src/main/java/io/github/tt432/eyelib/util/codec/` if present.
  </read_first>
  <files>src/main/java/io/github/tt432/eyelib/util/codec/TupleCodec.java</files>
  <action>
    Replace `import io.github.tt432.eyelib.util.codec.Tuple.*;` with explicit nested imports for every used `Tuple` nested type/function interface (`T2`, `T3`, ... up to the highest arity currently used, and `Function3`, ... up to the highest arity currently used) or qualify all nested references as `Tuple.T2`, `Tuple.Function3`, etc. Prefer explicit nested imports if IDE keeps the file readable. Also replace `import com.mojang.datafixers.util.*;` with explicit `com.mojang.datafixers.util.Pair` if it is the only used DataFixers type. Do not change codec logic, method signatures, generic arities, or tuple behavior.
  </action>
  <verify>
    <automated>`jetbrain_search_regex` over `src/main/java/io/github/tt432/eyelib/util/codec/TupleCodec.java` with pattern `import\s+io\.github\.tt432\.eyelib\.util\.codec\.Tuple\.\*;` returns zero results.</automated>
  </verify>
  <acceptance_criteria>
    - `TupleCodec.java` contains no `Tuple.*` wildcard import.
    - If `com.mojang.datafixers.util.*` existed, it is replaced by explicit used imports.
    - IDE diagnostics for `TupleCodec.java` report no unresolved symbol errors after the import cleanup.
  </acceptance_criteria>
  <done>TupleCodec's nested tuple dependencies are explicit and ready for Phase 19 codec migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wildcard imports → explicit imports | Hidden dependency set becomes explicit; missing imports can break compilation. |
| source edit → IDE/build diagnostics | Java import changes must be verified by IDE diagnostics/build, not text inspection alone. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Tampering | `BrAnimationEntry.java` imports | mitigate | Use IDE import optimization or diagnostics after replacing wildcard imports; do not change codec bodies. |
| T-15-05 | Tampering | `TupleCodec.java` nested tuple imports | mitigate | Keep tuple arity signatures unchanged and verify no unresolved symbols. |
| T-15-06 | Repudiation | wildcard cleanup evidence | mitigate | Record exact residual regex pattern and zero-match result in plan summary. |
</threat_model>

<verification>
Run JetBrains regex scans for util wildcard imports and IDE diagnostics on the two touched files. Do not run Gradle from shell.
</verification>

<success_criteria>
The regex `import\s+io\.github\.tt432\.eyelib\.util(?:\.[A-Za-z0-9_]+)*\.\*;` returns zero production source matches, and the touched files have no unresolved imports.
</success_criteria>

<output>
After completion, create `.planning/phases/15-pre-migration-audit-routing/15-02-SUMMARY.md`.
</output>
