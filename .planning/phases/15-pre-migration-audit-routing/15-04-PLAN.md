---
phase: 15-pre-migration-audit-routing
plan: 04
type: execute
wave: 2
depends_on:
  - 02
  - 03
files_modified:
  - MODULES.md
  - docs/architecture/01-module-boundaries.md
  - docs/architecture/02-side-boundaries.md
  - docs/architecture/migration/utility-routing-manifest.md
  - .planning/phases/15-pre-migration-audit-routing/15-VALIDATION.md
autonomous: true
requirements:
  - AUDIT-01
  - AUDIT-02
  - ROUTE-01
  - ROUTE-02
must_haves:
  truths:
    - "Phase 15 routing manifest, wildcard cleanup, and single-consumer routing all verify together."
    - "JetBrains MCP build verification succeeds; no shell Gradle command is used."
    - "Documentation/module inventory reflects any responsibility/path changes made during Phase 15."
  artifacts:
    - path: "MODULES.md"
      provides: "Updated module inventory if package responsibilities or paths changed"
      contains: "Shared Utility Modules"
    - path: "docs/architecture/01-module-boundaries.md"
      provides: "Updated boundary notes if util routes changed"
      contains: "Utility"
    - path: ".planning/phases/15-pre-migration-audit-routing/15-VALIDATION.md"
      provides: "Validation status updated from pending to execution evidence"
      contains: "15-04-01"
  key_links:
    - from: "JetBrains MCP build"
      to: "Phase 15 source moves"
      via: "project build or existing build run configuration"
      pattern: "exitCode 0|isSuccess=true"
    - from: "routing manifest"
      to: "docs/module inventory"
      via: "responsibility/path changes reflected when applicable"
      pattern: "AnimationApplier|Models|ModBridgeServer|BBModelSink"
---

<objective>
Run final Phase 15 verification and update documentation affected by routing changes.

Purpose: Prove the audit/routing baseline is complete before Phase 16 scaffolds `:eyelib-util`.
Output: JetBrains MCP build evidence, residual scans, validation status updates, and required documentation/module updates.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-pre-migration-audit-routing/15-CONTEXT.md
@.planning/phases/15-pre-migration-audit-routing/15-RESEARCH.md
@.planning/phases/15-pre-migration-audit-routing/15-VALIDATION.md
@.planning/phases/15-pre-migration-audit-routing/15-PATTERNS.md
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
@docs/architecture/migration/utility-routing-manifest.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run residual scans and JetBrains MCP build gate</name>
  <read_first>
    - `.planning/phases/15-pre-migration-audit-routing/15-VALIDATION.md` verification map.
    - `AGENTS.md` tooling restrictions: Gradle only through JetBrains MCP.
    - Plan 02 and Plan 03 summaries if present.
  </read_first>
  <files>.planning/phases/15-pre-migration-audit-routing/15-VALIDATION.md</files>
  <action>
    Run the Phase 15 verification set using IDE/JetBrains MCP tools only. Required scans: (1) util wildcard import regex `import\s+io\.github\.tt432\.eyelib\.util(?:\.[A-Za-z0-9_]+)*\.\*;` over `src/main/java/**/*.java` returns zero; (2) old moved-class package regex `io\.github\.tt432\.eyelib\.util\.(client|modbridge)\.(AnimationApplier|Models|ModBridgeServer|BBModelSink)` over `src/main/java/**/*.java` returns zero; (3) manifest coverage check confirms every current util/core-util Java path appears exactly once; (4) IDE diagnostics for touched Java files report no unresolved symbols. Then run project build through `jetbrain_build_project` with `rebuild=false`; if Gradle task state is stale, use `jetbrain_sync_gradle_projects` first, then build. Never run `gradlew`, `./gradlew`, `gradle`, or shell Gradle.
  </action>
  <verify>
    <automated>`jetbrain_build_project` with `projectPath="E:\\_ideaProjects\\qylEyelib"` and `rebuild=false` returns success / exit code 0; residual JetBrains regex scans return zero matches.</automated>
  </verify>
  <acceptance_criteria>
    - All residual scans return zero unexpected matches.
    - JetBrains MCP build succeeds with no shell Gradle invocation.
    - `15-VALIDATION.md` records green evidence/status for tasks `15-01-01` through `15-04-01` without changing scope.
  </acceptance_criteria>
  <done>Phase 15 source/doc changes are verified through approved tooling.</done>
</task>

<task type="auto">
  <name>Task 2: Update module and architecture docs for actual routing changes</name>
  <read_first>
    - `MODULES.md` Shared Utility Modules and Client Runtime Modules rows.
    - `docs/architecture/01-module-boundaries.md` utility boundary problem/current-target sections.
    - `docs/architecture/02-side-boundaries.md` side rules if any moved code changes client/common/MC ownership.
    - `docs/architecture/migration/utility-routing-manifest.md` final route decisions.
  </read_first>
  <files>MODULES.md, docs/architecture/01-module-boundaries.md, docs/architecture/02-side-boundaries.md, docs/architecture/migration/utility-routing-manifest.md</files>
  <action>
    Update documentation only where the implemented Phase 15 routing changed responsibilities, main paths, or interactions. `AnimationApplier`, `Models`, `ModBridgeServer`, and `BBModelSink` must have been moved, not deleted, by Plan 03 per the locked Phase 15 decision. Update `MODULES.md` client animation/util client rows and `01-module-boundaries.md` utility cleanup notes to state `AnimationApplier` now lives under `client/animation`. Update client model/util client rows and boundary notes to state `Models` now lives under `client/model`. Update `MODULES.md` MC impl utility bridges/modbridge rows and boundary notes for `ModBridgeServer`/`BBModelSink`; if side behavior did not change, do not invent new side-boundary rules, but note preserved local TCP listener ownership under `mc/impl/modbridge` if relevant. Do not document deletion for these four roadmap-named classes in Phase 15. Do not add `:eyelib-util` scaffold or package namespace docs beyond the manifest's future-route references.
  </action>
  <verify>
    <automated>Read `MODULES.md`, `docs/architecture/01-module-boundaries.md`, and `docs/architecture/migration/utility-routing-manifest.md`; verify `AnimationApplier`, `Models`, `ModBridgeServer`, and `BBModelSink` are mentioned consistently as moved to their functional owner packages, not deleted.</automated>
  </verify>
  <acceptance_criteria>
    - Documentation reflects only actual Phase 15 changes, not Phase 16+ planned scaffold work.
    - Each moved class path in docs resolves to an existing source file.
    - No docs claim `AnimationApplier`, `Models`, `ModBridgeServer`, or `BBModelSink` was deleted in Phase 15.
    - No docs claim root `util/client` or `util/modbridge` still owns one of the four moved classes.
  </acceptance_criteria>
  <done>Repository docs and module inventory match the final Phase 15 routing state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source/doc changes → build system | Package/import/documentation changes must not leave the project in an uncompilable or misleading state. |
| ModBridge route docs → runtime ownership | If TCP listener code remains, ownership docs must preserve its local listener and payload-bound behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-10 | Tampering | build integrity | mitigate | Run residual scans and JetBrains MCP build; no shell Gradle. |
| T-15-11 | Repudiation | documentation updates | mitigate | Tie doc statements to actual final routes in the manifest and plan summaries. |
| T-15-12 | Denial of Service | retained `ModBridgeServer` | mitigate | Verify build and docs preserve payload bound/lifecycle owner if the class is retained. |
</threat_model>

<verification>
Final gate requires zero residual wildcard imports, zero old named-class references, manifest coverage for all util/core-util Java files, and successful JetBrains MCP build.
</verification>

<success_criteria>
All four Phase 15 requirement IDs have green evidence; docs match actual source routes; approved JetBrains MCP build succeeds.
</success_criteria>

<output>
After completion, create `.planning/phases/15-pre-migration-audit-routing/15-04-SUMMARY.md`.
</output>
