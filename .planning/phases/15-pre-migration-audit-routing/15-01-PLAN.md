---
phase: 15-pre-migration-audit-routing
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/architecture/migration/utility-routing-manifest.md
autonomous: true
requirements:
  - AUDIT-01
  - ROUTE-02
must_haves:
  truths:
    - "Maintainer can inspect one committed routing manifest for every current root util and core util Java source file."
    - "Each manifest row records verified consumer count, 0/1/N classification, route decision, target phase, and evidence."
    - "ListHelper and EitherHelper are explicitly cataloged as compatibility shims with canonical replacements and deletion timing."
  artifacts:
    - path: "docs/architecture/migration/utility-routing-manifest.md"
      provides: "Phase 15 routing manifest for util/core-util migration"
      contains: "ListHelper"
    - path: "docs/architecture/migration/utility-routing-manifest.md"
      provides: "Shim deletion timing"
      contains: "EitherHelper"
  key_links:
    - from: "src/main/java/io/github/tt432/eyelib/util/**/*.java"
      to: "docs/architecture/migration/utility-routing-manifest.md"
      via: "one manifest row per current Java source file"
      pattern: "src/main/java/io/github/tt432/eyelib/util/.*\\.java"
    - from: "src/main/java/io/github/tt432/eyelib/core/util/**/*.java"
      to: "docs/architecture/migration/utility-routing-manifest.md"
      via: "one manifest row per current Java source file"
      pattern: "src/main/java/io/github/tt432/eyelib/core/util/.*\\.java"
---

<objective>
Create the Phase 15 routing manifest before any source relocation.

Purpose: Make later `:eyelib-util` phases deterministic by recording 0/1/N consumer evidence and route decisions for every existing `root/util/*` and `core/util/*` Java source file.
Output: `docs/architecture/migration/utility-routing-manifest.md` containing inventory rows, route decisions, and shim deletion timing.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-pre-migration-audit-routing/15-CONTEXT.md
@.planning/phases/15-pre-migration-audit-routing/15-RESEARCH.md
@.planning/phases/15-pre-migration-audit-routing/15-VALIDATION.md
@.planning/phases/15-pre-migration-audit-routing/15-PATTERNS.md
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
@docs/architecture/migration/utility-mc-bridges.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build fresh util/core-util inventory and reference evidence</name>
  <read_first>
    - `.planning/phases/15-pre-migration-audit-routing/15-RESEARCH.md` sections "Existing Utility Inventory for Manifest Baseline", "Single-Consumer / Shim Findings", and "Wildcard Import Findings".
    - `.planning/phases/15-pre-migration-audit-routing/15-PATTERNS.md` sections for manifest row shape, `ListHelper`, and `EitherHelper`.
    - `AGENTS.md`, `MODULES.md`, `docs/index/repo-map.md`, `docs/architecture/01-module-boundaries.md`, `docs/architecture/02-side-boundaries.md`.
  </read_first>
  <files>docs/architecture/migration/utility-routing-manifest.md</files>
  <action>
    Use fresh file discovery, not stale counts, to enumerate every Java source under exactly these roots: `src/main/java/io/github/tt432/eyelib/util/**/*.java` and `src/main/java/io/github/tt432/eyelib/core/util/**/*.java`. Expected research baseline is 32 root util files plus 5 core util files, including `package-info.java`; if the fresh count differs, use the fresh count and record the discrepancy in the manifest "Inventory baseline" section. For each non-`package-info.java` primary symbol, gather consumer evidence with IDE Index MCP semantic references when possible; use JetBrains regex/text searches only as supporting evidence. Classify each source as `0`, `1`, or `N`; use `N-internal` for helpers consumed by multiple files inside the same future migration unit, such as codec helpers. Do not scaffold `:eyelib-util`, do not move multi-consumer categories, and do not hand-edit generated Molang parser files.
  </action>
  <verify>
    <automated>Use `jetbrain_search_file` or IDE file search for `src/main/java/io/github/tt432/eyelib/util/**/*.java` and `src/main/java/io/github/tt432/eyelib/core/util/**/*.java`; then confirm every returned path appears exactly once in `docs/architecture/migration/utility-routing-manifest.md`.</automated>
  </verify>
  <acceptance_criteria>
    - Manifest inventory includes every fresh `util/**/*.java` and `core/util/**/*.java` path exactly once.
    - Manifest explicitly records the fresh total counts for root util and core util Java files.
    - Each non-package-info row has consumer evidence source, date, 0/1/N classification, route, and phase owner.
    - No row routes Phase 16+ multi-consumer utility categories to a Phase 15 move.
  </acceptance_criteria>
  <done>Fresh inventory and consumer evidence are sufficient for a maintainer to decide each file's migration route without re-discovering ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Write routing manifest with shim deletion timing</name>
  <read_first>
    - `docs/architecture/migration/utility-mc-bridges.md` for migration-doc style and existing util seam history.
    - `.planning/ROADMAP.md` Phase 15 and Phases 17-19 success criteria.
    - `.planning/REQUIREMENTS.md` AUDIT-01 and ROUTE-02.
  </read_first>
  <files>docs/architecture/migration/utility-routing-manifest.md</files>
  <action>
    Create `docs/architecture/migration/utility-routing-manifest.md` with these sections: `# utility-routing-manifest`, `## Scope`, `## Inventory baseline`, `## Routing rules`, `## Route table`, `## Compatibility shim deletion plan`, and `## Verification commands`. The route table columns must be exactly: `Source`, `Symbol`, `Consumers`, `Class`, `Route`, `Target phase`, `Evidence`. Route values must use only: `eyelib-util`, `functional owner`, `delete`, or `package cleanup`. Include named rows and deletion timing: `src/main/java/io/github/tt432/eyelib/util/ListHelper.java` routes to `delete` after Phase 17 caller migration to `core/util/collection/ListAccessors` / future `eyelibutil` equivalent; `src/main/java/io/github/tt432/eyelib/util/codec/EitherHelper.java` routes to `delete` after Phase 19 codec migration to `core/util/codec/Eithers` / future `eyelibutil` equivalent. Record `ResourceLocations.mod()` caller evidence and mark its delete/parameterize decision as Phase 18-owned. Locked Phase 15 decision: `AnimationApplier`, `Models`, `ModBridgeServer`, and `BBModelSink` must use route `functional owner` and the ROADMAP target packages even if consumer count is `0`; record consumer count/evidence, but do not route these four classes to `delete`.
  </action>
  <verify>
    <automated>Read `docs/architecture/migration/utility-routing-manifest.md` and verify it contains headings `## Route table` and `## Compatibility shim deletion plan`, plus literal strings `ListHelper`, `EitherHelper`, `ResourceLocations.mod()`, `AnimationApplier`, `Models`, `ModBridgeServer`, and `BBModelSink`.</automated>
  </verify>
  <acceptance_criteria>
    - Manifest route table has one row per current util/core-util Java source file.
    - `ListHelper` row states canonical replacement and Phase 17 deletion timing.
    - `EitherHelper` row states canonical replacement and Phase 19 deletion timing.
    - `ResourceLocations.mod()` evidence is present and assigned to Phase 18 decision handling.
    - Named single-consumer classes `AnimationApplier`, `Models`, `ModBridgeServer`, and `BBModelSink` have roadmap target package route `functional owner` and consumer-count evidence; none of these four rows routes to `delete`.
  </acceptance_criteria>
  <done>Manifest is a complete Phase 15 contract for AUDIT-01 and ROUTE-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree → migration manifest | Source inventory is transformed into a human-readable routing contract; stale or missing rows can misroute later code moves. |
| IDE semantic index → manifest evidence | IDE/reference tooling output informs consumer counts; stale indexes can create false zero-consumer decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | `utility-routing-manifest.md` | mitigate | Build manifest from fresh file discovery and require every util/core-util Java path to appear exactly once. |
| T-15-02 | Repudiation | route evidence rows | mitigate | Each row records evidence source/date and whether evidence came from IDE semantic references or supporting text scans. |
| T-15-03 | Denial of Service | later migration phases | mitigate | Do not mark `AnimationApplier`, `Models`, `ModBridgeServer`, or `BBModelSink` for deletion in Phase 15; record zero/one/many semantic evidence while routing them to functional owners per locked decision. |
</threat_model>

<verification>
Use IDE/JetBrains file search to list current util/core-util Java files and compare against manifest rows. Do not run Gradle from shell.
</verification>

<success_criteria>
`docs/architecture/migration/utility-routing-manifest.md` exists, covers AUDIT-01 and ROUTE-02, and contains complete route/deletion timing evidence for current util/core-util Java sources.
</success_criteria>

<output>
After completion, create `.planning/phases/15-pre-migration-audit-routing/15-01-SUMMARY.md`.
</output>
