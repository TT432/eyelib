# 21-01 Plan: Final Residual Checks

## Objective
- Prove old root/core utility Java sources and imports are gone.

## Steps
1. Run glob checks for root util and core util Java sources.
2. Run Java source searches for old util/core util imports.
3. Confirm `eyelib-util` has no imports from root/attachment/material and no `project(...)` dependencies.

## Verification
- All searches return zero matches/files.
