# 18-01 Plan: Migrate ResourceLocations

## Objective
- Move `ResourceLocations` into `eyelib-util/src/main/java/io/github/tt432/eyelibutil/resource/`.
- Delete the unused root-coupled `mod(String)` method.
- Rewire all current root/test callers to `io.github.tt432.eyelibutil.resource.ResourceLocations`.

## Steps
1. Add `eyelibutil.resource.ResourceLocations` with `EMPTY`, `isEmpty`, and `of(...)` methods only.
2. Update production and test imports from `io.github.tt432.eyelib.util.ResourceLocations` to `io.github.tt432.eyelibutil.resource.ResourceLocations`.
3. Delete the old root `src/main/java/io/github/tt432/eyelib/util/ResourceLocations.java`.
4. Search Java sources for the old import and for `ResourceLocations.mod`.

## Verification
- IDE diagnostics on `ResourceLocations` and touched callers.
- Java source search returns zero old imports and zero `ResourceLocations.mod` calls.
