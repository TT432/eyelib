# 18-02 Plan: Migrate TexturePaths And Delete Wrapper

## Objective
- Move canonical texture-path derivation into `eyelib-util`.
- Delete root `TexturePathHelper` and core `TexturePaths` after callers use the util module directly.

## Steps
1. Add `eyelibutil.texture.TexturePaths` with the existing deterministic `emissivePath(String)` behavior.
2. Update `RenderParams`, `RenderControllerEntry`, and `CoreUtilitySeamTest` to import `io.github.tt432.eyelibutil.texture.TexturePaths`.
3. Replace `TexturePathHelper::getEmissiveTexturePath` with `TexturePaths::emissivePath`.
4. Delete `src/main/java/io/github/tt432/eyelib/util/client/texture/TexturePathHelper.java` and `src/main/java/io/github/tt432/eyelib/core/util/texture/TexturePaths.java`.
5. Search Java sources for old texture imports and wrapper references.

## Verification
- IDE diagnostics on touched Java files.
- Java source search returns zero old texture imports and zero `TexturePathHelper` references.
