---
phase: 08-boundary-contract-gradle-module-skeleton
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md
  - MODULES.md
  - docs/index/repo-map.md
  - docs/architecture/01-module-boundaries.md
  - docs/architecture/02-side-boundaries.md
autonomous: true
requirements:
  - PGRAD-02
  - PAPI-02
user_setup: []
must_haves:
  truths:
    - "Maintainer can read where `:eyelib-particle` lives and what it owns."
    - "Maintainer can read that root may depend on particle, while particle must not depend back on root runtime packages or root platform wiring."
    - "Repository navigation and architecture docs route particle work to the new module contract without moving Phase 9-14 runtime responsibilities."
  artifacts:
    - path: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md"
      provides: "module-local ownership and boundary contract"
      contains: "Root runtime may depend on `:eyelib-particle`"
    - path: "MODULES.md"
      provides: "canonical module inventory row for particle subproject"
      contains: "Particle subproject"
    - path: "docs/index/repo-map.md"
      provides: "repository navigation route for particle module"
      contains: ":eyelib-particle"
    - path: "docs/architecture/01-module-boundaries.md"
      provides: "target ownership map for particle module"
      contains: ":eyelib-particle/**"
    - path: "docs/architecture/02-side-boundaries.md"
      provides: "side and dependency rule for particle module zone"
      contains: ":eyelib-particle"
  key_links:
    - from: "MODULES.md"
      to: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md"
      via: "module inventory points to module-local contract"
      pattern: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/"
    - from: "docs/index/repo-map.md"
      to: "docs/architecture/01-module-boundaries.md"
      via: "repo navigation routes boundary readers to architecture rules"
      pattern: "docs/architecture/01-module-boundaries.md"
    - from: "docs/architecture/02-side-boundaries.md"
      to: ":eyelib-particle"
      via: "side/dependency constraints for pure particle core and adapters"
      pattern: "must not depend on root runtime packages"
---

<objective>
Create the documentation half of Phase 8: module-local README, canonical module inventory, repo map, and architecture/side-boundary updates that make `:eyelib-particle` understandable and enforceable for later phases.

Purpose: PGRAD-02 requires readable ownership and integration rules; PAPI-02 requires explicit documentation that the new module must not depend back on root runtime, managers, registries, packets, capability helpers, or `mc/impl`.
Output: particle module README plus updates to `MODULES.md`, `docs/index/repo-map.md`, `docs/architecture/01-module-boundaries.md`, and `docs/architecture/02-side-boundaries.md`.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-CONTEXT.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-RESEARCH.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-PATTERNS.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-VALIDATION.md
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
@src/main/java/io/github/tt432/eyelib/client/particle/README.md
@eyelib-processor/src/main/java/io/github/tt432/eyelibprocessor/README.md

<interfaces>
Documentation contract from Phase 8 sources:
- Module name/path: `:eyelib-particle`, `eyelib-particle/`
- Module-local docs path: `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md`
- Ownership statement: first-class particle module boundary for future module-owned APIs, core/runtime definitions, and explicit integration seams.
- Direction statement: root runtime may consume `:eyelib-particle`; `:eyelib-particle` must not depend back on root runtime packages, root managers, root registries, root packets, root capability helpers, or root `mc/impl` classes.
- Scope boundary: moving particle APIs, schema/runtime adapters, loader publication, command/network integration, render/runtime code, and verification coverage belongs to Phases 9-14; this plan documents the boundary without relocating those responsibilities.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document module-local contract and canonical module inventory</name>
  <files>eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md, MODULES.md</files>
  <read_first>
    - `AGENTS.md` lines 14-21 for module documentation update rules.
    - `MODULES.md` lines 1-13 for inventory purpose and summary style.
    - `MODULES.md` lines 38-42 for subproject rows.
    - `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-PATTERNS.md` sections for module README and `MODULES.md`.
    - `src/main/java/io/github/tt432/eyelib/client/particle/README.md` for current particle runtime wording.
    - `eyelib-processor/src/main/java/io/github/tt432/eyelibprocessor/README.md` for module-local README structure.
  </read_first>
  <action>Create `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md` with these exact sections: `# Eyelib Particle Module`, `## Scope`, `## Current Responsibilities`, `## Dependency Direction`, `## Integration Rule`, `## Current Consumers`, and `## Verification Rule`. Under `Scope`, state path `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/` and that this module owns the particle module boundary for future particle-module APIs, core/runtime definitions, and explicit integration seams. Under `Current Responsibilities`, state that Phase 8 owns build metadata, source/resource layout, and boundary documentation, while current executable particle runtime remains in `src/main/java/io/github/tt432/eyelib/client/particle/` until later phase plans move it through explicit seams. Under `Dependency Direction`, include the exact sentence: `Root runtime may depend on :eyelib-particle, but :eyelib-particle must not depend on root runtime packages, root managers, root registries, root packets, root capability helpers, or root mc/impl classes.` Under `Integration Rule`, state pure particle core stays free of root, Minecraft, and Forge contamination; Minecraft/Forge-facing integration must live in explicitly documented adapters before introduction. Under `Current Consumers`, state root runtime `:` consumes the module through Gradle project dependency wiring. Under `Verification Rule`, state Gradle verification for this repository must be executed through JetBrains MCP Gradle tools only, never through shell Gradle commands. In `MODULES.md`, update the Summary bullet that lists subprojects to include `eyelib-particle`, and add a Root And Documentation Modules inventory row named `Particle subproject` with responsibility `First-class particle module boundary for future particle APIs, core/runtime definitions, and explicit integration seams; Phase 8 owns build metadata and boundary contract before runtime code moves`, main paths ``eyelib-particle/build.gradle`, `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/`, `eyelib-particle/src/main/resources/META-INF/mods.toml`, `eyelib-particle/src/test/``, and interactions `consumed by root via project(':eyelib-particle'); must not depend back on root runtime packages, root managers, root registries, root packets, root capability helpers, or root mc/impl; pure core stays root/MC/Forge-clean until documented adapters are introduced`.</action>
  <verify>
    <automated>PowerShell: `(Test-Path "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md")`; `(Select-String -Path "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/README.md" -SimpleMatch "Root runtime may depend on :eyelib-particle, but :eyelib-particle must not depend on root runtime packages, root managers, root registries, root packets, root capability helpers, or root mc/impl classes.").Count -eq 1`; `(Select-String -Path "MODULES.md" -SimpleMatch "Particle subproject").Count -eq 1`; `(Select-String -Path "MODULES.md" -SimpleMatch "project(':eyelib-particle')").Count -ge 1`.</automated>
  </verify>
  <acceptance_criteria>
    - Module README exists with all seven required headings.
    - Module README contains the exact root-to-particle dependency direction sentence from the action.
    - Module README states current executable particle runtime remains at `src/main/java/io/github/tt432/eyelib/client/particle/` until later phase plans move it through explicit seams.
    - `MODULES.md` Summary includes `eyelib-particle` in the multi-project subproject list.
    - `MODULES.md` has exactly one `Particle subproject` row with the target paths and PAPI-02 forbidden dependency categories.
  </acceptance_criteria>
  <done>Maintainers can read the particle module's local contract and find the new module in the canonical inventory.</done>
</task>

<task type="auto">
  <name>Task 2: Update repo navigation, module-boundary map, and side-boundary rules</name>
  <files>docs/index/repo-map.md, docs/architecture/01-module-boundaries.md, docs/architecture/02-side-boundaries.md</files>
  <read_first>
    - `docs/index/repo-map.md` lines 10-28 for repository shape and topic routing style.
    - `docs/architecture/01-module-boundaries.md` lines 3-16 for current major areas and lines 49-76 for ownership map style.
    - `docs/architecture/02-side-boundaries.md` lines 14-33 for current anchors and cross-zone dependency rules.
    - `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-PATTERNS.md` sections for repo map and architecture docs.
    - `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-CONTEXT.md` lines 7-10 for the phase boundary.
  </read_first>
  <action>In `docs/index/repo-map.md`, update the repository summary bullet to include `:eyelib-particle` as `particle module boundary` alongside the existing root, processor, importer, Molang, material, and clientsmoke entries. Add a topic bullet after the client rendering/runtime or loader bullets: `- Particle module boundary: start in `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/` for the module contract; current executable runtime remains in `src/main/java/io/github/tt432/eyelib/client/particle/` until the particle API/store/schema/runtime phases move it through explicit seams.` In `docs/architecture/01-module-boundaries.md`, add `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/`: first-class particle module boundary, build/package contract, and future module-owned particle APIs/core/runtime definitions without root runtime reverse dependency to `Current Major Areas`. Add ownership-map row `:eyelib-particle/**` → `client.particle.module` with boundary intent `Own particle module contracts and future particle-module APIs/core/runtime definitions; root may consume it, but it must not depend on root runtime packages, root managers, root registries, root packets, root capability helpers, or root mc/impl; Phase 8 is skeleton/boundary only and current executable runtime remains in root client particle packages until later phase plans move it through explicit seams`. Add a short boundary note under the particle/current breaking-refactor area stating `Phase 8 establishes the Gradle/documentation boundary only; do not move ParticleSpawnService, BrParticleRenderManager, loaders, packets, or BrParticle runtime/schema ownership in this phase.` In `docs/architecture/02-side-boundaries.md`, add a Current Anchors bullet for `:eyelib-particle` as a particle module zone: it may own particle module contracts and future pure particle core/runtime definitions, must not depend on root runtime packages or `mc/impl`, and any Minecraft/Forge-facing integration must be introduced through documented adapters. Add a Rules bullet: `Pure particle core in :eyelib-particle must remain root/MC/Forge-clean; platform-specific command, packet, player, identifier, lifecycle, and render hook concerns require explicit adapter documentation before introduction.`</action>
  <verify>
    <automated>PowerShell: `(Select-String -Path "docs/index/repo-map.md" -SimpleMatch ":eyelib-particle").Count -ge 2`; `(Select-String -Path "docs/architecture/01-module-boundaries.md" -SimpleMatch ":eyelib-particle/**").Count -eq 1`; `(Select-String -Path "docs/architecture/01-module-boundaries.md" -SimpleMatch "do not move ParticleSpawnService, BrParticleRenderManager, loaders, packets, or BrParticle").Count -eq 1`; `(Select-String -Path "docs/architecture/02-side-boundaries.md" -SimpleMatch "Pure particle core in :eyelib-particle must remain root/MC/Forge-clean").Count -eq 1`.</automated>
  </verify>
  <acceptance_criteria>
    - `docs/index/repo-map.md` names `:eyelib-particle` in the repository shape summary and has a particle module boundary topic bullet pointing to both the new module contract and current root runtime path.
    - `docs/architecture/01-module-boundaries.md` names `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/` in Current Major Areas.
    - `docs/architecture/01-module-boundaries.md` has exactly one ownership-map row for `:eyelib-particle/**` with PAPI-02 forbidden dependency categories.
    - `docs/architecture/01-module-boundaries.md` states Phase 8 does not move `ParticleSpawnService`, `BrParticleRenderManager`, loaders, packets, or `BrParticle` runtime/schema ownership.
    - `docs/architecture/02-side-boundaries.md` states pure particle core in `:eyelib-particle` must remain root/MC/Forge-clean and that platform-specific concerns require documented adapters.
    - None of the touched docs introduces shell Gradle commands.
  </acceptance_criteria>
  <done>Repository navigation and architecture docs make the particle module boundary discoverable, side-aware, and constrained by the one-way dependency rule.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → documentation | Maintainer decisions rely on docs accurately stating ownership and dependency direction. |
| pure particle core → platform integration | Platform-specific Minecraft/Forge concerns must cross only through documented adapters. |
| docs → verification workflow | Documentation must not instruct shell Gradle execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Repudiation | `MODULES.md` and module README | mitigate | Required exact wording records ownership, consumers, and forbidden dependency direction. |
| T-08-05 | Tampering | architecture docs | mitigate | Side-boundary and module-boundary docs require adapter documentation before platform-specific particle integration is introduced. |
| T-08-06 | Tampering | docs verification guidance | mitigate | Acceptance criteria checks that touched docs do not introduce shell Gradle commands. |
</threat_model>

<verification>
Phase-level checks after this plan:
- File/content checks prove module README, `MODULES.md`, repo map, module-boundary doc, and side-boundary doc contain the exact required particle boundary strings.
- Verify every referenced path in touched docs exists after Plan 01 creates the module skeleton.
- Search touched docs for shell Gradle invocations; none may be present as newly introduced guidance.
- Re-run Plan 01 JetBrains MCP Gradle verification if documentation edits reveal a build path typo or module-name mismatch.
</verification>

<success_criteria>
- Maintainer can discover `:eyelib-particle` from `MODULES.md` and `docs/index/repo-map.md`.
- Maintainer can read module-local README and architecture docs to understand ownership, one-way dependency direction, and allowed integration layers.
- Documentation explicitly prevents Phase 8 from moving particle APIs, loader publication, command/network integration, render/runtime code, or schema/runtime ownership.
- Documentation verification remains compatible with the project rule that Gradle runs only through JetBrains MCP.
</success_criteria>

<output>
After completion, create `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-boundary-contract-gradle-module-skeleton-02-SUMMARY.md`.
</output>
