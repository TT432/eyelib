---
phase: 08-boundary-contract-gradle-module-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle
  - build.gradle
  - eyelib-particle/build.gradle
  - eyelib-particle/src/main/resources/META-INF/mods.toml
  - eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/package-info.java
autonomous: true
requirements:
  - PGRAD-01
  - PAPI-02
user_setup: []
must_haves:
  truths:
    - "Maintainer can see :eyelib-particle included as a first-class Gradle subproject."
    - "Root runtime consumes :eyelib-particle through one-way root-to-particle dependency wiring."
    - ":eyelib-particle has its own build metadata, Java source root, resource metadata, and no dependency back to root runtime."
  artifacts:
    - path: "settings.gradle"
      provides: "Gradle include for eyelib-particle"
      contains: "include(\"eyelib-particle\")"
    - path: "build.gradle"
      provides: "root project dependency wiring"
      contains: "project(':eyelib-particle')"
    - path: "eyelib-particle/build.gradle"
      provides: "particle subproject build metadata"
      contains: "archivesName = 'eyelib-particle'"
    - path: "eyelib-particle/src/main/resources/META-INF/mods.toml"
      provides: "Forge-visible module metadata"
      contains: "modId=\"eyelibparticle\""
    - path: "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/package-info.java"
      provides: "package-level boundary and nullness metadata"
      contains: "@NullMarked"
  key_links:
    - from: "settings.gradle"
      to: "eyelib-particle/build.gradle"
      via: "Gradle include resolves module directory"
      pattern: "include\\(\\\"eyelib-particle\\\"\\)"
    - from: "build.gradle"
      to: ":eyelib-particle"
      via: "api/modImplementation/jarJar project dependency"
      pattern: "project\\(':eyelib-particle'\\)"
    - from: "eyelib-particle/build.gradle"
      to: "root runtime boundary"
      via: "absence of project(':') reverse dependency"
      pattern: "no implementation project(':') inside eyelib-particle/build.gradle"
---

<objective>
Create the build-system half of Phase 8: a buildable, Forge-visible `:eyelib-particle` Gradle subproject with source/resource layout and one-way root → particle consumption.

Purpose: PGRAD-01 requires a real Gradle module, and PAPI-02 requires that module to avoid root runtime dependencies from the first build slice.
Output: `settings.gradle`, root `build.gradle`, `eyelib-particle/build.gradle`, `eyelib-particle` Forge metadata, and package-level boundary metadata.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-CONTEXT.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-RESEARCH.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-PATTERNS.md
@.planning/phases/08-boundary-contract-gradle-module-skeleton/08-VALIDATION.md
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md
@settings.gradle
@build.gradle
@eyelib-material/build.gradle
@eyelib-material/src/main/resources/META-INF/mods.toml

<interfaces>
Build contract from Phase 8 sources:
- Gradle project path: `:eyelib-particle`
- Module directory: `eyelib-particle/`
- Java package root: `io.github.tt432.eyelibparticle`
- Forge mod id: `eyelibparticle`
- Archive name: `eyelib-particle`
- Root dependency direction: root `:` may depend on `:eyelib-particle`; `:eyelib-particle` must not declare `project(':')` or import root runtime packages.
- Gradle verification must use JetBrains MCP only: `jetbrain_sync_gradle_projects`, `jetbrain_run_gradle_tasks`, or `jetbrain_build_project`. Do not run shell Gradle commands.
</interfaces>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | Build and understand `:eyelib-particle` as a real Gradle module with one-way root → particle dependency | 08-01, 08-02 | COVERED | Build wiring here; documentation in Plan 02.
REQ | PGRAD-01 | Real subproject with build metadata, source sets, resources, root wiring | 08-01 | COVERED | Tasks 1-2 create Gradle include, dependency wiring, module build, resources, package root.
REQ | PGRAD-02 | Ownership and allowed integration documentation | 08-02 | COVERED | Plan 02 owns docs.
REQ | PAPI-02 | No dependency on root runtime packages/managers/registries/packets/capability helpers/mc/impl | 08-01, 08-02 | COVERED | This plan prevents reverse Gradle dependency; Plan 02 documents rule.
RESEARCH | — | Use existing Forge-visible module pattern and Java 17/JUnit/Lombok/legacyForge metadata | 08-01 | COVERED | Task 2 mirrors material/attachment pattern with particle ids.
RESEARCH | — | JetBrains MCP-only Gradle verification | 08-01, 08-02 | COVERED | Verification sections name MCP tools only.
CONTEXT | discretion | Pure infrastructure phase; use roadmap/research/module docs | 08-01, 08-02 | COVERED | Plans use existing project patterns and no deferred runtime moves.
</source_audit>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register `:eyelib-particle` and wire one-way root consumption</name>
  <files>settings.gradle, build.gradle</files>
  <read_first>
    - `AGENTS.md` lines 28-31 for JetBrains MCP-only Gradle rule.
    - `MODULES.md` lines 38-42 for existing subproject inventory style.
    - `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-PATTERNS.md` sections `settings.gradle` and `build.gradle` for exact analogs.
    - `settings.gradle` current include block.
    - `build.gradle` current root `dependencies { ... }` block.
  </read_first>
  <action>Add `include("eyelib-particle")` to `settings.gradle` beside the other first-class `eyelib-*` subprojects, keeping `includeBuild("clientsmoke")` separate. In root `build.gradle`, add these three root-to-particle dependency lines in the existing subproject dependency cluster: `api project(':eyelib-particle')`, `modImplementation project(':eyelib-particle')`, and `jarJar project(':eyelib-particle')`. Place them with the Forge-visible module dependencies, preferably after the `:eyelib-material` block and before processor/importer/molang wiring. Do not add any dependency from `:eyelib-particle` back to root; no `project(':')` reference belongs in the new module build file.</action>
  <verify>
    <automated>PowerShell: `(Select-String -Path "settings.gradle" -SimpleMatch 'include("eyelib-particle")').Count -eq 1`; `(Select-String -Path "build.gradle" -SimpleMatch "api project(':eyelib-particle')").Count -eq 1`; `(Select-String -Path "build.gradle" -SimpleMatch "modImplementation project(':eyelib-particle')").Count -eq 1`; `(Select-String -Path "build.gradle" -SimpleMatch "jarJar project(':eyelib-particle')").Count -eq 1`.</automated>
  </verify>
  <acceptance_criteria>
    - `settings.gradle` contains exactly one `include("eyelib-particle")` line.
    - Root `build.gradle` contains exactly one each of `api project(':eyelib-particle')`, `modImplementation project(':eyelib-particle')`, and `jarJar project(':eyelib-particle')`.
    - No task or documentation introduced by this plan contains a shell Gradle invocation such as `./gradlew`.
  </acceptance_criteria>
  <done>Gradle can discover `:eyelib-particle` from settings, and root build metadata declares one-way consumption of the particle module.</done>
</task>

<task type="auto">
  <name>Task 2: Create particle module build metadata and package boundary marker</name>
  <files>eyelib-particle/build.gradle, eyelib-particle/src/main/resources/META-INF/mods.toml, eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/package-info.java</files>
  <read_first>
    - `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-RESEARCH.md` sections `Standard Stack`, `Code Examples`, and `Validation Architecture`.
    - `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-PATTERNS.md` sections `eyelib-particle/build.gradle`, `mods.toml`, and `package-info.java`.
    - `eyelib-material/build.gradle` for Forge-visible build metadata.
    - `eyelib-material/src/main/resources/META-INF/mods.toml` for Forge metadata shape.
    - `docs/architecture/01-module-boundaries.md` lines 49-76 for target-owner wording.
  </read_first>
  <action>Create `eyelib-particle/build.gradle` using Groovy DSL with plugins `java-library`, `io.freefair.lombok` version `8.6`, `net.neoforged.moddev.legacyforge` version `2.0.91`, and `maven-publish`; set `version = rootProject.version`, `group = rootProject.group`, `base { archivesName = 'eyelib-particle' }`, and `java.toolchain.languageVersion = JavaLanguageVersion.of(17)`. Add `sourceSets.test` compile/runtime classpath extension matching `:eyelib-material`. Add repositories `mavenCentral()`, `mavenLocal()`, and `maven { url 'https://libraries.minecraft.net/' }`. Configure `legacyForge` with `version = project.minecraft_version + '-' + project.forge_version`, parchment mappings from root properties, and `mods { eyelibparticle { sourceSet(sourceSets.main) } }`. Add dependencies only for `compileOnly 'org.jspecify:jspecify:1.0.0'`, `testImplementation platform('org.junit:junit-bom:5.10.2')`, `testImplementation 'org.junit.jupiter:junit-jupiter'`, and `testRuntimeOnly 'org.junit.platform:junit-platform-launcher'`; do not add `implementation project(':')` or root package dependencies. Configure UTF-8 JavaCompile, `test { useJUnitPlatform() }`, `java { withSourcesJar() }`, `processResources` token expansion for `minecraft_version_range`, `forge_version_range`, `loader_version_range`, `mod_version`, `mod_license`, `mod_authors`, and `mod_description = 'Particle module APIs, core contracts, and explicit integration seams for Eyelib.'`; add `publishing { publications { mavenJava(MavenPublication) { from components.java } } repositories { mavenLocal() } }`. Create `eyelib-particle/src/main/resources/META-INF/mods.toml` with `modId="eyelibparticle"`, `displayName="Eyelib Particle"`, `${mod_version}`, `${mod_license}`, `${mod_authors}`, description `Particle module APIs, core contracts, and explicit integration seams for Eyelib.`, and Forge/Minecraft dependency blocks keyed as `[[dependencies.eyelibparticle]]` with `side="BOTH"`. Create `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/package-info.java` with a Javadoc stating this package owns the particle module boundary and must not depend on root runtime packages, root managers, root registries, root packets, root capability helpers, or root `mc/impl`; annotate package `io.github.tt432.eyelibparticle` with `@NullMarked` and import `org.jspecify.annotations.NullMarked`.</action>
  <verify>
    <automated>JetBrains MCP only: first run `jetbrain_sync_gradle_projects` for project path `E:\_ideaProjects\qylEyelib`; then run `jetbrain_run_gradle_tasks` with `externalProjectPath: "E:\_ideaProjects\qylEyelib"`, `taskNames: [":eyelib-particle:compileJava"]`, and require exit code 0. If JetBrains MCP Gradle tools are unavailable, stop and ask the user to re-enable MCP; do not run shell Gradle.</automated>
    <automated>PowerShell: confirm `(Select-String -Path "eyelib-particle/build.gradle" -SimpleMatch "archivesName = 'eyelib-particle'").Count -eq 1`; `(Select-String -Path "eyelib-particle/build.gradle" -SimpleMatch "implementation project(':')").Count -eq 0`; `(Select-String -Path "eyelib-particle/src/main/resources/META-INF/mods.toml" -SimpleMatch 'modId="eyelibparticle"').Count -eq 1`; `(Select-String -Path "eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/package-info.java" -SimpleMatch '@NullMarked').Count -eq 1`.</automated>
  </verify>
  <acceptance_criteria>
    - `eyelib-particle/build.gradle` exists and declares `archivesName = 'eyelib-particle'`, `legacyForge`, Java 17, JUnit Jupiter, sources jar, processResources token expansion, and Maven publication.
    - `eyelib-particle/build.gradle` does not contain `project(':')`, `io.github.tt432.eyelib.client`, `io.github.tt432.eyelib.network`, `io.github.tt432.eyelib.capability`, or `io.github.tt432.eyelib.mc.impl`.
    - `eyelib-particle/src/main/resources/META-INF/mods.toml` exists and contains exactly the `eyelibparticle` mod id and dependency table names.
    - `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/package-info.java` exists, is `@NullMarked`, and states the PAPI-02 forbidden root dependency categories.
    - JetBrains MCP `:eyelib-particle:compileJava` exits 0.
  </acceptance_criteria>
  <done>`:eyelib-particle` has independent build/resource/package metadata and compiles without reverse dependency on root runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| root Gradle project → `:eyelib-particle` | Root consumes the new module artifact through declared project dependencies. |
| `:eyelib-particle` → root runtime | Forbidden reverse dependency boundary; particle must not compile against root managers, packets, registries, capability helpers, or `mc/impl`. |
| executor → Gradle verification | Gradle checks must cross through JetBrains MCP, not shell commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `eyelib-particle/build.gradle` | mitigate | Do not declare `project(':')`; acceptance criteria checks absence of reverse project dependency and forbidden root package strings. |
| T-08-02 | Tampering | root `build.gradle` | mitigate | Use explicit `api`, `modImplementation`, and `jarJar` root-to-particle dependencies only; no file-based or ad-hoc dependency wiring. |
| T-08-03 | Tampering | verification workflow | mitigate | Verification names JetBrains MCP Gradle tools only and instructs executor to stop if MCP is unavailable. |
</threat_model>

<verification>
Phase-level checks after this plan:
- JetBrains MCP `jetbrain_sync_gradle_projects` succeeds for `E:\_ideaProjects\qylEyelib`.
- JetBrains MCP `jetbrain_run_gradle_tasks` for `:eyelib-particle:compileJava` exits 0.
- File/content checks prove `settings.gradle`, root `build.gradle`, `eyelib-particle/build.gradle`, `mods.toml`, and `package-info.java` contain the target strings listed in acceptance criteria.
- Search introduced planning/docs text for shell Gradle examples; none may be introduced by this plan.
</verification>

<success_criteria>
- `:eyelib-particle` is included in Gradle settings.
- Root build metadata consumes the particle module through one-way project dependency wiring.
- Particle module build/resource/package metadata exists and compiles through JetBrains MCP Gradle execution.
- Particle module build metadata contains no reverse root dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/08-boundary-contract-gradle-module-skeleton/08-boundary-contract-gradle-module-skeleton-01-SUMMARY.md`.
</output>
