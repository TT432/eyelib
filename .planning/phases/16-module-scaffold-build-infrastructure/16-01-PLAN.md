---
phase: 16-module-scaffold-build-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - settings.gradle
  - eyelib-util/build.gradle
  - eyelib-util/src/main/resources/META-INF/mods.toml
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/bootstrap/EyelibUtilMod.java
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/package-info.java
  - eyelib-util/src/test/java/io/github/tt432/eyelibutil/UtilModuleIdentityTest.java
autonomous: true
requirements:
  - MOD-01
must_haves:
  truths:
    - ":eyelib-util is registered as a Gradle subproject and can be targeted by JetBrains MCP Gradle tasks."
    - "eyelib-util build metadata declares the Forge module identity without depending on any project module."
    - "The module root package is io.github.tt432.eyelibutil and does not create split packages with root."
  artifacts:
    - path: "settings.gradle"
      provides: "Gradle include for :eyelib-util"
      contains: "include(\"eyelib-util\")"
    - path: "eyelib-util/build.gradle"
      provides: "Forge Java 17 subproject build scaffold"
      contains: "net.neoforged.moddev.legacyforge"
      excludes: "project("
    - path: "eyelib-util/src/main/resources/META-INF/mods.toml"
      provides: "Forge mod metadata"
      contains: "modId=\"eyelibutil\""
    - path: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/bootstrap/EyelibUtilMod.java"
      provides: "javafml @Mod bootstrap marker"
      contains: "@Mod(EyelibUtilMod.MOD_ID)"
    - path: "eyelib-util/src/test/java/io/github/tt432/eyelibutil/UtilModuleIdentityTest.java"
      provides: "MOD-01 static guardrails without project dependencies"
      contains: "assertFalse(build.contains(\"project(\"))"
  key_links:
    - from: "settings.gradle"
      to: "eyelib-util/build.gradle"
      via: "Gradle include creates :eyelib-util project path"
      pattern: "include\\(\"eyelib-util\"\\)"
    - from: "eyelib-util/build.gradle"
      to: "eyelib-util/src/main/resources/META-INF/mods.toml"
      via: "processResources expands META-INF/mods.toml"
      pattern: "filesMatching\\('META-INF/mods.toml'\\)"
    - from: "eyelib-util/build.gradle"
      to: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/bootstrap/EyelibUtilMod.java"
      via: "legacyForge.mods.eyelibutil binds main source set to @Mod marker"
      pattern: "eyelibutil\\s*\\{\\s*sourceSet\\(sourceSets.main\\)"
---

<objective>
Create the buildable `:eyelib-util` Forge subproject scaffold required by MOD-01.

Purpose: Phase 16 must prove the new utility module exists as an independent Gradle leaf before any utility implementation migration begins.
Output: `settings.gradle` include, external-only `eyelib-util/build.gradle`, Forge metadata, bootstrap marker, root package marker, and a source-level identity test that does not require project dependencies.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-CONTEXT.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-RESEARCH.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-PATTERNS.md
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md

<interfaces>
<!-- Key contracts the executor must preserve. -->

Phase 16 fixed decisions from CONTEXT/RESEARCH:
- Module path: `:eyelib-util`.
- Mod id: `eyelibutil`.
- Package namespace: `io.github.tt432.eyelibutil`.
- Build dependency direction: `eyelib-util/build.gradle` must contain zero `project(...)` dependencies.
- MC/Forge/external dependencies are allowed; project-internal dependencies are not.
    - No root/core utility implementation files move in Phase 16; Phases 17-20 own migrations.
    - Root `build.gradle` must not depend on `:eyelib-util` in Phase 16; root consumption starts in Phase 17+ when code migrates.
- All Gradle verification uses JetBrains MCP only; never run Gradle from shell.

Build pattern to clone from `16-PATTERNS.md`:
```groovy
plugins {
    id 'java-library'
    id 'io.freefair.lombok' version '8.6'
    id 'net.neoforged.moddev.legacyforge' version '2.0.91'
    id 'maven-publish'
}

legacyForge {
    version = project.minecraft_version + '-' + project.forge_version
    parchment {
        mappingsVersion = project.parchment_mappings_version
        minecraftVersion = project.parchment_minecraft_version
    }
    mods {
        eyelibutil { sourceSet(sourceSets.main) }
    }
}
```

Allowed non-project dependencies for `eyelib-util/build.gradle`:
```groovy
implementation 'com.mojang:datafixerupper:6.0.8'
implementation 'org.joml:joml:1.10.5'
implementation 'org.slf4j:slf4j-api:2.0.7'
compileOnly 'org.jspecify:jspecify:1.0.0'
testImplementation platform('org.junit:junit-bom:5.10.2')
testImplementation 'org.junit.jupiter:junit-jupiter'
testRuntimeOnly 'org.junit.platform:junit-platform-launcher'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register and configure the leaf Forge subproject</name>
  <files>settings.gradle, eyelib-util/build.gradle</files>
  <behavior>
    - Test 1: `settings.gradle` contains exactly one `include("eyelib-util")` entry adjacent to sibling subproject includes.
    - Test 2: `eyelib-util/build.gradle` applies `java-library`, `io.freefair.lombok` 8.6, `net.neoforged.moddev.legacyforge` 2.0.91, and `maven-publish`.
    - Test 3: `eyelib-util/build.gradle` binds `legacyForge.mods.eyelibutil` to `sourceSets.main` and contains no `project(` substring.
  </behavior>
  <action>Add `include("eyelib-util")` to the existing sibling include block in `settings.gradle` per MOD-01. Create `eyelib-util/build.gradle` by adapting the sibling Forge module pattern from `eyelib-attachment`/`eyelib-material`, with `base.archivesName = 'eyelib-util'`, Java 17 toolchain, `legacyForge` configured from root Gradle properties, `mods { eyelibutil { sourceSet(sourceSets.main) } }`, JUnit 5 test setup, UTF-8 JavaCompile, sourcesJar, processResources expansion for `META-INF/mods.toml`, and mavenLocal publishing. Use only the allowed external dependencies listed in the context block; do not copy any `project(...)` dependency from sibling modules because `:eyelib-util` is a leaf module per the Phase 16 decisions.</action>
  <verify>
    <automated>PowerShell: `$ErrorActionPreference = 'Stop'; if ((Select-String -Path settings.gradle -SimpleMatch 'include("eyelib-util")').Count -ne 1) { throw 'settings.gradle must contain exactly one include("eyelib-util")' }; if (-not (Test-Path 'eyelib-util/build.gradle')) { throw 'missing eyelib-util/build.gradle' }; if ((Select-String -Path eyelib-util/build.gradle -SimpleMatch 'project(').Count -ne 0) { throw 'eyelib-util/build.gradle must contain zero project( dependencies' }; foreach ($needle in @('net.neoforged.moddev.legacyforge','eyelibutil','datafixerupper','org.joml:joml','org.slf4j:slf4j-api')) { if (-not (Select-String -Path eyelib-util/build.gradle -SimpleMatch $needle -Quiet)) { throw "missing required build.gradle token: $needle" } }`</automated>
  </verify>
  <done>`settings.gradle` registers `:eyelib-util`; `eyelib-util/build.gradle` is a Forge Java 17 leaf subproject and contains zero `project(` matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Forge identity files and source-level scaffold guardrails</name>
  <files>eyelib-util/src/main/resources/META-INF/mods.toml, eyelib-util/src/main/java/io/github/tt432/eyelibutil/bootstrap/EyelibUtilMod.java, eyelib-util/src/main/java/io/github/tt432/eyelibutil/package-info.java, eyelib-util/src/test/java/io/github/tt432/eyelibutil/UtilModuleIdentityTest.java</files>
  <behavior>
    - Test 1: `mods.toml`, `legacyForge.mods`, and `EyelibUtilMod.MOD_ID` all use `eyelibutil` exactly.
    - Test 2: Java package declarations start with `io.github.tt432.eyelibutil`, not `io.github.tt432.eyelib.util`.
    - Test 3: `UtilModuleIdentityTest` reads module-local files only and asserts zero `project(` dependencies plus correct mod id/bootstrap/package identity.
  </behavior>
  <action>Create `mods.toml` under `eyelib-util/src/main/resources/META-INF/` using the sibling Forge metadata shape and mod id `eyelibutil`. Create `EyelibUtilMod` in `io.github.tt432.eyelibutil.bootstrap` with `public static final String MOD_ID = "eyelibutil"` and `@Mod(EyelibUtilMod.MOD_ID)`. Create package-level `package-info.java` for `io.github.tt432.eyelibutil` with `@NullMarked` and a boundary comment stating the module owns shared utility code, must not import root/sibling project modules, may use MC/Forge only when utility ownership requires it, and receives no migrated utility implementations during Phase 16. Create `UtilModuleIdentityTest` under the util module test tree; adapt the existing sibling identity-test pattern but keep checks within the module/repo files and avoid any project dependency. The test must check `build.gradle` has the legacyForge plugin, `eyelibutil`, and no `project(`; `mods.toml` contains `modId="eyelibutil"`; bootstrap uses `@Mod(EyelibUtilMod.MOD_ID)`; and package-info declares `io.github.tt432.eyelibutil`.</action>
  <verify>
    <automated>JetBrains MCP: after `jetbrain_sync_gradle_projects(projectPath="E:\\_ideaProjects\\qylEyelib")`, run `jetbrain_run_gradle_tasks(projectPath="E:\\_ideaProjects\\qylEyelib", externalProjectPath="E:\\_ideaProjects\\qylEyelib", taskNames=[":eyelib-util:test"], scriptParameters="", timeoutMillis=240000)` and require exitCode 0.</automated>
  </verify>
  <done>Forge metadata, @Mod marker, package namespace, and identity test all agree on `eyelibutil` / `io.github.tt432.eyelibutil`, and the util module test task passes through JetBrains MCP.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gradle settings → new subproject | A new project path enters the build graph and must resolve only to the intended `eyelib-util/` directory. |
| Gradle dependency resolution → external artifacts | The new module pulls external libraries and must not couple to root/sibling projects. |
| Forge mod discovery → module metadata/bootstrap | Forge discovers `mods.toml` and `@Mod`; id mismatch or collision breaks runtime identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | `eyelib-util/build.gradle` | mitigate | Assert no `project(` in `build.gradle` through static check and `UtilModuleIdentityTest`; copy only explicit external coordinates. |
| T-16-02 | Spoofing | `mods.toml` / `EyelibUtilMod` | mitigate | Use unique `eyelibutil` in `mods.toml`, `legacyForge.mods`, and `@Mod`; identity test checks all three. |
| T-16-03 | Tampering | Tooling artifacts | mitigate | Do not create VS Code, Eclipse, or JDTLS artifacts; use IntelliJ/JetBrains MCP for Gradle verification. |
</threat_model>

<verification>
Phase-level checks after this plan:
- JetBrains MCP Gradle sync succeeds for the project after `settings.gradle` includes `eyelib-util`.
- JetBrains MCP `:eyelib-util:test` exits 0.
- Static audit of `eyelib-util/build.gradle` reports zero `project(` matches.
- No utility implementation files are moved into `eyelib-util` in this plan.
</verification>

<success_criteria>
MOD-01 is satisfied for scaffold creation when `:eyelib-util` is registered, has a Forge build script with no project dependencies, has matching `eyelibutil` metadata/bootstrap, and its identity test runs through JetBrains MCP with exit code 0.
</success_criteria>

<output>
After completion, create `.planning/phases/16-module-scaffold-build-infrastructure/16-01-SUMMARY.md`.
</output>
