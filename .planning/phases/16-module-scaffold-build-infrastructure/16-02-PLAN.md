---
phase: 16-module-scaffold-build-infrastructure
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - eyelib-util/README.md
  - eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md
  - MODULES.md
  - docs/index/repo-map.md
  - docs/architecture/01-module-boundaries.md
autonomous: true
requirements:
  - MOD-01
  - MOD-02
must_haves:
  truths:
    - "Maintainer can identify :eyelib-util ownership and package namespace from repository documentation."
    - "Documentation states :eyelib-util is a project-dependency leaf with no project-internal dependencies."
    - "Solo JetBrains MCP build verification proves the documented scaffold compiles before utility migrations."
  artifacts:
    - path: "eyelib-util/README.md"
      provides: "Module-level ownership and dependency direction contract"
      contains: "io.github.tt432.eyelibutil"
    - path: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md"
      provides: "Package-local navigation and boundary rules"
      contains: "leaf"
    - path: "MODULES.md"
      provides: "Canonical module inventory entry for eyelib-util"
      contains: "eyelib-util"
    - path: "docs/index/repo-map.md"
      provides: "Repository navigation includes the util subproject"
      contains: ":eyelib-util"
    - path: "docs/architecture/01-module-boundaries.md"
      provides: "Architecture boundary and target ownership map for util module"
      contains: ":eyelib-util/**"
  key_links:
    - from: "MODULES.md"
      to: "eyelib-util/README.md"
      via: "module inventory main paths point to module docs"
      pattern: "eyelib-util/README.md"
    - from: "docs/architecture/01-module-boundaries.md"
      to: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/"
      via: "current major areas and ownership map document new module boundary"
      pattern: "eyelib-util/src/main/java/io/github/tt432/eyelibutil/"
    - from: "JetBrains MCP :eyelib-util:build"
      to: "eyelib-util/build.gradle"
      via: "solo build validates scaffold metadata and tests"
      pattern: ":eyelib-util:build"
---

<objective>
Document the `:eyelib-util` boundary and run the Phase 16 solo build gate.

Purpose: MOD-02 requires the new module boundary to be readable from the repository inventory, architecture docs, and package-local docs; MOD-01 requires JetBrains MCP solo build verification.
Output: module README, package README, updated inventory/index/boundary docs, and JetBrains MCP `:eyelib-util:build` evidence.
</objective>

<execution_context>
@C:/Users/q2437/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/q2437/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-CONTEXT.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-RESEARCH.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-PATTERNS.md
@.planning/phases/16-module-scaffold-build-infrastructure/16-01-SUMMARY.md
@AGENTS.md
@MODULES.md
@docs/index/repo-map.md
@docs/architecture/01-module-boundaries.md
@docs/architecture/02-side-boundaries.md

<interfaces>
<!-- Inputs from Plan 16-01 that this plan verifies/document. -->
- `settings.gradle` includes `include("eyelib-util")`.
- `eyelib-util/build.gradle` is a Forge Java 17 leaf module and contains zero `project(` matches.
- `eyelib-util/src/main/resources/META-INF/mods.toml` uses `modId="eyelibutil"`.
- `eyelib-util/src/main/java/io/github/tt432/eyelibutil/bootstrap/EyelibUtilMod.java` uses `@Mod(EyelibUtilMod.MOD_ID)`.
- Package namespace is `io.github.tt432.eyelibutil`.

Documentation decisions to preserve:
- Do not state that utility implementation migration is complete in Phase 16.
- State that root/sibling modules may depend on `:eyelib-util` only in later migration phases; Phase 16 does not add root dependency on `:eyelib-util`.
- State that `:eyelib-util` may depend on MC/Forge and external libraries, but must not depend on root or sibling Gradle projects.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write module-local ownership documentation</name>
  <files>eyelib-util/README.md, eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md</files>
  <action>Create `eyelib-util/README.md` documenting: module path `:eyelib-util`; mod id `eyelibutil`; package namespace `io.github.tt432.eyelibutil`; ownership of shared cross-cutting utility code after later migration phases; Phase 16 scaffold-only boundary with no utility implementation migration; dependency direction as a leaf module with zero `project(...)` dependencies; allowed integration layers as MC/Forge APIs plus external libraries already declared in `build.gradle`; and explicit rejection of root/sibling imports. Create package-local `eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md` using the sibling package README style, listing `bootstrap/` as the Forge marker package and stating new utility packages added in later phases must stay under `io.github.tt432.eyelibutil` without split packages against `io.github.tt432.eyelib.util`.</action>
  <verify>
    <automated>PowerShell: `$ErrorActionPreference = 'Stop'; $paths = @('eyelib-util/README.md','eyelib-util/src/main/java/io/github/tt432/eyelibutil/README.md'); foreach ($path in $paths) { if (-not (Test-Path $path)) { throw "missing documentation file: $path" }; foreach ($needle in @(':eyelib-util','eyelibutil','io.github.tt432.eyelibutil','leaf','project(...)')) { if (-not (Select-String -Path $path -SimpleMatch $needle -Quiet)) { throw "missing required token '$needle' in $path" } } }`</automated>
  </verify>
  <done>Module-local docs describe ownership, namespace, dependency direction, allowed integrations, and the Phase 16 no-migration boundary required by MOD-02.</done>
</task>

<task type="auto">
  <name>Task 2: Update repository inventory and architecture boundary docs</name>
  <files>MODULES.md, docs/index/repo-map.md, docs/architecture/01-module-boundaries.md</files>
  <action>Update `MODULES.md` summary to include the new `eyelib-util` subproject and add an inventory row with paths `eyelib-util/build.gradle`, `eyelib-util/src/main/java/io/github/tt432/eyelibutil/`, `eyelib-util/src/main/resources/META-INF/mods.toml`, and `eyelib-util/src/test/`. The row must state that `:eyelib-util` is a Forge shared utility leaf module, uses namespace `io.github.tt432.eyelibutil`, may use MC/Forge/external libraries, must not depend on root/sibling project modules, and is not consumed by root until later migration phases. Update `docs/index/repo-map.md` repository description and topic routing to mention `:eyelib-util` as the shared utility module scaffold while keeping existing platform-free utility/core routing accurate. Update `docs/architecture/01-module-boundaries.md` current major areas and ownership map with `:eyelib-util/**` as `shared.utility.module`; document that later phases migrate selected root/core utility classes into it, while Phase 16 only establishes build/documentation boundaries. Do not edit `docs/architecture/02-side-boundaries.md` unless execution discovers a concrete side-boundary wording mismatch; Phase 16 adds no packet/client/runtime behavior.</action>
  <verify>
    <automated>PowerShell: `$ErrorActionPreference = 'Stop'; $docs = @('MODULES.md','docs/index/repo-map.md','docs/architecture/01-module-boundaries.md'); foreach ($doc in $docs) { foreach ($needle in @(':eyelib-util','eyelib-util','io.github.tt432.eyelibutil')) { if (-not (Select-String -Path $doc -SimpleMatch $needle -Quiet)) { throw "missing required token '$needle' in $doc" } } }; foreach ($doc in @('MODULES.md','docs/architecture/01-module-boundaries.md')) { if (-not (Select-String -Path $doc -SimpleMatch 'leaf' -Quiet)) { throw "missing leaf dependency wording in $doc" } }`</automated>
  </verify>
  <done>Repository inventory, repo map, and module-boundary docs all include `:eyelib-util` with correct namespace, ownership, and leaf dependency direction.</done>
</task>

<task type="auto">
  <name>Task 3: Run JetBrains MCP solo build verification</name>
  <files>eyelib-util/build.gradle, eyelib-util/src/main/resources/META-INF/mods.toml, eyelib-util/src/test/java/io/github/tt432/eyelibutil/UtilModuleIdentityTest.java</files>
  <action>Use JetBrains MCP only. First run Gradle sync for project path `E:\_ideaProjects\qylEyelib`; then run the solo task `:eyelib-util:build` with `jetbrain_run_gradle_tasks`. If task listing is stale before sync, sync and retry through JetBrains MCP. Do not run `gradle`, `gradlew`, or `./gradlew` in shell. Record the exit code and any relevant output in the plan summary. Also perform a final text audit that `eyelib-util/build.gradle` still contains zero `project(` matches after docs work.</action>
  <verify>
    <automated>JetBrains MCP: `jetbrain_sync_gradle_projects(projectPath="E:\\_ideaProjects\\qylEyelib", externalProjectPath="E:\\_ideaProjects\\qylEyelib", timeoutMillis=240000)`, then `jetbrain_run_gradle_tasks(projectPath="E:\\_ideaProjects\\qylEyelib", externalProjectPath="E:\\_ideaProjects\\qylEyelib", taskNames=[":eyelib-util:build"], scriptParameters="", timeoutMillis=240000)` and require exitCode 0.</automated>
  </verify>
  <done>`:eyelib-util:build` completes with exit code 0 via JetBrains MCP, and `eyelib-util/build.gradle` still has zero `project(` dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → executor/maintainer decisions | Module docs become the source humans use for later migrations; inaccurate ownership text can cause reverse dependencies. |
| IDE Gradle model → build verification | JetBrains MCP sync/build is the approved boundary for proving Gradle correctness. |
| Scaffold → later migration phases | Phase 16 docs must not imply utility implementation migration has already happened. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Tampering | `MODULES.md` / architecture docs | mitigate | Explicitly state leaf direction and no root/sibling project dependencies in inventory and boundary docs. |
| T-16-05 | Repudiation | Phase 16 verification evidence | mitigate | Record JetBrains MCP sync/build command and exit code in `16-02-SUMMARY.md`. |
| T-16-06 | Information Disclosure | Docs | accept | Module docs expose only repository structure and dependency policy, no secrets or private endpoints. |
| T-16-07 | Tampering | Build verification tooling | mitigate | Use JetBrains MCP Gradle sync/build only; shell Gradle remains prohibited. |
</threat_model>

<verification>
Overall phase checks:
- `MODULES.md`, `docs/index/repo-map.md`, and `docs/architecture/01-module-boundaries.md` document `:eyelib-util` as a Forge shared utility leaf module.
- `eyelib-util/README.md` documents ownership, package namespace `io.github.tt432.eyelibutil`, zero project dependencies, MC/Forge allowance, and no Phase 16 utility migration.
- JetBrains MCP `:eyelib-util:build` exits 0.
- `eyelib-util/build.gradle` contains zero `project(` matches.
</verification>

<success_criteria>
Phase 16 is complete when MOD-01 and MOD-02 are both true: the standalone `:eyelib-util` Forge scaffold builds through JetBrains MCP with no project dependencies, and repository/module docs clearly document ownership, namespace, dependency direction, and allowed integration layers.
</success_criteria>

<output>
After completion, create `.planning/phases/16-module-scaffold-build-infrastructure/16-02-SUMMARY.md`.
</output>
