# Phase 25 Plan 01

## Scope

完成 capability payload/data/codec 抽取，不迁移 runtime owner。

## Steps

1. 将独立 attachment payload records 迁入 `:eyelib-attachment`。
2. 提取 component sync/persistence payload 类型到 `eyelibattachment.capability`。
3. 调整 root runtime owner、network packet、sync apply、tests 依赖到新 payload 类型。
4. 更新 attachment/network/boundary 文档。
5. 运行 attachment 模块测试、全量测试、全量 rebuild。
