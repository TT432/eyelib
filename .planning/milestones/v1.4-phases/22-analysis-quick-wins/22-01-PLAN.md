# Phase 22 Plan 01

## Scope

实现 `CODEQ-01` 与 `CODEQ-02` 的最小闭环。

## Steps

1. 确认 `client/animation` 下哪些接口是真正零引用，只删除确定为 dead code 的接口。
2. 将 `InstrumentDatabase` H2 文件路径切到 `.cache/eyelib_instrument`，并确保目录自动创建。
3. 同步修正 instrumentation 相关测试与日志文案。
4. 运行全量 `test` 与全量 `rebuild` 验证 Phase 22 成功标准。

## Verification

- `grep` 校验 `KeyFrame` 在 `.java/.json/.toml` 中为零出现
- `jetbrain_run_gradle_tasks ["test"]`
- `jetbrain_build_project rebuild=true`
