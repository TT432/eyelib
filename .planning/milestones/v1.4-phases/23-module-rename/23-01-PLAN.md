# Phase 23 Plan 01

## Scope

完成模块 rename + Forge 化的原子闭环。

## Steps

1. 将 Gradle 模块名切换到 `:eyelib-preprocessing`。
2. 将 `io.github.tt432.eyelibprocessor.*` 语义重命名为 `io.github.tt432.eyelibpreprocessing.*`。
3. 把模块源码/测试/README 与 IDEA 模块文件迁移到 `eyelib-preprocessing/`。
4. 为新模块补 `legacyForge` 骨架与 `META-INF/mods.toml`。
5. 更新关键入口文档与边界文档。
6. 运行模块测试、全量 rebuild、全量 test。
