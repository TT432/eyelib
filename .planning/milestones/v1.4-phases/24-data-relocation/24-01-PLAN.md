# Phase 24 Plan 01

## Scope

执行已确认的数据归位集合，不做 capability 迁移。

## Steps

1. 将 `client/model/bake` 整体迁入 `:eyelib-preprocessing`。
2. 将 controller 纯定义记录与其纯定义接口迁入 `:eyelib-importer`。
3. 把迁移后暴露出的 root 事件耦合收敛到 root 桥接层。
4. 更新模块文档和边界文档。
5. 运行模块测试、全量测试、全量 rebuild。
