# Eyelib Module Inventory

## Purpose
- This file is the canonical module inventory for the repository.
- A "module" here means any maintained responsibility unit, regardless of size: package subtree, boundary seam, documentation subsystem, or narrow support area that has its own reason to exist.
- Use this file when deciding scope, ownership, affected areas, and summary regeneration requirements.

## Summary
- Eyelib is a single-project `Gradle + Java 17 + Forge` repository. All source is under `src/main/java/io/github/tt432/eyelib/<module>/`, with modules separated by package convention (see [ADR-0014](docs/decisions/0014-flat-merge.md)). `clientsmoke/` is a composite build for the smoke test framework.
- Final particle ownership for the v1.2 gate is fixed as follows: `particle` owns module APIs, `ParticleDefinition`, `ParticleDefinitionAdapter`, executable runtime, client integration, render manager, particle packet contracts under `io.github.tt432.eyelib.particle.network`, and loading/publication through `ParticleDefinitionRegistry` plus `ParticleResourcePublication`; root owns Forge/resource adapter `BrParticleLoader`, root compatibility adapters, `mc/impl/common/command`, transport registration, and `NetClientHandlers` delegation; importer owns raw `io.github.tt432.eyelib.importer.particle.BrParticle` schema/codec. Active particle keys are `ParticleDefinition.identifier()`; source `ResourceLocation` values are diagnostics metadata only.
- Phase 14 verification keeps source tests independent from historical planning artifacts, records ClientSmoke/hardware evidence separately from JetBrains MCP Gradle gates, and treats PFUT-03 independent particle artifact publication, unrelated fixture cleanup, and manual visual proof as non-blocking scope boundaries.
- Phase 13 rewires command/network integration through root/MC adapters only; Phase 14 owns final split verification with stable source tests and JetBrains MCP gates.
- The repository currently centers on six large code domains: `bootstrap`, `client`, `molang`, `network/sync`, `dataattach/capability`, and `shared util/common`.
- Recent refactor work introduced several narrow seam modules to reduce context leakage: domain-specific `client/registry` writers, `client/gui/manager/io`, `client/gui/manager/reload`, `client/gui/manager/hotkey`, `client/particle` lookup/spawn seams, `network/dataattach`, and named client helper owners under `client/render/*`, `client/gui/preview/*`, and `client/*`.
- Utility split work now also includes additive platform-free seams, with adapter-style compatibility preserved in existing `util/*` callers.
- Documentation is also modularized: root guidance, architecture docs, index docs, plan docs, and package-local package-info.java files all act as maintained repository modules.
- Documentation follows the Diátaxis framework (concepts/decisions). The entry point is `docs/README.md`. ADR records live in `docs/decisions/`, architecture overview in `docs/concepts/architecture.md`. Operational knowledge lives in Hermes Skills.

## Inventory

> **Interactions 列**记录架构意图。模块边界以包名约定（`io.github.tt432.eyelib.<module>`）为准。

### Root And Documentation Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
| Root bootstrap guide | repository-wide working rules for humans and AI | `AGENTS.md` | points to repo map, architecture docs, package-info.java files |
|| Root package index | top-level code package overview | `src/main/java/io/github/tt432/eyelib/package-info.java` | routes readers into child modules |
||| Module inventory (this file) | repo map merged into self; canonical module boundary and ownership reference | `MODULES.md` | self-referential; entrypoint into architecture docs and package docs |
||| Documentation entry point | navigation index for the entire documentation system | `docs/README.md` | routes readers into concepts/decisions/reference/guides/ quadrants |
||| Architecture concepts | system architecture overview with C4 diagrams and module layering | `docs/concepts/architecture.md` | entry point for understanding system structure |
||| Architecture decisions | all ADR records in one directory | `docs/decisions/` (0001 through 0011) | governs structural edits |
||| Architecture operations | domain extraction guides, acceptance gates, port templates | `docs/architecture/` | operational manuals for the hexagonal refactoring |
|| Module boundaries doc | current ownership map and boundary rules | `docs/decisions/0002-module-boundaries.md` | used to classify affected modules |
|| Generated code policy | generated-vs-handwritten Molang rule set | `docs/decisions/0004-generated-code-policy.md` | governs parser regeneration/isolation |
|| Key decisions | historical record of architecture decisions | `docs/decisions/0006-key-architecture-decisions.md` | reference for why critical boundary choices were made |
|| Known pitfalls | 8 documented anti-patterns discovered during module separation | `docs/decisions/0007-known-pitfalls.md` | must read before any boundary-crossing refactor work |
|| Molang roadmap and refactor plan docs | current-state Molang roadmap plus dependency-ordered rewrite plan, entry gates, parity matrix, and cutover guidance | `docs/molang/ROADMAP.md`, `docs/molang/refactor-plan/` | `ROADMAP.md` is the current-state source of truth and must be updated when Molang phase status changes |
||| Util index | navigation for shared helper modules | `docs/decisions/` | points to util subtree and split destinations; merged into ADR context |
| Attachment module | Typed data attachment contracts, storage, and root-independent attachment packet contracts owned by the attachment functional module | `src/main/java/io/github/tt432/eyelib/attachment/` | root still owns transitional attachment registry and sync dispatch until those are moved by feature |
| Utility module | `:`util`` is a Forge shared utility leaf module | `src/main/java/io/github/tt432/eyelib/util/` | zero project-internal dependencies, must not depend on root or sibling packages |
| Model data module | `<module>` is a Forge canonical model data leaf module | `src/main/java/io/github/tt432/eyelib/model/` | |
| Resources importer module | Importer/schema Forge functional module for Bedrock addon parsing | `src/main/java/io/github/tt432/eyelib/importer/` | importer-owned code lives under `io.github.tt432.eyelib.importer.*` |
| Molang engine module | Gradle module for Molang-owned value/runtime, scope/compiler, parser frontend | `src/main/java/io/github/tt432/eyelib/molang/` | root keeps only root-coupled `MolangQuery` |
| `material` module | Bedrock material definitions, GL state, shader pipeline | `src/main/java/io/github/tt432/eyelib/material/` | contains `@ClientSmoke` material checks |
| Particle module | First-class particle module boundary for particle API/store/spawn | `src/main/java/io/github/tt432/eyelib/particle/` | |
| ItemStack tracking module
| Client smoke framework | External standalone client-side smoke discovery, annotation API, world setup, screenshot/report output, mouse-release support, and visual hook API | `clientsmoke/` Git submodule | scans `@ClientSmoke` tests from runtime modules; must not depend on feature modules such as `:`material`` |

FM-005 particle compatibility note: root legacy `client/particle/bedrock/**`, `ParticleLookup`, `ParticleManager`, `ParticleAssetRegistry`, and root `BrParticleRenderManager` have been deleted. `ParticleSpawnService` remains the temporary root context adapter and no longer accepts root `BrParticle` or `BrParticleEmitter` overloads.
 
### Bootstrap And API Surface Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
|| Bootstrap entrypoint | mod startup, capability registration, network registration | `src/main/java/io/github/tt432/eyelib/EyelibMod.java`, `src/main/java/io/github/tt432/eyelib/package-info.java`, `src/main/java/io/github/tt432/eyelib/Eyelib.java` | `EyelibMod` owns Forge startup composition; top-level `Eyelib` remains a constant holder for legacy call sites |
| API marker | future stable external API landing zone | `src/main/java/io/github/tt432/eyelib/api/package-info.java` | documents intended public surface |
| Internal marker | explicit default-internal policy | `src/main/java/io/github/tt432/eyelib/internal/package-info.java` | marks implementation packages as internal |

### Capability And Data Attachment Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
| Capability registry/data types | defines attachable data types and render-related data holders | `src/main/java/io/github/tt432/eyelib/capability/` | used by rendering, network, data attachment helpers |
| Capability component models | render/model component payload types plus RenderData-owned client/runtime component state; component-local invalidation state stays in `capability/component` while Forge client event hooks remain transitional runtime wiring; `ExtraEntityUpdateDataRuntimeHooks` owns the runtime observation/sync hook for the data-only extra-entity update attachment | `src/main/java/io/github/tt432/eyelib/capability/component/`, `src/main/java/io/github/tt432/eyelib/capability/` | synced by network packets and consumed by client render/runtime orchestration; Forge event-bus subscription for manager/texture invalidation and extra-entity update sync remains transitional runtime wiring |
| Data attachment container layer | typed attachment storage and mutation contracts now owned by the attachment module | `src/main/java/io/github/tt432/eyelib/attachment/dataattach/` | used by capability registry, entity state, and network sync payload seams |
| MC attachment capability wiring | transitional Forge capability/provider/event/NBT implementation for attachment containers | `src/main/java/io/github/tt432/eyelib/attachment/dataattach/mc/`, `src/main/java/io/github/tt432/eyelib/attachment/dataattach/(removed)` | binds Minecraft entity capabilities to `:`attachment`` storage and calls `network/dataattach` sync service until moved to the attachment functional owner |

### Client Runtime Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
|| Client root | root index for all client-only runtime code | `src/main/java/io/github/tt432/eyelib/client/package-info.java` | routes into render/model/loader/tooling submodules |
| | Animation module | Animation runtime, Bedrock clip/controller execution, keyframe interpolation, particle effect spawning through string-keyed interface. Owns `Animation`, `BrAnimationEntry`, `BrAnimationController`, `ModelRuntimeData`, `AnimationComponent`, `AnimationManager`, `AnimationParticleSpawner` (interface). | `src/main/java/io/github/tt432/eyelib/animation/` | particle type dependency eliminated in ADR-0009; root retains `AnimationAssetRegistry`, `BrAnimationLoader`, `BrAnimationControllerLoader` as adapters |
| Behavior module | Bedrock entity behavior component model: `BehaviorEntity`, component definitions, `ComponentGroup`, event filters, logic nodes, `EntityBehaviorData`, and the lightweight `SyncedBehaviorState` payload. | `src/main/java/io/github/tt432/eyelib/behavior/` | root retains capability wiring through `EyelibAttachableData` |
| Client entity runtime | runtime-facing client-entity and attachable lookup/helpers plus RenderData-side client-entity component support over importer-owned client-entity/attachable schema | `src/main/java/io/github/tt432/eyelib/client/entity/`, `src/main/java/io/github/tt432/eyelib/client/entity/package-info.java` | interacts with importer-owned parsing/schema, client-entity and attachable manager stores, RenderData capability components, render controllers, and particles |
|| Client render pipeline | render parameters, visitors, targets, render helpers, render type resolution, texture IO/merging | `src/main/java/io/github/tt432/eyelib/client/render/` | depends on models, materials, and render-owned helper classes |
| Client render sync seam | applies model/animation sync packets into render state via a string-keyed model payload seam (`RenderModelSyncPayload`) and runtime-only decode wiring | `src/main/java/io/github/tt432/eyelib/client/render/sync/ClientRenderSyncService.java`, `src/main/java/io/github/tt432/eyelib/client/render/sync/RenderSyncApplyOps.java`, `src/main/java/io/github/tt432/eyelib/client/render/sync/RenderModelSyncPayload.java` | called from network client handlers; keeps `ResourceLocation` decode at client runtime apply boundary |
| Client model domain | runtime-facing model helpers, bake/runtime data, root-side traversal/render adapters for imported models, and the Phase 15-routed `Models` merge/diff helper moved out of legacy `util/client` | `src/main/java/io/github/tt432/eyelib/client/model/` | consumes ``importer`` model definitions and feeds render/runtime systems |
| Client material domain | material definitions and entries | `src/main/java/io/github/tt432/eyelib/material/` | used by render controllers and material manager |
| Client particle compatibility adapters | transitional root particle spawn adapter path retained for packet/module-definition callers that still need root Minecraft/capability context | `src/main/java/io/github/tt432/eyelib/client/particle/`, `src/main/java/io/github/tt432/eyelib/client/particle/package-info.java` | delegates executable runtime, emitter/particle lifecycle, render-manager behavior, render/client hook ownership, and active spawn reads into `:`particle``; animation and command active lookup callers read `ParticleDefinitionRegistry` directly; root legacy `bedrock/**`, `ParticleLookup`, `ParticleManager`, `ParticleAssetRegistry`, and root `BrParticleRenderManager` were deleted during FM-005; `ParticleSpawnService` reads active definitions from `ParticleDefinitionRegistry`; spawn request and packet particle-id seams stay string-keyed, MC identifier validation/adaptation remains in MC runtime command/network boundaries, command/network integration remains Phase 13, and broad/client verification remains Phase 14 |
| Client compatibility adapters | external client compatibility integrations | `src/main/java/io/github/tt432/eyelib/client/compat/` | bridges to surrounding mod/client systems |
| Client cursor/gl helpers | cursor and GL-specific client support | `src/main/java/io/github/tt432/eyelib/client/cursor/`, `src/main/java/io/github/tt432/eyelib/client/gl/` | low-level client support for rendering/tooling |
| Client tick/runtime hooks | client-side periodic runtime orchestration and deferred task scheduling | `src/main/java/io/github/tt432/eyelib/client/ClientTickHandler.java`, `src/main/java/io/github/tt432/eyelib/client/EntityRenderSystem.java`, `src/main/java/io/github/tt432/eyelib/client/ClientTaskScheduler.java` | feeds render/tooling animation loops and next-tick actions; consumes server-synced behavior state for Molang context without executing behavior-pack spawn events on the client |

### Client Loader, Manager, Registry, And Tooling Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
| Resource loader base | common resource loading pattern used by client loaders | `src/main/java/io/github/tt432/eyelib/client/loader/BrResourcesLoader.java`, `src/main/java/io/github/tt432/eyelib/client/loader/SimpleJsonWithSuffixResourceReloadListener.java` | base for JSON/suffix loading behavior while lifecycle ownership continues migrating toward `mc/impl` |
| Asset loaders | own reload orchestration and root-side adaptation for resource-pack-side animations, materials, particles, client entities, attachables, render controllers, models, and textures while importer-owned schema parsing migrates into `:`importer`` | `src/main/java/io/github/tt432/eyelib/client/loader/`, `src/main/java/io/github/tt432/eyelib/client/loader/package-info.java` | publish resource-pack assets into domain-specific registry writers; behavior-pack publication is excluded from client reload and owned by the common behavior runtime; loader registration lifecycle is bound in `client/loader/`; `BrParticleLoader` preserves Forge `particles/*.json` scanning and side-safe registration while delegating particle publication to module loading/publication services; parse/translate helpers (LoaderParsingOps) are now owned by root client/loader/; client-entity/attachable codecs and controller-set parsing now come from `:`importer``, while animation-entry parsing is still transitional |
| Client loader lifecycle hook | Forge client reload-listener registration for all `Br*Loader` listeners | `src/main/java/io/github/tt432/eyelib/client/loader/` | owns `RegisterClientReloadListenersEvent` wiring and registers concrete loader instances |
| Runtime managers | singleton manager stores for animations, models, materials, render controllers, client entities, and attachables | `src/main/java/io/github/tt432/eyelib/client/manager/` | consumed through lookup seams and domain-specific writer classes; manager entry-change publication is owned by this functional module; the legacy particle manager compatibility map was removed during FM-005 because active particles live in `ParticleDefinitionRegistry` |
| Client registry seam | domain-specific loader/tooling→store publication boundaries, including attachable publication alongside client entities and addon bridge consumers | `src/main/java/io/github/tt432/eyelib/client/registry/`, `src/main/java/io/github/tt432/eyelib/client/registry/package-info.java` | used by loaders, manager import planner, manager import actions, and addon runtime bridging; behavior entity and spawn rule entries are compatibility wrappers over `common/behavior`; replacement seams are string-keyed where possible to avoid platform identifier leakage; particle loading/publication is no longer exposed through root `ParticleAssetRegistry` and must use module-owned `ParticleResourcePublication`/`ParticleDefinitionRegistry.publisher()` |
| Manager screen UI | developer/debug UI for importing and watching non-model client resources | `src/main/java/io/github/tt432/eyelib/client/gui/manager/`, `src/main/java/io/github/tt432/eyelib/client/gui/manager/package-info.java` | interacts with entities screen, import actions, and folder session helpers |
| Manager screen IO seam | async file/folder dialog handling | `src/main/java/io/github/tt432/eyelib/client/gui/manager/io/` | used only by manager screen UI |
| Manager screen reload seam | resource-folder and Bedrock addon import plus file watch lifecycle for animations, controllers, particles, entities, attachables, Bedrock models, and textures; runtime publication/upload orchestration stays in root while route/texture-key/batch helpers (ManagerResourceReloadPlan, ManagerResourceBatchPlanner) are now owned by root client/gui/manager/reload/ | `src/main/java/io/github/tt432/eyelib/client/gui/manager/reload/` | publishes through domain-specific registry seams, holds monitored folder session state, consumes addon aggregates through root bridge helpers, delegates plain planning/batching to `:`preprocessing``, and should avoid owning importer normalization logic |
| Manager screen hotkey seam | keybind registration and open-screen tick event | `src/main/java/io/github/tt432/eyelib/client/gui/manager/hotkey/` | opens `EyelibManagerScreen` without embedding event logic in UI class |
| Auxiliary client screens | extra screen tooling outside manager screen | `src/main/java/io/github/tt432/eyelib/client/gui/` | uses client/model/util helpers |

### Molang Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
|| Molang runtime core | Molang value wrappers, expression entrypoints, and vector helpers (`MolangValue*`) | `src/main/java/io/github/tt432/eyelib/molang/MolangValue*.java` | |
|| Molang engine scope/compiler/type/frontend | Engine-owned scope, compilation, persistent disk cache (MolangDiskCache with header validation), diagnostic/bytecode emission, Molang type system, and handwritten parser frontend (ANTLR-generated parser removed) | `src/main/java/io/github/tt432/eyelib/molang/MolangScope.java`, `src/main/java/io/github/tt432/eyelib/molang/compiler/`, `src/main/java/io/github/tt432/eyelib/molang/compiler/cache/`, `src/main/java/io/github/tt432/eyelib/molang/compiler/cache/MolangDiskCache.java`, `src/main/java/io/github/tt432/eyelib/molang/compiler/diagnostic/`, `src/main/java/io/github/tt432/eyelib/molang/type/`, `src/main/java/io/github/tt432/eyelib/molang/compiler/frontend/` | no reverse dependency back into root runtime |
| Molang generated parser ~~(removed)~~ | ~~generated lexer/parser/visitor artifacts~~ (ANTLR-generated artifacts deleted; responsibility merged into scope/compiler/type/frontend row) | ~~`src/main/java/io/github/tt432/eyelib/molang/generated/`~~ (directory removed) | ~~read-only generated code used by compiler~~ |
| Molang mappings | named Molang mapping support and built-in mapping entries | `src/main/java/io/github/tt432/eyelib/molang/mapping/`, `src/test/java/io/github/tt432/eyelib/molang/mapping/` | supports compiler/runtime lookup inside `:`molang``; platform bindings in ``molang`` populate extra mappings through discovery ports |
| Molang platform bindings | Forge/Minecraft platform ports: lifecycle hooks, Forge annotation discovery, Minecraft query runtime | `src/main/java/io/github/tt432/eyelib/molang/platform/` | engine mapping/compile code consumes ports from `:`molang``; platform bindings live in `io.github.tt432.eyelib.molang.platform.*`; root keeps only root-coupled `MolangQuery` in `molang/mapping/` |
| Manager event publication | owns manager entry-change publication and Forge event posting for manager store changes | `src/main/java/io/github/tt432/eyelib/client/manager/ManagerEventPublishBridge.java`, `src/main/java/io/github/tt432/eyelib/client/manager/ManagerEventPublisher.java`, `src/main/java/io/github/tt432/eyelib/client/manager/ForgeManagerEventPublisher.java`, `src/main/java/io/github/tt432/eyelib/client/manager/ManagerEventLifecycleHooks.java` | `client/manager/Manager.java` publishes through the manager-owned bridge; Forge `EVENT_BUS` posting remains in the same functional owner because Minecraft/Forge imports are not a module-boundary violation |
| Molang type system | Molang object/array/primitive type layer | `src/main/java/io/github/tt432/eyelib/molang/type/` | used by runtime and render controllers via `:`molang`` |
| Molang package index | local package guidance for Molang work plus root-side legacy marker handoff | `src/main/java/io/github/tt432/eyelib/molang/package-info.java`, `src/main/java/io/github/tt432/eyelib/molang/package-info.java` | directs readers into engine/runtime areas and the root legacy marker |

### Network And Sync Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
| Network registration/channel | sync network entrypoint, transport delegation seam, and context-free client handler dispatch | `src/main/java/io/github/tt432/eyelib/network/EyelibNetworkManager.java`, `src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java`, `src/main/java/io/github/tt432/eyelib/network/package-info.java` | owns shared channel entrypoints and handler delegation only; delegates packet channel registration plus side/context transport to `network/` (transport) `; particle packet DTO/codecs are owned by `:`particle`` under `io.github.tt432.eyelib.particle.network`, and root-independent attachment packet DTO/codecs are owned by `:`attachment`` under `io.github.tt432.eyelib.attachment.network` |
| MC sync transport/channel wiring | Forge channel setup, packet context handling, side gating, and player/entity dispatch | `src/main/java/io/github/tt432/eyelib/network/` (transport) `, `src/main/java/io/github/tt432/eyelib/network/` (transport) package-info.java`, `src/main/java/io/github/tt432/eyelib/(removed)` | owns direct `SimpleChannel`, `NetworkEvent.Context`, `DistExecutor`, and packet distributor wiring; registers particle-owned packet contracts from `io.github.tt432.eyelib.particle.network` and attachment-owned root-independent packet contracts from `io.github.tt432.eyelib.attachment.network`; remaining packet classes under `(removed)` are root-coupled blockers tied to root render/model/capability/data owners, not a destination for new feature protocols |
| Client packet handlers | client application of sync packets | `src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java` | Phase 13 keeps this context-free: particle spawn/remove packets delegate only to `ParticleSpawnService`, while data-attachment applies delegate to dedicated runtime services |
| Model/animation sync packets | client synchronization of render/model components; `ModelComponentSyncPacket` is owned by `:`model`` under `io.github.tt432.eyelib.model.network.packet`, `AnimationComponentSyncPacket` is owned by `:`animation`` under `io.github.tt432.eyelib.animation.network` | `src/main/java/io/github/tt432/eyelib/model/network/packet/`, `src/main/java/io/github/tt432/eyelib/animation/network/` | apply into render data on client; registered by root network manager |
| Particle packet layer | remove/spawn particle packet contracts with string-keyed payload seams | `src/main/java/io/github/tt432/eyelib/particle/network/RemoveParticlePacket.java`, `src/main/java/io/github/tt432/eyelib/particle/network/SpawnParticlePacket.java` | Particle-owned `SpawnParticlePacket(String spawnId, String particleId, Vector3f position)` and `RemoveParticlePacket(String removeId)` stay under `:`particle``; root transport registers them and client side delegates into `ParticleSpawnService`; packet id adaptation no longer requires `ResourceLocation` in root `network/**` handlers |
| Attachment packet contracts | generic and specialized attachment packet contracts | `src/main/java/io/github/tt432/eyelib/attachment/network/DataAttachmentSyncPacket.java`, `src/main/java/io/github/tt432/eyelib/attachment/network/UpdateDestroyInfoPacket.java`, `src/main/java/io/github/tt432/eyelib/(removed)DataAttachmentUpdatePacket.java`, `src/main/java/io/github/tt432/eyelib/(removed)UniDataUpdatePacket.java`, `src/main/java/io/github/tt432/eyelib/(removed)ExtraEntityDataPacket.java`, `src/main/java/io/github/tt432/eyelib/(removed)ExtraEntityUpdateDataPacket.java` | root-independent full-container sync and destroy-info contracts are attachment-owned; root retains update/extra packet contracts that still decode through root `EyelibAttachableData` or attachment-facing capability payload types |
| Network data-attach seam | transport-independent attachment payload/state mapping for remaining root-coupled attachment sync | `src/main/java/io/github/tt432/eyelib/(removed)`, `src/main/java/io/github/tt432/eyelib/(removed)README.md` | shared by runtime sync owners under `eyelib-(removed)` until root attachment registry/capability data blockers move to attachment-owned contracts |

### Common, Command, Event, And Mixin Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
| Entity extra-data handlers | entity extra-data lifecycle and sync triggers | `src/main/java/io/github/tt432/eyelib/attachment/runtime/EntityExtraDataRuntimeHooks.java` | runtime hooks use attachment helpers, ExtraEntityDataUpdater, and network sync runtime; moved from root `common/entity/` |
| Behavior pack runtime | server-side Bedrock behavior pack loading, vanilla behavior entity fallback publication, behavior entity/spawn rule registries, entity spawn-state calculation, and lightweight behavior-state sync | `src/main/java/io/github/tt432/eyelib/common/behavior/`, `src/main/java/io/github/tt432/eyelib/CommonEntityEventHandler.java` | consumes importer-owned Bedrock addon schemas and behavior-module runtime models; writes server-local `EntityBehaviorData` and syncs `SyncedBehaviorState` through the attachment/network runtime |
| Command module | Phase 13 particle-related command adapter entrypoints | `src/main/java/io/github/tt432/eyelib/common/command/EyelibParticleCommand.java`, `src/main/java/io/github/tt432/eyelib/(removed)common/command/README.md` | `ParticleCommandRuntime` owns deterministic platform-free suggestion/request/message shaping; `EyelibParticleCommand` owns Brigadier parsing, `ResourceLocationArgument`, `CommandSourceStack`, `ServerPlayer`, `Vec3Argument`, `Component`, conversion to string ids, and dispatch of string-keyed particle sync packets via MC transport |
| Event module | custom Eyelib events for init, manager entry changes, texture changes | `src/main/java/io/github/tt432/eyelib/event/` | consumed across client runtime and tooling |
| Mixin module | technical Sponge Mixin wiring and mixin config alignment, not feature ownership by package name | `src/main/java/io/github/tt432/eyelib/mixin/`, `src/main/resources/eyelib.mixins.json` | runtime integration with vanilla classes through a shared mixin package root; feature ownership follows the consuming module, including the client-render-owned `LivingEntityRendererAccessor` used by `SimpleRenderAction` |

### Shared Utility Modules
| Module | Responsibility | Main paths | Interactions |
|---|---|---|---|
|| Util root | local index and rules for utility code | `src/main/java/io/github/tt432/eyelib/util/package-info.java` | routes into client/data_attach/codec/math/search |
| Util client deterministic helpers | historical helper area drained by utility split; Phase 18 deleted the remaining `TexturePathHelper` wrapper | `src/main/java/io/github/tt432/eyelib/util/client/` | do not add new helpers here; callers should use functional owners or `:`util`` directly |
| Texture path helper | deterministic texture-path transform now owned by `:`util`` | `src/main/java/io/github/tt432/eyelib/util/texture/TexturePaths.java` | consumed by render/controller code and tests without root/core wrapper duplication |
| Render pose copy helper | `PoseStack.Pose` copy helper under render ownership | `src/main/java/io/github/tt432/eyelib/client/render/PoseCopies.java` | used by render params and visitors |
| Native image IO | image load/upload helpers | `src/main/java/io/github/tt432/eyelib/client/render/texture/NativeImageIO.java` | used by render controller textures, cursor loading, and manager import planner |
| Texture layer merger | merged texture atlas generation for layered render-controller textures | `src/main/java/io/github/tt432/eyelib/client/render/texture/TextureLayerMerger.java` | used by render controller texture composition |
| Render type resolver | render-type id to runtime factory resolution | `src/main/java/io/github/tt432/eyelib/client/render/RenderTypeResolver.java` | used by materials, model components, and particles |
| Model preview asset | preview-only model/atlas pair for GUI preview flow | `src/main/java/io/github/tt432/eyelib/client/gui/preview/ModelPreviewAsset.java` | used by `ModelPreviewScreen` |
| Codec utilities | custom stream/codec helpers now owned by `:`util`` | `src/main/java/io/github/tt432/eyelib/util/codec/` | used by root animation/behavior serialization and kept atomic with `ImmutableFloatTreeMap`; material duplicate codec cleanup remains Phase 20 |
| Math utilities | math and transform helpers | `src/main/java/io/github/tt432/eyelib/util/math/` | used by render/model/UI helpers |
| Search utilities | search/index result helpers | `src/main/java/io/github/tt432/eyelib/util/search/` | used by loaders and tooling UI |
| Modbridge (removed) | historical Phase 15 route record for the drained legacy modbridge utility package after `ModBridgeServer` and `BBModelSink` were deleted | (deleted — migration records removed) | modbridge module has been removed; `ModelPreviewScreen` no longer includes `ModBridgeModelUpdateEvent` |
| Generic util helpers | root util Java source is fully drained after Phase 19; the `util/` directory has been removed | removed | Phase 17 moved lists/blackboards/library loading/timers, Phase 18 moved resource locations, Phase 19 moved codec infrastructure, and the final `util/model/InventoryModelResourceLocations` moved to `:`util`` |

## Module Update Rules
1. Before any change, identify every affected module in this file.
2. If a change modifies code or docs inside a listed module, update that module’s summary if its responsibility, paths, or interactions changed.
3. If a change adds a new module or removes an existing module, update this file in the same change.
4. If a change alters module boundaries, also update `docs/decisions/0002-module-boundaries.md` and any relevant package-info.java files.
5. If a change affects packet/data-attachment/client-side applicability, also re-check `docs/decisions/0002-module-boundaries.md`.
6. If a change affects Molang phase status, milestones, gates, ownership, verification commands, corpus layers, binder/runtime semantics, host/query behavior, policy/specialization/cache behavior, or cutover posture, update `docs/molang/ROADMAP.md` in the same change.

## Architecture Adapter Modules

### Bridge Subproject
|| Module | Responsibility | Main paths | Interactions |
||---|---|---|---|
|| `bridge` module | `:`bridge`` is the hexagonal architecture adapter layer: implements Domain Port interfaces for MC/Forge runtime, provides Entity→PortEntity adaptation, RenderType resolution bridge, platform Molang query bindings, and MinecraftMolangQueryRuntime. All MC import surface for domain modules concentrates here. | `src/main/java/io/github/tt432/eyelib/bridge/`, `src/main/java/io/github/tt432/eyelib/bridge/material/`, `src/main/java/io/github/tt432/eyelib/bridge/molang/`, `` | depends on all domain modules (material, molang, model, animation, importer, behavior) + `util`; implements domain Port interfaces; MC RenderType/Entity/Level types used internally; direction: bridge → domain (no reverse) |

## Regeneration Checklist
- Re-read the affected package-info.java files.
- Re-check the current package tree under `src/main/java/io/github/tt432/eyelib/`.
- Update the affected module rows in this file.
- Add/remove rows for newly created or deleted modules.
- Re-check links from `AGENTS.md`, `docs/decisions/`, and any touched package-info.java files.
