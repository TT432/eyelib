# Eyelib Module Boundaries

## Current Major Areas
- `eyelib-preprocessing/src/main/java/io/github/tt432/eyelibpreprocessing/`: platform-free processing and batching helpers for loader and manager reload planning seams.
- `eyelib-importer/src/main/java/io/github/tt432/eyelibimporter/model/`: model definitions, locators, source formats (`bbmodel`, `bedrock`), and importer normalization/support data.
- `src/main/java/io/github/tt432/eyelib/client/`: client rendering, animation, particles, GUI, loaders, managers, tooling.
- `src/main/java/io/github/tt432/eyelib/molang/`: legacy Molang marker/docs handoff path.
- `eyelib-molang/src/main/java/io/github/tt432/eyelibmolang/`: engine-owned Molang value/runtime wrappers, scope/compiler/type/mapping-api/built-in mappings, plus generated grammar artifacts.
- `eyelib-material/src/main/java/io/github/tt432/eyelibmaterial/`: Bedrock material definitions, GL state management, shader pipeline, and shared pure-data types under `shared/` package.
- `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/`: first-class particle module boundary, build/package contract, module-owned particle APIs, `io.github.tt432.eyelibparticle.runtime.ParticleDefinition` as the canonical module runtime definition owner, `ParticleDefinitionAdapter` as the explicit adapter seam without root runtime reverse dependency, Phase 11 executable runtime/render-manager/client integration ownership, module-owned spawn/runtime adapters, Phase 12 loading/publication ownership through `ParticleDefinitionRegistry`, `ParticleResourcePublication`, and the active `ParticleStore<ParticleDefinition>` keyed by `ParticleDefinition.identifier()`, plus particle packet contracts under `io.github.tt432.eyelibparticle.network`.
- `:eyelib-util/**`: Forge shared utility leaf module with active ownership for `io.github.tt432.eyelibutil.time`, `.color`, `.loader`, `.math`, `.search`, `.collection`, `.resource`, `.texture`, `.codec`, and `.streamcodec`; root and approved sibling modules consume it through explicit dependency edges, while the util module keeps a distinct non-split namespace, allowed MC/Forge/external-library integration layers, and zero project-internal dependencies.
- `clientsmoke/`: external standalone client smoke framework and annotation API, consumed through a Gradle composite build and kept independent from feature modules.
- `src/main/java/io/github/tt432/eyelib/network/`: shared channel entrypoint, transport delegation, and context-free client/server handler dispatch only; particle packet DTO/codecs live under `io.github.tt432.eyelibparticle.network`, and root-independent attachment packet DTO/codecs live under `io.github.tt432.eyelibattachment.network`.
- `src/main/java/io/github/tt432/eyelib/capability/`: attachment-related capability registration and data holders.
- `src/main/java/io/github/tt432/eyelib/common/`: shared behavior logic.
- `src/main/java/io/github/tt432/eyelib/util/`: shared helpers plus mixed client/data-attachment utilities.
- `src/main/java/io/github/tt432/eyelib/mc/`: transitional root-level Minecraft/Forge wiring for code that has not yet been assigned to a functional owner.
- `eyelib-attachment/src/main/java/io/github/tt432/eyelibattachment/`: attachment functional module for typed attachment storage/contracts and Minecraft stream codec utilities.

## Existing Patterns To Preserve
- Manager pattern around `src/main/java/io/github/tt432/eyelib/client/manager/Manager.java`.
- Loader pattern in `src/main/java/io/github/tt432/eyelib/client/loader/`.
- Visitor pattern in `src/main/java/io/github/tt432/eyelib/client/render/visitor/`.
- Codec-heavy serialization approach across model, animation, particle, and Molang-related types.

## Current Boundary Problems
- `src/main/java/io/github/tt432/eyelib/client/gui/manager/EyelibManagerScreen.java` is now narrowed to UI composition and action delegation, with folder session state and import actions extracted into dedicated helpers.
- `src/main/java/io/github/tt432/eyelib/molang/grammer/` is a legacy name that historically made generated parser artifacts look like normal source files.
- `src/main/java/io/github/tt432/eyelib/client/loader/` now parses non-model assets into local maps first, then hands publication to domain-specific registry owners or module-owned publication services; `BrParticleLoader` is only the Forge/resource adapter that preserves `particles/*.json` scanning and delegates active particle publication to `ParticleResourcePublication`.
- `src/main/java/io/github/tt432/eyelib/util/client/` has been drained of its major historical shims; render type resolution, native image IO, texture merging, preview assets, client tick scheduling, `AnimationApplier`, and `Models` now live under named client owners (`AnimationApplier` in `client/animation`, `Models` in `client/model`), and the remaining texture-path wrapper moved to `:eyelib-util` during Phase 18.
- The v1.3 utility split drained root `src/main/java/io/github/tt432/eyelib/util/` and `src/main/java/io/github/tt432/eyelib/core/util/` Java sources into `:eyelib-util` or functional owners.
- `src/main/java/io/github/tt432/eyelib/network/` and `src/main/java/io/github/tt432/eyelib/util/data_attach/` now share a narrower sync seam via `network/dataattach/DataAttachmentSyncPayloadOps.java`, while transport/context/apply runtime ownership moved under `mc/impl/network/`; attachment writes no longer auto-trigger sync implicitly through `DataAttachmentHelper`.
- `src/main/java/io/github/tt432/eyelib/util/data_attach/` now owns platform-type-free attachment contracts/storage only; Forge capability provider/event wiring plus NBT persistence moved under `src/main/java/io/github/tt432/eyelib/mc/impl/data_attach/`.
- Core runtime reads are starting to move away from `Eyelib.java` reach-through and into domain lookup seams such as `client/animation/AnimationLookup.java`, `client/model/ModelLookup.java`, `client/entity/ClientEntityLookup.java`, and `client/render/controller/RenderControllerLookup.java`.
- First hard-quarantine move is now in place for Molang: Minecraft query/runtime lifecycle hooks and query implementation moved under `src/main/java/io/github/tt432/eyelib/mc/impl/molang/`, while the root `src/main/java/io/github/tt432/eyelib/molang/` path is now only a legacy marker/docs handoff.
- Manager/registry publication has been corrected to functional ownership: `client/manager/Manager.java` now publishes through manager-owned bridge contracts, Forge event-bus wiring lives under `src/main/java/io/github/tt432/eyelib/client/manager/`, and `client/registry/ClientEntityAssetRegistry.java` no longer exposes `ResourceLocation` in its replacement seam.
- Utility hard-quarantine follow-up is in progress: fixed-step timer Minecraft runtime access and Forge `modbridge` update event ownership moved to `src/main/java/io/github/tt432/eyelib/mc/impl/util/time/` and `src/main/java/io/github/tt432/eyelib/mc/impl/modbridge/`, while deterministic timer state moved to `src/main/java/io/github/tt432/eyelib/core/util/time/`.
- Utility hard-quarantine follow-up completed for the v1.3 scope: legacy render/model/modbridge helpers moved to functional owners, all shared helper families moved to `:eyelib-util`, and root/core util Java source globs are empty.
- Client-loader hard-quarantine follow-up is in progress: Forge `RegisterClientReloadListenersEvent` wiring for concrete `Br*Loader` classes is now owned by `src/main/java/io/github/tt432/eyelib/mc/impl/client/loader/ClientLoaderLifecycleHooks.java`, and platform-free parse/translation helpers now live in `:eyelib-preprocessing` (`io.github.tt432.eyelibpreprocessing.loader.LoaderParsingOps`) without `ResourceLocation` in method signatures.
- Client-render hard-quarantine follow-up has started from the sync seam: `client/render/sync/RenderSyncApplyOps` now uses string-keyed `RenderModelSyncPayload` for model payload collection/replacement, while `ResourceLocation` decode stays in `client/render/sync/ClientRenderSyncService` runtime wiring.
- Client-particle ownership follow-up has advanced from the spawn/request seam: spawn request state is owned by the module API `io.github.tt432.eyelibparticle.api.ParticleSpawnRequest`, `io.github.tt432.eyelibparticle.network.SpawnParticlePacket` now uses a string-keyed particle id contract, and module client adapter `io.github.tt432.eyelibparticle.client.ParticleSpawnRuntimeAdapter` owns particle-only definition lookup, emitter creation, spawn, and remove delegation; Minecraft identifier validation/adaptation stays in `mc/impl/common/command` and transport runtime boundaries, while root `ParticleSpawnService` remains a compatibility facade for packet entrypoints, module-definition runtime callers, and root Minecraft/capability context.
- Common-runtime hard-quarantine follow-up has started from the command hotspot: command registration/runtime wiring moved to `src/main/java/io/github/tt432/eyelib/mc/impl/common/command/EyelibParticleCommand.java`, while deterministic suggestion/request/message shaping now lives in platform-type-free `src/main/java/io/github/tt432/eyelib/common/runtime/ParticleCommandRuntime.java`.
- Common entity attachment ownership follow-up has landed for extra-entity data: pure goal-flag update policy stays in `src/main/java/io/github/tt432/eyelib/common/runtime/ExtraEntityDataUpdater.java`, while Forge start-tracking/tick subscriptions, mob-goal observation, attachment mutation, and sync dispatch live in `src/main/java/io/github/tt432/eyelib/mc/impl/common/entity/EntityExtraDataRuntimeHooks.java`.
- Client-gui-tools hard-quarantine follow-up has started from reload planning state: platform-free path normalization, single-file route classification, and texture-key planning (`Path`/`String` only) now live in `:eyelib-preprocessing` (`io.github.tt432.eyelibpreprocessing.manager.reload.ManagerResourceReloadPlan`), while root `ManagerResourceImportPlanner` keeps Minecraft/Forge texture upload and event posting runtime wiring and now delegates plain file-batch assembly to `:eyelib-preprocessing`.
- Client-model hard-quarantine follow-up has started from definition visible-bounds state: `Model` and importer-side `ImportedModelData` now use platform-free `VisibleBox` instead of `AABB`; remaining MC runtime ownership in this area is concentrated in bake/model-part/render helpers (`ModelPart`, `PartPose`, `PoseStack`, and texture/runtime bake paths).
- Client-model hard-quarantine follow-up advanced with a second definition-side seam: `Model.TextureMesh` vector codec fields no longer use direct `net.minecraft.util.ExtraCodecs` from `client/model/Model.java` and now route through Eyelib float-list codecs; remaining MC runtime ownership in this area is still concentrated in bake/model-part/render helpers (`ModelPart`, `PartPose`, `PoseStack`, texture/runtime bake paths) plus importer/runtime native image flow.
- Capability runtime-component hard-quarantine follow-up has started: Forge `ManagerEntryChangedEvent` and `TextureChangedEvent` listener ownership for `AnimationComponent`/`RenderControllerComponent` invalidation now lives in `src/main/java/io/github/tt432/eyelib/mc/impl/capability/CapabilityComponentRuntimeHooks.java`, while component-local invalidation state/methods stay in `capability/component/`. `ExtraEntityUpdateData` is now data/codec-only, with living damage/tick observation, attachment mutation, and update-packet sync in `src/main/java/io/github/tt432/eyelib/mc/impl/capability/ExtraEntityUpdateDataRuntimeHooks.java`.
- Mixin integration relocation has now landed as technical wiring: mixin classes moved from legacy `io.github.tt432.eyelib.mixin` to `src/main/java/io/github/tt432/eyelib/mc/impl/mixin/`, and `src/main/resources/eyelib.mixins.json` now points at `io.github.tt432.eyelib.mc.impl.mixin`; feature ownership still follows the consumer, so `LivingEntityRendererAccessor` is client-render-owned even though the shared mixin package root physically hosts it.
- Importer/model split has landed as a Gradle subproject: `:eyelib-importer` now owns `client/model/Model`, locator/tree support, `client/model/bbmodel`, `client/model/bedrock`, and importer-side data/repacker/support classes; root `client/model/importer` keeps runtime adapter/build steps and tooling entrypoints, while render traversal stays in root visitors.
- Next importer-boundary expansion is now materially landed: `eyelib-importer` owns client-entity/attachable schema codecs, importer image data (`ImportedImageData`), controller-side schema records/sets, and importer-side model/entity/animation schema under the dedicated `io.github.tt432.eyelibimporter.*` namespace, while root `client/animation`, `client/entity`, `client/loader`, and `client/gui/manager/reload` retain runtime execution, manager publication, texture upload, and remaining runtime adaptation.
- Full Molang extraction has landed as a Gradle subproject: `eyelib-molang` now owns Molang value/runtime wrappers, scope/owner-set/compiler/cache/generated parser/mapping-api/type code, plus built-in mapping entries (`MolangMath`, `MolangToplevel`); root keeps only `mc/impl/molang/**` platform/query/lifecycle bindings and the legacy `molang/` marker path.
- Phase 15 utility routing moved the roadmap-named single-consumer helpers instead of deleting them: `AnimationApplier` now lives under `src/main/java/io/github/tt432/eyelib/client/animation/`, `Models` now lives under `src/main/java/io/github/tt432/eyelib/client/model/`, and retained ModBridge local TCP/update bridge ownership (`ModBridgeServer` plus `BBModelSink`) now lives under `src/main/java/io/github/tt432/eyelib/mc/impl/modbridge/` with the existing payload-bound/lifecycle behavior preserved.

## Current To Target Ownership Map
| Current area | Target owner | Boundary intent |
|---|---|---|
| `mc/impl/bootstrap/EyelibMod.java` | `bootstrap` | Own Forge `@Mod` startup composition in allowed `mc/impl` quarantine, while top-level `Eyelib` stays constant-only |
| `client/loader/` | `client.asset` | Parse and reload resources, avoid owning runtime publication, and keep parser seams free of platform identifier types; for particles, root `BrParticleLoader` preserves Forge reload scanning/side-safe registration and delegates active publication to `:eyelib-particle` loading services |
| `mc/impl/client/loader/` | platform integration zone | Own Forge reload-listener lifecycle registration for client loader listeners |
| `mc/impl/common/command/` | platform integration zone | Own Forge command registration, Brigadier runtime wiring, `ResourceLocationArgument`, `CommandSourceStack`, `ServerPlayer`, `Vec3Argument`, `Component`, and conversion to string ids before command requests/packets; deterministic suggestion/request/message shaping stays in `ParticleCommandRuntime` |
| `mc/impl/common/entity/` | common entity runtime observer | Own Forge entity event subscriptions, Minecraft mob-goal observation, attachment mutation calls, and sync dispatch for common entity-derived attachment state; deterministic update policy stays in `common/runtime` |
| `:eyelib-preprocessing/**` | `client.processing` | Own platform-free processing and batching helpers for loader/reload planning; may consume importer schema and plain-JVM Molang analysis, but must not own runtime manager publication, texture upload, Forge events, RenderSystem/session lifecycle, or UI |
| `:eyelib-importer/**` | `client.model.importer.core` | Current identity is an importer/schema Forge functional module: own model definitions plus source-format parsing/normalization/import support, addon/pack discovery, raw resource-side aggregation, and isolated `eyelibimporter` Forge bootstrap without runtime manager/event/upload ownership; a separate plain importer library is future debt |
| importer-owned client-entity / animation-controller schema under `:eyelib-importer/**` | `client.importer.schema` | Own codec trees, parsed definitions, importer-only normalization, and platform-free image/data representations for import flows; must not own runtime execution, manager publication, texture upload, or Forge/Minecraft lifecycle wiring |
| `client/model/importer/` | `client.model.importer` | Adapt `eyelib-importer` outputs into root runtime flows without leaking source-format behavior into tooling, managers, or render execution |
| `client/animation/` | `client.animation.runtime` | Keep runtime animation/controller execution, playback state, runtime lookups, and schema-to-runtime adapter/build logic in root even as parsed schema moves into `eyelib-importer` |
| `client/entity/` | `client.entity.runtime` | Keep runtime-facing lookup/helpers and RenderData integration in root while parsed client-entity/attachable schema migrates into `eyelib-importer` |
| `client/manager/` | `client.registry` | Keep runtime lookup, manager event publication, and event-backed storage centralized, including client entities |
| `client/gui/manager/` | `client.tools` | Development/debug UI only; move import/watch/IO into helpers/services |
| `client/registry/` | `client.registry` | Hold domain-specific loader/tooling-to-manager publication seams; root `ParticleAssetRegistry` was removed and particle writes use `ParticleResourcePublication`/`ParticleDefinitionRegistry.publisher()` directly |
| `client/* lookup seams` | domain-local read ports | Narrow runtime queries instead of bootstrap reach-through |
| `mc/impl/**` | transitional root wiring | Remaining shared Minecraft/Forge wiring that still needs functional ownership decisions; it is not the sole long-term home for Minecraft/Forge imports |
| `mc/impl/mixin/` + `eyelib.mixins.json` | technical mixin wiring zone | Own Sponge Mixin package/config wiring only; feature ownership follows the behavior/consumer, including the client-render-owned `LivingEntityRendererAccessor` accessor used by `SimpleRenderAction` |
| `eyelib-molang/src/main/java/io/github/tt432/eyelibmolang/generated/` | `molang.generated` | Treat as generated and isolate from normal handwritten work |
| `eyelib-molang/**` | `molang.engine` | Own Molang value/runtime, compile/type/scope/mapping-api, and built-in mappings without depending on root runtime packages |
| `:eyelib-particle/**` | `client.particle.module` | Own particle module contracts, `io.github.tt432.eyelibparticle.runtime.ParticleDefinition` as the canonical module runtime definition owner, `ParticleDefinitionAdapter` as the named conversion seam from importer-owned schema, executable runtime components/lifecycle, spawn/runtime adapter behavior, render-manager behavior, render adapter behavior, `Dist.CLIENT` client hook integration, particle packet contracts in `io.github.tt432.eyelibparticle.network`, and Phase 12 active loading/publication through `ParticleDefinitionRegistry` plus `ParticleResourcePublication`; root may consume it, but it must not depend on root runtime packages, root managers, root registries, root packets, root capability helpers, or root mc/impl; `io.github.tt432.eyelibimporter.particle.BrParticle` is the canonical raw Bedrock particle schema/codec owner, the allowed particle -> importer dependency for ParticleDefinitionAdapter is the only schema dependency direction, and active publication parses importer-schema JSON through `ParticleResourcePublication`, converts with `ParticleDefinitionAdapter`, and stores by `ParticleDefinition.identifier()` in the module `ParticleStore<ParticleDefinition>`; root legacy `client/particle/bedrock/**`, `ParticleAssetRegistry`, `ParticleManager`, `ParticleLookup`, and root `BrParticleRenderManager` have been deleted; retained root particle classes are limited to `BrParticleLoader` and temporary `ParticleSpawnService`, and must not own canonical loading/publication business logic; `ParticleSpawnService` may only adapt packet/module-definition runtime callers and root Minecraft/capability context into module client services; Minecraft/Forge client rendering and `:eyelib-material` render-type resolution are allowed only in the documented particle `client/**` integration layer; root command/network adapters register and dispatch particle-owned packets, and Phase 14 owns broad/client verification evidence |
| `:eyelib-util/**` | `shared.utility.module` | Own active shared utility packages `time`, `color`, `loader`, `math`, `search`, `collection`, `resource`, `texture`, `codec`, and `streamcodec` under `io.github.tt432.eyelibutil`; root and approved sibling modules may consume the module through explicit Gradle dependency edges, but `:eyelib-util` remains a leaf with no `project(` dependencies |
| `network/` | `sync` | Own shared network entrypoints, transport delegation, and context-free handler dispatch only; `NetClientHandlers` stays transport/context-free and routes particle spawn/remove packets to `ParticleSpawnService` without render-manager, loader, or registry internals |
| `mc/impl/network/packet/` | root-coupled protocol blockers | Owns remaining root-coupled packet DTO/codecs only while they decode through root owners: render/model sync contracts depend on `AnimationComponent.SerializableInfo` or `RenderModelSyncPayload`, and attachment update/extra packets depend on `EyelibAttachableData`, `ExtraEntityData`, or `ExtraEntityUpdateData`; root transport registers particle-owned packet contracts from `io.github.tt432.eyelibparticle.network` and root-independent attachment packet contracts from `io.github.tt432.eyelibattachment.network` |
| `network/dataattach/` | `sync` + `dataattach` seam | Centralize remaining root-coupled attachment payload/state mapping until root attachment registry and capability data move to the attachment owner or expose attachment-owned lookup contracts |
| `capability/` + `:eyelib-attachment` | `dataattach` | Own attachment state, ids, storage, codecs, root-independent packet contracts, and mutation rules by functional responsibility |
| `mc/impl/data_attach/` | platform integration zone | Own Forge capability/provider/event and NBT serialization wiring for attachment containers |
| `mc/impl/capability/` | platform integration zone | Own Forge client event-hook wiring for capability runtime-component invalidation |
| selected `util/` classes | `shared` | Keep only truly cross-cutting helpers here |

## Boundary Notes For Client Entity And Render Controller Runtime
- `BrClientEntity` is a parsed client-entity definition, not a runtime-state owner.
- Per-entity client-entity runtime belongs on `RenderData`-owned capability components, with `ClientEntityComponent` as the owner for the currently applied client-entity reference and its derived runtime caches.
- `RenderControllerEntry` stays definition-only. Render-controller runtime may cache controller-local derived state, but it must not own or bootstrap client-entity runtime.
- `EntityRenderSystem` is the composition seam that applies client-entity context to render-controller definitions and produces render/model components.

## Boundary Notes For Importer Expansion
- `io.github.tt432.eyelibimporter.particle.BrParticle` is the canonical raw Bedrock particle schema/codec owner; do not introduce a second particle-module `BrParticle`.
- `io.github.tt432.eyelibparticle.runtime.ParticleDefinition` is the canonical module runtime definition owner produced through `ParticleDefinitionAdapter`, with mapped fields: identifier, format version, basic render material/texture, curves, events, raw components, billboard flipbook summary, and Molang value preservation.
- Root `src/main/java/io/github/tt432/eyelib/client/particle/bedrock/BrParticle.java` has been deleted; `io.github.tt432.eyelibimporter.particle.BrParticle` remains the only raw Bedrock particle schema/codec owner.
- `:eyelib-importer` now uses its own importer namespace (`io.github.tt432.eyelibimporter.*`) so it can be consumed as the `eyelibimporter` Forge functional module/artifact without split packages against root runtime packages. It is not currently promised to be a plain JVM library; splitting plain importer core from Forge bootstrap is future debt.
- Client-entity/attachable schema ownership has already moved to `:eyelib-importer`, and controller reload/planner paths now parse importer-owned controller schema before adapting to root runtime controllers.
- Bedrock addon container handling (`manifest.json`, folder/archive pack discovery, texture decode, and importer-owned aggregate loading) belongs in `:eyelib-importer`; root should consume importer results instead of re-implementing pack traversal.
- Runtime executors such as `Animation<?>`, playback state owners, particle spawning, and entity/model runtime integration remain root-owned even when their source JSON codecs/definitions move out.
- Importer-owned image handling is now platform-free via `ImportedImageData`. `NativeImage` creation, upload, and other Minecraft/Blaze3D runtime wiring stay root-owned.
- Root loaders and manager tooling may call importer parsers, but they should not duplicate importer normalization logic once migration completes.
- The current animation save/sync boundary is intentionally binding-only: `AnimationComponent.SerializableInfo` carries selected animation names and Molang inputs, while per-instance playback/controller/effect state stays runtime-local and should be characterized before any refactor widens or relocates it.
- Runtime state extraction stays root-owned even after internal splits: owner/executor seams such as `BrClipStateOwner`/`BrClipExecutor` and `BrControllerStateOwner`/`BrControllerExecutor` are runtime implementation details, not importer concerns and not sync payload types.

## Boundary Notes For Particle Loading And Publication
- `ParticleDefinitionRegistry` is the module-owned active particle registry. Its publisher/store own active `ParticleDefinition` entries and key publication by `ParticleDefinition.identifier()`, not by JSON file path, source `ResourceLocation`, or other reload metadata.
- `ParticleResourcePublication` is the module-owned loading/publication service for Phase 12: it parses canonical importer schema `io.github.tt432.eyelibimporter.particle.BrParticle`, converts through `ParticleDefinitionAdapter`, reports failures/duplicates, and publishes valid definitions into the active module store.
- Root `BrParticleLoader` remains the Forge/resource reload adapter only. It preserves existing `particles/*.json` discovery plus `ClientLoaderLifecycleHooks` side-safe registration and converts root `ResourceLocation` source ids to diagnostic strings before delegating particle publication to module services.
- Root legacy `client/particle/bedrock/**` schema/runtime tree has been deleted, along with root `ParticleAssetRegistry`, `ParticleManager`, `ParticleLookup`, and `BrParticleRenderManager`. `ParticleSpawnService` remains the only root particle runtime compatibility adapter and may only adapt packet/module-definition callers plus root Minecraft/capability context into module client services. Root must not reintroduce canonical particle loading/publication business logic, particle-only spawn/runtime behavior, or root legacy `BrParticle` as an active publication schema.
- Phase 13 remains responsible for command/network rewires, and Phase 14 remains responsible for broad root/client smoke and visual verification evidence.
- Final v1.2 documentation must keep the same ownership map: `:eyelib-particle` owns module APIs, `ParticleDefinition`, `ParticleDefinitionAdapter`, executable runtime, client integration, render manager, particle packet contracts under `io.github.tt432.eyelibparticle.network`, and loading/publication through `ParticleDefinitionRegistry` plus `ParticleResourcePublication`; root owns `BrParticleLoader`, the temporary `ParticleSpawnService` context adapter, `mc/impl/common/command`, transport registration, and `NetClientHandlers` delegation; importer owns raw `io.github.tt432.eyelibimporter.particle.BrParticle`. Normal source tests must not read `.planning` files; PFUT-03 independent particle artifact publication, unrelated fixture cleanup, and manual visual proof remain non-blocking scope boundaries recorded separately from automated JetBrains MCP gates.

## Boundary Notes For Particle Command And Network Integration
- Phase 13 command/network integration preserves observable `/eyelib particle` syntax, suggestions, validation, spawn position fallback, success message, and string-keyed packet behavior without adding new user-visible command features.
- `ParticleCommandRuntime` owns deterministic platform-free suggestion filtering, spawn request construction, and success-message formatting.
- `src/main/java/io/github/tt432/eyelib/mc/impl/common/command/EyelibParticleCommand.java` owns Brigadier parsing, `ResourceLocationArgument`, `CommandSourceStack`, `ServerPlayer`, `Vec3Argument`, `Component`, active-name suggestions from `ParticleDefinitionRegistry`, and conversion from parsed `ResourceLocation` to string identifiers before request and packet seams.
- `eyelib-particle/src/main/java/io/github/tt432/eyelibparticle/network/` owns `SpawnParticlePacket(String spawnId, String particleId, Vector3f position)` and `RemoveParticlePacket(String removeId)` codec contracts.
- `src/main/java/io/github/tt432/eyelib/network/NetClientHandlers.java` remains context-free and delegates spawn/remove into `ParticleSpawnService`; `ParticleSpawnService` converts packets into module `ParticleSpawnRequest` and delegates particle-only spawn/runtime work into module `ParticleSpawnRuntimeAdapter`.
- Broad ClientSmoke/hardware visual evidence remains Phase 14 scope.

## Public vs Internal Bias
- Default assumption: most packages are internal unless intentionally surfaced through a stable facade.
- `EyelibMod` should remain the only Forge `@Mod` composition root, while top-level `Eyelib` stays constant-only and not a runtime gateway.
- Generated zones, tooling zones, and runtime implementation details should not be treated as public API.

## Current bootstrap surface inventory
| Symbol | Current role | Current classification |
|---|---|---|
| `MOD_ID` | mod identifier constant | public bootstrap constant |
| constructor `EyelibMod()` | startup wiring for capability registration and network registration | `mc/impl` bootstrap-only |

## Current Breaking-Refactor State
- Direct manager reach-through accessors have been removed from top-level bootstrap access points.
- Runtime reads now go through lookup seams such as `AnimationLookup` and `ModelLookup`; active particle definitions are read from `ParticleDefinitionRegistry`, and the legacy root `ParticleLookup` facade has been deleted.
- Loader/tooling publication now routes through domain-specific registry owners instead of `ClientAssetRegistry`.
- Earlier Phase 8 guidance established the Gradle/documentation boundary only; current FM-005 cleanup has since deleted root `BrParticleRenderManager` and root `BrParticle` runtime/schema ownership after module runtime/publication became authoritative.

## Breaking Refactor Rule
- Do not add new singleton reach-through methods to top-level `Eyelib`.
- Migrate remaining callers to domain-local read seams, then delete the legacy reach-through methods instead of preserving them for compatibility.
