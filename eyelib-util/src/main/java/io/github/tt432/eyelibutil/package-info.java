/**
 * Shared utility module boundary for Eyelib.
 * <p>
 * This leaf module owns the active utility packages for time, color, loader, math, search,
 * collection, resource, texture, codec, and streamcodec helpers under the non-split {@code io.github.tt432.eyelibutil} namespace.
 * It must not import root or sibling project modules. Minecraft/Forge APIs are allowed only when
 * utility ownership explicitly requires that platform-facing contract.
 */
@NullMarked
package io.github.tt432.eyelibutil;

import org.jspecify.annotations.NullMarked;
