/**
 * Client-only Eyelib implementation packages.
 * <p>
 * Rendering, models, animation, particles, loaders, managers, and tooling live here.
 * Treat these packages as internal unless a narrower facade explicitly exposes a stable contract.
 */
package io.github.tt432.eyelib.client;
