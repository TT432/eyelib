# Molang Grammar Legacy Marker

## Status
- Generated parser artifacts have been isolated to `src/main/java/io/github/tt432/eyelib/molang/generated/`.
- This directory now serves as a legacy marker because older discussion and plan documents may still mention `molang/grammer/`.

## Edit Rule
- Do not add handwritten logic here.

## Reading Rule
- If your task is about Molang runtime or compiler behavior, prefer handwritten `molang/` code first and open `../generated/` only when parser artifacts are actually relevant.
