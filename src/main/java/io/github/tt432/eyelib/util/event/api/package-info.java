/**
 * 反射调度注解与发现接口，对齐 {@code molang/mapping/api/} 范式。
 * 注解定义在 domain 层（零 MC），bridge 层用 (Neo)Forge 反射系统实现发现。
 */
@NullMarked
package io.github.tt432.eyelib.util.event.api;

import org.jspecify.annotations.NullMarked;
